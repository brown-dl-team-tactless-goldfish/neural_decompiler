#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
int is_leap_year(int year) {
return ((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0);
}