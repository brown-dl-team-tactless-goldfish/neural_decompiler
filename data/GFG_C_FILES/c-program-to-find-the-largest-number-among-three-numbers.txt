#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
int main()
{
int A, B, C;
scanf("%d %d %d", &A, &B, &C);
if (A >= B && A >= C)
if (B >= A && B >= C)
if (C >= A && C >= B)
return 0;
}