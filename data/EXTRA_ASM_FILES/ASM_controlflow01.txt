f:
        pushq   %rbp
        movq    %rsp, %rbp
        movl    a(%rip), %edx
        movl    b(%rip), %eax
        cmpl    %eax, %edx
        jge     .L2
        movl    $0, %eax
        jmp     .L3
.L2:
        movl    a(%rip), %eax
.L3:
        popq    %rbp
        ret
