main:
        pushq   %rbp
        movq    %rsp, %rbp
        subq    $16, %rsp
        movl    $99, -4(%rbp)
        movl    -4(%rbp), %eax
        cltq
        movq    %rax, %r9
        movl    $42, %r8d
        movl    $45, %ecx
        movl    $123, %edx
        movl    $100, %esi
        movl    $1, %edi
        call    f
        movl    $0, %eax
        leave
        ret
