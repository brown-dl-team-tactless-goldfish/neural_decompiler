sum:
        pushq   %rbp
        movq    %rsp, %rbp
        movq    a(%rip), %rax
        movl    %eax, %edx
        movl    b(%rip), %eax
        addl    %edx, %eax
        popq    %rbp
        ret
