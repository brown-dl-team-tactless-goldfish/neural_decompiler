is_palindrome:
        pushq   %rbp
        movq    %rsp, %rbp
        subq    $16, %rsp
        movl    %edi, -4(%rbp)
        movl    -4(%rbp), %eax
        movl    %eax, %edi
        movl    $0, %eax
        call    reverse_num
        cmpl    %eax, -4(%rbp)
        sete    %al
        movzbl  %al, %eax
        leave
        ret
