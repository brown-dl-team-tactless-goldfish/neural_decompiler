#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
int main()
{
int A;
scanf("%d", &A);
if (A > 0)
else if (A < 0)
else if (A == 0)
return 0;
}