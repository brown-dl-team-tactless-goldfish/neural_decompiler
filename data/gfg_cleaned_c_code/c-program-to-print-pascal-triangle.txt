#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
{
int arr[n][n];
for (int line = 0; line < n; line++)
{
for (int i = 0; i <= line; i++)
{
if (line == i || i == 0)
arr[line][i] = 1;
else
arr[line][i] = (arr[line - 1][i - 1] +
arr[line - 1][i]);
arr[line][i]);
}
}
}
int main()
{
int n = 5;
return 0;
}