#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
void pattern_fun(int row)
{
for (int j = 1; j <= row; j++)
{
for (int sp = 1; sp <= j - 1; sp++)
{
}
int last_col = (row * 2 - (2 * j - 1));
for (int k = 1; k <= last_col; k++)
{
if (j == 1 || k == 1)
else if (k == last_col)
else
}
}
}
int main()
{
int row = 7;
pattern_fun(row);
return 0;
}