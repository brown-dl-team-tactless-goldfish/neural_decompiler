#include<stdlib.h>
#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<stdint.h>
#include<math.h>
void floyd(int n)
{
int i, j = 1;
for (i = 1; i <= (n * (n + 1)) / 2; i++) {
if (i == (j * (j + 1)) / 2) {
j++;
}
}
}
int main() { floyd(6); }