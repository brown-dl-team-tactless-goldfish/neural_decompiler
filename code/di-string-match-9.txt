int* diStringMatch(char * S, int* returnSize){
    int n = strlen(S), *a = malloc(sizeof(int [*returnSize = n + 1])), i = 0;
    for (int b = 0, e = n++ ; n-- ; a[i++] = *S++ == 'D' ? e-- : b++);
    return a;
}