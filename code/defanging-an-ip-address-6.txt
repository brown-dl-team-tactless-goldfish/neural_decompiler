   char *defanged;
    int i,j=0;
    
    defanged = calloc(1, strlen(address) +6 +1);
    for(i=0;i<strlen(address);i++)
        if(address[i] == '.'){
            defanged[j++]='[';
            defanged[j++]='.';
            defanged[j++]=']';
        }
        else
            defanged[j++]=address[i];
    free(address);
    return defanged;
	``