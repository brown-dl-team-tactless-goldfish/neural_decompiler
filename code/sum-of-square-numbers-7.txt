bool judgeSquareSum(int c){
    int m=sqrt(c);
    int i;
    for(int a=0;a<=m;++a){
        long int b=round(sqrt(c-a*a)); //if use int will overflow
        if(a*a+b*b==c)
            return true;
    }
    return false;
}