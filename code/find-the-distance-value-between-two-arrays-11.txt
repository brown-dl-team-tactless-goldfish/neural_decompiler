int findTheDistanceValue(int* a, int n, int* b, int m, int d){

int count = 0, i, j;
for(i = 0; i<n; i++){ 
   j = 0;
    while(j<m && (abs(a[i] - b[j]) > d )  ){
        j++;
        if(j == m)
            count++;
     }
}
    return count;
}