long long pickGifts(vector<int>& gifts, int k) {
	priority_queue<int> pq(gifts.begin(), gifts.end());
	while (k--) {
		int pile = pq.top();
		pq.pop();
		pq.push(floor(sqrt(pile)));
	}
	long long sum = 0;
	while (!pq.empty()) {
		sum += (pq.top() % 1000000007);
		pq.pop();
	}
	return sum;
}