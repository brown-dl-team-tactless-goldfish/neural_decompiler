public class Solution {
    public int[] FindColumnWidth(int[][] grid) {
        int[] result = new int[grid[0].Length];

        foreach (int[] line in grid) 
            for (int i = 0; i < result.Length; ++i)
                result[i] = Math.Max(result[i], line[i].ToString().Length);
        
        return result;
    }
}