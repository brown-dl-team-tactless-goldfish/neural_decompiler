/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */


/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* postorderTraversal(struct TreeNode* root, int* returnSize){
    int* arr = malloc(sizeof(int) * 100);
    int idx[] = {0};
    *returnSize = inOrderTraversing(root,arr, idx);
    return arr;
}
int inOrderTraversing(struct TreeNode* root, int * arr, int* n)
{
    if(!root)
        return 0;
    if(root->left)a
    {
        inOrderTraversing(root->left, arr, n);
    }
    
    if(root->right)
    {
        inOrderTraversing(root->right, arr, n);
    }
    arr[(*n)++] = root->val;
    return *n;
}