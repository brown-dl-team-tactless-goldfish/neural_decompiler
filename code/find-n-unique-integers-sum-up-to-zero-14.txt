int* sumZero(int n, int* returnSize){
    int* ret=(int*)malloc(sizeof(int)*n);
    *returnSize=n;
    if(n%2==1) {
        ret[n/2]=0; //odd give one zero;
    }
    for(int i=0;i<(n/2);i++){
        ret[i]=rand();
        ret[n-1-i]=-ret[i];
    }
    return ret;
}
