bool checkOnesSegment(char * s){
    int n = strlen(s) ;
    int gap = 0 ;
    for(int i = 1 ; i < n ; i++){
        if(s[i] != s[i-1]){
            gap++ ;
            if(gap > 1)
                return false ;
        }
    }
    return true ;
}