
int minFlips(char * target){
    int ans = 0;
    int i;
    int len = strlen(target);
    char temp = '0';
    for (i = 0; i < len; i++) {
        if (temp != target[i]) {
            ans++;
            temp = temp == '0' ? '1' : '0';
        }
    }
    return ans;
}