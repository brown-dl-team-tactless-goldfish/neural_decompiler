class Solution {
public:
  string kthDistinct(vector<string>& arr, int k) {
    unordered_map<string, int>t;
    for(auto &w:arr) t[w]++;
    for(auto &w:arr)
      if(t[w] == 1) if(--k == 0) return w;
    
    return "";
  }
};