int* getRow(int rowIndex, int* returnSize){
    int nums = rowIndex+1;
    *returnSize = nums;
    int* res = (int*) calloc(nums, sizeof(int));
    res[rowIndex] = 0, res[0] = 1;
    for(int i = 0; i < nums; i++) 
        for(int j = i; j >= 1; j--)
            res[j] += res[j-1];
    return res;
}