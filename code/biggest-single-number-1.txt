# Write your MySQL query statement below
WITH c as (SELECT distinct num, count(num) cnt
FROM MyNumbers
GROUP BY num)
SELECT 
CASE 
    WHEN c.cnt = 1 THEN num 
    ELSE NULL
END AS NUM
FROM c
ORDER BY num DESC
LIMIT 1