if(t==NULL) return ;
*p=strcpy(*p,IntToStringNoEnd(t->val));
*p+=strlen(*p);
if(t->left!=NULL||t->right!=NULL)
{
    *(*p)++='(';
    trans(t->left,p);
    *(*p)++=')';
}
if(t->right!=NULL)
{
    *(*p)++='(';
    trans(t->right,p);
    *(*p)++=')';        
}