#include <string.h>
#include <stdbool.h>

char repeatedCharacter(char * s){
    bool appeared[26] = {false};
    for (int i = 0; i < strlen(s); i++) {
        if (appeared[s[i] - 'a'] == true) {
            return s[i];
        }
        appeared[s[i] - 'a'] = true;
    }
    
    return '\0';
}