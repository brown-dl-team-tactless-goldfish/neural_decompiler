#define min(A, B) (A < B ? A : B)
char *getSmallestString(int n, int k) {
  char *array = malloc(sizeof(char) * (n + 1));
  array[n] = '\0';
  memset(array, 'a', n);

  k -= n;
  while (k > 0) {
    array[--n] += min(25, k);
    k -= min(25, k);
  }
  return array;
}

