int maxRotateFunction(int* A, int ASize) {
    int sum=0,F=0;
    for(int i=0;i<ASize;++i){
        sum+=A[i];
        F+=i*A[i];
    }
    int max=F;
    for(int i=ASize-1;i>0;--i){
        F+=(sum-ASize*A[i]);
        if(max<F)
            max=F;
    }
    return max;
}