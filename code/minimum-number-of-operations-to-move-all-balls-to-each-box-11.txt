int len = strlen(boxes);int sum=0;
int *result = (int*)malloc(sizeof(int)*len);

for(int i=0; i<len;i++){
    sum=0;                          //reset sum to 0 each iteration of i 
    for(int j=0; j<len;j++){
        if(boxes[j] == '1' && (j!=i) ){
           sum += abs(i-j) ;        // add absolute values to get the total difference 
        }
        
    }
    result[i]=sum; // this is the difference needed for position i.
}
*returnSize=len;