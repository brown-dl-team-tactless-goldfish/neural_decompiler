int* sumZero(int n, int* returnSize){
    *returnSize = n;
    int val = -n/2, *a, i;
    a = (int*)malloc(sizeof(int)*n);
    for(i=0; i<n; i++){
        a[i]=val++;
    }
    if(n%2==0) a[i-1]+=val;
    return a;
}