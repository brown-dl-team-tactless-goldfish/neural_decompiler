int averageValue(int* nums, int numsSize) {
	register const char THREE = 3;
	register unsigned int i, sum = 0, ct = 0;
	for(i = 0; i < numsSize; i++) {
		if(nums[i] % 2 == 0 && nums[i] % THREE == 0) {
			sum += nums[i];
			ct++;
		}
	}
	if(!sum)
		return 0;
	return sum / ct;
}