int smallestRangeI(int* A, int ASize, int K) {
    int i;
    int min = 20000;
    int max = -1;
    for(i=0;i<ASize;i++){
        if(A[i]>max) max = A[i];
        if(A[i]<min) min = A[i];
    }
    max = max - K;
    min = min + K;
    if(min>max)return 0;
    return max-min;
}