	int fillCups(vector<int>& amount) {
        int a = amount[0];
        int b = amount[1];
        int c = amount[2];
        if(a > b + c || b > a + c || c > a + b) return max({a, b, c});
        return (a + b + c + 1) / 2;
    }