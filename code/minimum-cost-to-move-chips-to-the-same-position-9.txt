int minimumf(int a, int b)
{
    return a<b?a:b;
}

int minCostToMoveChips(int* position, int positionSize){
    
    int i,even=0,odd=0;
    for(i=0;i<positionSize;i++)
    {
        if(position[i]%2==0) even++;
        else odd++;
    }
    return minimumf(even,odd);
}