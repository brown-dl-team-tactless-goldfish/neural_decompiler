int binaryGap(int n){
    int start = -1, max = 0, idx = -1;
    while(n)
    {
        idx++;
        if(n&1 == 1)
        {
            if((start != -1) && (idx - start > max)) max = idx - start;
            start = idx;
        }
        n >>= 1;
    }
    return max;
}