char * makeGood(char * s){
    int left = 1;
    int right = 0;
    while (s[++right] != '\0') {
        if (right-left) s[left] = s[right];
        if (++left > 1 && ((s[left-1]-s[left-2] == 32) || (s[left-1]-s[left-2] == -32)))  left -= 2;
    }
    s[left] = '\0';
    return s;
}