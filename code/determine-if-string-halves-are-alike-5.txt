bool halvesAreAlike(char * s){
    register unsigned short half = strlen(s) / 2, i = 0, left = 0, right = 0;
	while(s[i] != '\0') {
		if (strchr("aeiouAEIOU", s[i])) {
			if(i < half)
				left++;
			else
				right++;
		}
		i++;
	}
	return (left == right);
}