int longestContinuousSubstring(char * s){
    int n = strlen(s);
    int l = 0, r = 0;
    int max = 1;
    for(r = 1; r < n; r++){
        if(s[r] != s[r-1]+1){
            max = fmax(max, r-l);
            l = r;
        }        
    }
    max = fmax(max, r-l);
    return max;
}