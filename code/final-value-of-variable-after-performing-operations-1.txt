int finalValueAfterOperations(char ** operations, int operationsSize){
    register short i, sum = 0;
	for(i = 0; i < operationsSize; i++)
        sum += ((!strcmp(operations[i], "++X") || !strcmp(operations[i], "X++")) ? 1 : -1);
	return sum;
}