    int mod = 1e9+7;
    
    for(auto x : s)
    {
        endsWith[x-'a']=accumulate(endsWith.begin(),endsWith.end(),1L)%mod;
    }
    
    long  sum = accumulate(endsWith.begin(),endsWith.end(), 0L) % mod;
    
    return sum;
}