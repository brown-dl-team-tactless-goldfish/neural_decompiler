bool* prefixesDivBy5(int* A, int ASize, int* returnSize){
    *returnSize = ASize;
    bool* arr = malloc(ASize * sizeof(bool));
    int sum = 0;
    for(int i=0; i<ASize; i++){
        sum = (sum * 2 + A[i]) % 5;
        arr[i] = sum == 0;
    }
    return arr;
}