class Solution {
public:
    int minMoves(vector<int>& nums) 
    {
      long long  sum=0;
      int minnum=INT_MAX;
      for(auto n:nums)
      {
        sum+=n;
        if(n<minnum)
        minnum=n;
      }
      return (sum-minnum*nums.size());
    }
};