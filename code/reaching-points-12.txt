 bool reachingPoints(int sx, int sy, int tx, int ty) {
        while(sx<=tx && sy<=ty){
            
            if(tx>ty) tx = tx%ty; 
            else  ty = ty%tx;
           // Edge Cases
           if(sy == ty) return (tx-sx) % ty == 0;
           if(sx == tx) return (ty-sy)%tx==0;
            
        }
     return false;
    }