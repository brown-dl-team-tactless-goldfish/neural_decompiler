bool carPooling(vector<vector<int>>& nums, int k) {
    vector<int>dp(1001,0);
    for(auto x:nums){
        for(int i=x[1];i<x[2];i++){
            dp[i] += (x[0]);
        }
    }
    int c = *max_element(dp.begin(),dp.end());
    return (c<=k);
}