static int num = 0;
int j;
strcpy(a[num],word);
num++; 
char* s = (char*)malloc((len+1)*sizeof(char));
strcpy(s,word);

for(int i = start;i<len+1;i++)
{
    if(isalpha(word[i]))
    {
        if(isupper(word[i]))
        {
            word[i] = tolower(word[i]);
            findAllPermutation(a,word,i+1,len);
        }
        else
        {
            word[i] = toupper(word[i]);
            findAllPermutation(a,word,i+1,len);
        }
    }
 strcpy(word,s);
}
