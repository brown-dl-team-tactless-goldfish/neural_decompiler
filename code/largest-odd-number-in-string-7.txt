class Solution 
{
public:
    string largestOddNumber(string nums)
    {
        int i = nums.length()-1;
        for(; i >= 0; i--)
        {
            if((nums[i]-'a') % 2 == 0)
                break;
        }
        return nums.substr(0, i+1);
    }
};