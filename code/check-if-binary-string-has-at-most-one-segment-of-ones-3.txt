

bool checkOnesSegment(char * s){
    if(s == NULL) return true;
    for(;*s != '\0' && *s != '0'; s++);    //read all the 1's
    if(*s == '\0') return true;
    for(;*s != '\0' && *s != '1'; s++);    //read all the 0's
    if(*s == '\0') return true;
    return false;
}