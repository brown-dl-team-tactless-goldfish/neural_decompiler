bool isPerfectSquare(int x)
{
	bool ret = false;
	if (0 <= x) {
		for (size_t foo = 0; (foo*foo) <= x; ++foo) {
		//no need to go further when x < foo**2 
			if ((foo*foo) == x) {
				ret = true;
				break;
			}
		}
	}
	return ret;
}