class Solution {
public:
    int minDeletion(vector<int>& a) {
        int n=a.size(),c=0,i=0,j=1;
        while(j<n)
        {
            if(a[i]==a[j])
                c++,j++;
            else
                i=j+1,j+=2;
        }
        if((n-c)%2)
            c++;
        return c;
    }
};