int guessNumber(int n){
    long result,sp,ep,mid;
    for(sp=1,ep=n;sp<=ep;)
    {
        mid=(sp+ep)/2;
        result=guess(mid);
        if(result==0) return mid;
        else if(result==-1) ep=mid-1;
        else sp=mid+1;
    }
    return -1;
}