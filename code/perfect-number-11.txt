bool checkPerfectNumber(int num)
{
    if(num==1)
        return false;
    
    int divisor = 2;
    int sum = 1;
    while( (divisor*divisor)<num)
    {
        if((num%divisor) == 0)
        {
            sum += divisor + num/divisor;
        }
        divisor++;
    }
    return (sum==num);
}