int* sumZero(int n, int* returnSize)
{
    int i;
    int* output = (int*)calloc(n, sizeof(int));  // calloc: Allocate and zero-initialize array
    *returnSize = n;
    for (i = 0; i < (n / 2); i++)
    {
        output[i] = i + 1;
        output[n - i - 1] = -(i + 1);
    }
    return output;
}