permutation(A,0,ASize-1);
char *a=(char *)calloc(6,sizeof(char));
if(hr==-1&&min==-1)
{
    a[0]='\0';
    return a;
}
if(hr<10&&min<10)
{
    sprintf(a,"0%d:0%d",hr,min);
}
else if(hr<10)sprintf(a,"0%d:%d",hr,min);
else if(min<10)sprintf(a,"%d:0%d",hr,min);
else
sprintf(a,"%d:%d",hr,min);
hr=-1;min=-1;
return a;