int minCostToMoveChips(int* position, int positionSize){
    int even = 0;
    for (int i=0; i<positionSize; i++) {
        if (position[i] % 2 == 0)
            even++;
    }
    int odd = positionSize - even;
    return even < odd ? even : odd;
}