 int stackLen;
 int* nextGreaterElements(int* nums, int numsSize, int* returnSize) {
*returnSize = numsSize;
int* ret = (int*)malloc(sizeof(int)*numsSize);
for (int i = numsSize*2-1; i >= 0; i--)
{
	while (stackLen > 0 && nums[i%numsSize] >= nums[stack[stackLen - 1]])
	{
		stackLen--;
	}
	ret[i%numsSize] = ( stackLen > 0 )?nums[stack[stackLen-1]]:-1;
	stack[stackLen++] = i%numsSize;
}
return ret;