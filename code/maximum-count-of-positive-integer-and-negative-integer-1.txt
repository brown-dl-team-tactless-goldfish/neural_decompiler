int maximumCount(int* nums, int numsSize) {
	if(nums[0] > 0 || nums[numsSize-1] < 0)
		return numsSize;
	
	register short i, pos = 0, neg = 0;
	for(i = 0; i < numsSize; i++) {
		if(nums[i] > 0)
			pos++;
		else if(nums[i] < 0)
			neg++;
	}
	if(pos > neg)
		return pos;
	return neg;
}