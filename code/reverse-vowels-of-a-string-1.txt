char *start, *end;
start = s;
end = s+strlen(s)-1;

while(start<end){
    while( (!(isVowel(*start))) && *start!='�' ) start++;
    while( !(isVowel(*end)) && end>start) end--;
    if( start<end ){
        char c = *start;
        *start = *end;
        *end = c;
    }
    ++start;
    --end;
};

return s;