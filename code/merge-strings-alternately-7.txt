

char * mergeAlternately(char * word1, char * word2){
    int i,j,k,len=strlen(word1)+strlen(word2);
    char*str=calloc(len+1,sizeof(char));
    for(i=j=k=0;k<len;){
        if(word1[i])
            str[k++]=word1[i++];
        if(word2[j])
            str[k++]=word2[j++];
    }
    return str;
}