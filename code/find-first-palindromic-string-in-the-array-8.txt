bool chk(char *a)
{
    for(int i=0;i<=strlen(a)/2;i++)if(a[i]!=a[strlen(a)-1-i])return false;
    return true;
}

char * firstPalindrome(char ** a, int n)
{
    for(int i=0;i<n;i++)if(chk(a[i]))return a[i];
    return "";
}