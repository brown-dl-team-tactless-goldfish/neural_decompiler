with cte_data as 
    (select Score, row_number() OVER (ORDER BY score desc) rn 
     from (select distinct score from Scores))
select s.score as "Score", c.rn as "Rank"
from Scores  s
inner join cte_data c on s.score = c.score
order by s.score desc