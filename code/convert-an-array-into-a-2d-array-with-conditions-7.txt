    vector<vector<int>> findMatrix(vector<int>& A) {
        int cnt[201]{}, sz = 0;
        for(auto e : A) sz = max(sz, ++cnt[e]);
        vector<vector<int>> res(sz);
        for(int i = 0; i < 201; i++) while(cnt[i] > 0) res[--cnt[i]].push_back(i);
        return res;
    }