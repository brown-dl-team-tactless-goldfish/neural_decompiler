for(i=1 ; i < pricesSize ; i++) 
{
    if(prices[i] > minimun)
    {    
        mprofit = (prices[i]-minimun >= mprofit) ? (prices[i]-minimun) : mprofit;
    }
    else
        minimun = prices[i];
}    

return mprofit;