int sumOddLengthSubarrays(int* arr, int arrSize){
    int x=0, n=1;
while(n<=arrSize){
        for(int i=0;i<arrSize;i++){
            for(int j=i;j<n+i && n+i<=arrSize;j++)
            x=x+arr[j];
        }
    n+=2;
}
return x;
}
