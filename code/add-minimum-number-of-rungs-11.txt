int addRungs(vector<int>& v, int dist) {
    
     int prev=0;
     int ans=0;  
        
     for(int i=0;i<v.size();i++){
         int x=(v[i]-prev)/dist;    
         ans+=x;
         if((v[i]-prev)%dist==0)ans--;    
         prev=v[i];    
     }   
        