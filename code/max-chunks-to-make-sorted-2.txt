int maxChunksToSorted(int* arr, int arrSize){
    int running = 0, total = 0;
    for (int i = 0; i < arrSize; i++) {
        running += i - arr[i];
        total += (running == 0);
    }
    return total;
}