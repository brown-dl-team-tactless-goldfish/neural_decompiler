class Solution {
public:
    int maximumGroups(vector<int>& nums) {
        long long l = 1, h = nums.size(), re = 0;
        while(l<=h){
            long long m = (l+h)/2;
            if(m*(m+1)/2<=nums.size()){ re = m; l = m+1; }
            else h = m-1;
        }
        return re;
    }
};