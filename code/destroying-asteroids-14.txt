class Solution {
public:
    bool asteroidsDestroyed(int mass, vector<int>& arr) {
        sort(arr.begin(),arr.end());
        long long m = mass;
        for(auto it:arr){
            if(it <= m){
                m += it;
            }else{
                return false;
            }
        }
        return true;
    }
};