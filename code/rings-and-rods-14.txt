int countPoints(char * rings){
    char rods[10][3] = {0};
    int cnt = 0;
    for (int i = 1; i < strlen(rings); i += 2) {
        rods[rings[i] - 48][rings[i-1] % 3] = 'a';
    }
    for (int i = 0; i < 10; i++) {
        cnt += (strncmp(rods[i],"aaa",3) == 0);  
    }
    return cnt;
}