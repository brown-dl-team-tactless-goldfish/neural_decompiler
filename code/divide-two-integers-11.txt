

int divide(int dividend, int divisor){
    long long int a = dividend;
    long long int b = divisor;
    
    if(a/b > pow(2,31)-1){
        return (pow(2,31)-1);
    }else{
        return(a/b);
    }
    return a/b;
}