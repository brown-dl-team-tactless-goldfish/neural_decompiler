int guessNumber(int n){
	int low=1;
    int high= n;
    int mid;
    int res;
    while(low<=high){
        mid=  low + (high - low) / 2;
        res=guess(mid);
        if(res==0)
            return mid;
        else if(res<0)
            high=mid-1;
        else
            low=mid+1;
    }
    return -1; 
}