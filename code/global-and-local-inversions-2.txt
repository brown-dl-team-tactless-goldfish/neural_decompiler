bool isIdealPermutation(int* A, int ASize) {
    int i,j,locals=0,globals=0;
    for (i=0;i<ASize-1;i++){
        if(A[i]>A[i+1]) locals++;
        for(j=i+1;j<ASize;j++){
            if (A[i]>A[j]) globals++;
        }
    }
    return (locals==globals?1:0);
}