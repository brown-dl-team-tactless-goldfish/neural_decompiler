class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        int e = 0, o = 0;
        for(int i = 0; i < piles.size(); i+=2) {
            e += piles[i];
            o += piles[i + 1];
        }
        return e != o;
    }
};