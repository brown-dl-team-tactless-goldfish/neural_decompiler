int countGoodNumbers(long long n){
    long long int mod = 7+1e9,twnty=20,ans = (n&1)*5 + 1*(!(n&1));
    
    while(n/=2){
        ans = (n&1) ? ((ans%mod)*(twnty%mod))%mod : ans;
        twnty = ((twnty%mod)*(twnty%mod))%mod;
    };
    
    return ans;
}