    def unequalTriplets(self, A: List[int]) -> int:
        trips = pairs = 0
        count = Counter()
        for i,a in enumerate(A):
            trips += pairs - count[a] * (i - count[a])
            pairs += i - count[a]
            count[a] += 1
        return trips