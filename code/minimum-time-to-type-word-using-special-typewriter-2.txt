int val=0,min=0,val_i=0;

for(int i=0;i<strlen(word);i++)
{
    if(i==0)
        min = word[i]-'a'>26-(word[i]-'a')?26-(word[i]-'a'):word[i]-'a';
    else
    {
        val_i=abs(word[i]-word[i-1]);
        min = val_i>26-val_i?26-val_i:val_i;
    }
    val+=min;
}
val+=strlen(word);

return val;