

int countSegments(char * s){
    int ans=0;
    int lastsp=1;
    for(int i=0;i<strlen(s);i++)
    {
        if(*(s+i)!=' '&&lastsp==1)
        {
            ans++;
            lastsp=0;
        }
            
        if(*(s+i)==' ')
            lastsp=1;
    }
    return ans;
}