

char * destCity(char *** paths, int pathsSize, int* pathsColSize){

    char *city = paths[0][0];
    for (int i = 0; i < pathsSize; i++) {
        if (!strcmp(paths[i][0], city)) { //equal
            city = paths[i][1];
            i = 0;
        }
    }
    return city;
}