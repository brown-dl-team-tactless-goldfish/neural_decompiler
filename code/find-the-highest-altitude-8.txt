int largestAltitude(int* gain, int gainSize)
{
    for(int i=1; i<gainSize; i++)
        gain[i] += gain[i-1];
    
    int ans=0;
    for(int i=0; i<gainSize; i++)
        if(ans<gain[i])
            ans = gain[i];
    
    return ans;
}