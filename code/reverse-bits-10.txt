uint32_t reverseBits(uint32_t n) {
    if(n==0){return n;}
    uint32_t temp=0;
    uint32_t ret=0;
    for(int i=0;i<32;i++){
        temp=(uint32_t)pow(2,31-i);
        if(temp&n){
            ret=ret+pow(2,i);
        }
    }
    return ret;
}