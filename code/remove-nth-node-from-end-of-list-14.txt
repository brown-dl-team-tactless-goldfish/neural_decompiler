struct ListNode* p=head,*q=head;
int i=1,k=1;
if(head==NULL)
    return NULL;
while(p->next)
{
    p=p->next;
    k++;
}
printf("\n%d",k);
int m=k-n+1;
if(m==1)
{
    p=q;
    q=q->next;
    free(p);
    return q;
}
else
{
    p=head;
    while(p && i<m)
    {
        q=p;
        p=p->next;
        i++;
    }
        q->next=p->next;
}
return head;