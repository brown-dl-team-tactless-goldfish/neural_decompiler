int* findArray(int* pref, int prefSize, int* returnSize){
    *returnSize=prefSize;
    int sum=0;
    int *e=(int*)malloc(prefSize*sizeof(int));
    for(int i=0;i<prefSize;i++)
    {
        e[i]=sum^pref[i];
        sum=pref[i];
    }
    return e;
}