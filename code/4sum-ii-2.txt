        foreach (var c in C)
        {
            foreach (var d in D)
            {
                if (twoSums.ContainsKey(c + d))  rs += twoSums[c + d];
            }
        }
        return rs;
}