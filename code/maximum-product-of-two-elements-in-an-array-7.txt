int maxProduct(int* nums, int numsSize){
    int pr=0;
    for(int i=0;i<numsSize;i++)
    {
        for(int j=i+1;j<numsSize;j++)
        {
            if(pr<(nums[i]-1)*(nums[j]-1)) pr=(nums[i]-1)*(nums[j]-1);
        }
    }
    return pr;
}