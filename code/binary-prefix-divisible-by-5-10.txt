

/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
bool* prefixesDivBy5(int* nums, int numsSize, int* returnSize){
    int i,sum=0;
    bool * retarr = malloc(sizeof(bool)*numsSize);
    for(i=0;i<numsSize;i++)
    {
        
        sum=(sum*2+nums[i])%5;
        
        retarr[i]=(sum==0);
        
    }
    
    *returnSize=numsSize;
    return retarr;
}