public int PartitionString(string s) => s
    .Select(c => 1 << c - 'a')
    .Aggregate(
        (count: 0, mask: ~0),
        (acc, next) => (acc.mask & next) == 0 ?
            (acc.count, acc.mask | next) :
            (++acc.count, next),
        acc => acc.count
    );