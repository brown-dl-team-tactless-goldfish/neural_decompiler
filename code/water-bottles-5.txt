int empty = 0;
int full = numBottles;
int answer = 0;
int temp;
while(1)
{
    temp = full;
    if(full <= 0)
    {
        break;
    }
    else
    {
        answer += temp;
        full = (temp+empty) / numExchange;
        empty = (temp+empty) % numExchange;
    }
}
return answer;