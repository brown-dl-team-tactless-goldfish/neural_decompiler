    long long appealSum(string s) {
        long long res = 0, n = s.size();
        vector<int> vpre(128, -1);
        for(int i=0;i<n;i++){
            int& pre = vpre[s[i]];
            res += (i-pre) * (n-i);
            pre = i;
        }
        return res;
    }