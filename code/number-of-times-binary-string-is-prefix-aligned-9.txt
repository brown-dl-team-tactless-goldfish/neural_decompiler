class Solution {
public:
    int numTimesAllBlue(vector<int>& flips) {
        
    int count = 0;
    int n=flips.size();
    int sum=0;
    for (int i = 0; i < flips.size(); i++)
    {
        sum=max(sum,flips[i]);
        if(sum==i+1)
            count++;
    }
    return count;

    }
};