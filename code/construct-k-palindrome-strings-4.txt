bool canConstruct (char *s, int k) {
  int  n, pairs = 0;
  char map[128] = {0};

  for (n = 0; s[n]; n++) {
    if (map[s[n]]) {
      pairs++;
      map[s[n]] = 0;
    } else {
      map[s[n]] = 1;
    }
  }

  return (k >= n) && (pairs >= (n - k + 1) / 2);
}