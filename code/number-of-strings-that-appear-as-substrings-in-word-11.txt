class Solution {
public:
    int numOfStrings(vector<string>& p, string w)
    {
        int count=0;
        for(auto it:p)
        {
            if(w.find(it)!=string::npos)
            {
                count++;
            }
        }
        return count;
    }
};