

bool checkIfExist(int* arr, int arrSize){
for(int i=0;i<arrSize;i++)
{
    double a=arr[i];
    for(int j=0;(j<arrSize && j!=i);j++)
    {
        double b=arr[j]/2.0;
        double c=arr[j]*2.0;
        if(a==b || a==c)
            return true;
    }
    
}
    return false;
}