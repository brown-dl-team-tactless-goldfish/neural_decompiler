select 
round(SUM(case when DATEDIFF(A.event_date, C.min_dt) =1 then 1 else 0 end) 
/count( distinct A.player_id),2) as fraction

from Activity A left join
(  
select
player_id, min(event_date) as min_dt
from Activity
group by 1 ) C using (player_id) 