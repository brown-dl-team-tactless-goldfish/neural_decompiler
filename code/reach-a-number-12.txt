class Solution {
public:
    int reachNumber(int target) {
        target = abs(target);
        for (int i = 1; target; i++) {
            target -= i;
            if(target <= 0 && ~target & 1)
                return i;
        }
        return 0;
    }
};