SELECT customer_number
FROM Orders
GROUP BY customer_number
HAVING COUNT(order_number)=(
                            SELECT TOP 1 COUNT(order_number) as c
                            FROM Orders
                            GROUP BY customer_number
                            ORDER BY c DESC
                            )```