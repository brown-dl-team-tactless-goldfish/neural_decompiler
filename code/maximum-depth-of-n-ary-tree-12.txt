    int maxDepth(Node* root) {
        int currentMax =0;
        
        if(!root) return currentMax;
        
            for(auto c : root->children)
                
                currentMax = max(currentMax, maxDepth(c));
        
        return currentMax+1;
    }