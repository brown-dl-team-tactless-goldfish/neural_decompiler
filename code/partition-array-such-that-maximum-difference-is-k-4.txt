    def partitionArray(self, A, k):
        A.sort()
        res, j = 1, 0
        for i in range(len(A)):
             if A[i] - A[j] > k:
                res += 1
                j = i
        return res