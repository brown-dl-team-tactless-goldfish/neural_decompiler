

char * restoreString(char * s, int* indices, int indicesSize){
    char * s2 = (char *) malloc (sizeof(char) * (indicesSize+1));
    strcpy(s2,s);
    int i;
    for(i = 0; i < indicesSize; i++) {
        s[indices[i]] = s2[i];
    }
    s[i] = '\0';
    free(s2);
    return s;

}