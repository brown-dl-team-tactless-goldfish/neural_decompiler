int minDeletion(vector<int>& v)
{   
        int c = 0;
        int n = v.size();
        for (int i = 0; i <n-1 ; i++)
            if (v[i] == v[i + 1] &&  i% 2 == c%2 ) c++;
        
        return c + (n- c) % 2;
 }
	