vector<string> divideString(string s, int k, char fill) 
{
	vector<string> total;
	int n=s.length();
	for(int i=0;i<n;i+=k) total.push_back(s.substr(i,k));

	if(total.back().length()!=k)
	{
		int t=total.back().length();
		for(int i=0;i<k-t;i++) total.back()+=fill;
	}
	return total;
}