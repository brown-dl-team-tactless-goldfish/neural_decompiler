int removeDuplicates(int* nums, int numsSize){
   int i, k;
   
   if(numsSize <= 1) {
      return numsSize;
   }
   for(i = 1, k = 1; i < numsSize; i++) {
      if(nums[i] != nums[i - 1]) {
         nums[k] = nums[i];
         k++;
      }
   }
   return k;
}