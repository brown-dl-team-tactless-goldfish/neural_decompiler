int
longestPalindrome(char *s)
{
	static int freq[128];
	int mirror, center, i;

	(void)memset(freq, 0, sizeof(freq));
	while (*s != '\0')
		++freq[*s++];
	mirror = center = 0;
	for (i = 0; i < 128; ++i) {
		mirror += freq[i] / 2;
		center |= freq[i] & 1;
	}

	return mirror * 2 + center;
}