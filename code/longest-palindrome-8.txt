int longestPalindrome(char* s) {
    int a[150]={0},count=0,rr=0;
    int len = strlen(s);

    for(int i=0;i<len;i++)
    {
        a[s[i]+0]++;
        if(a[s[i]+0]%2==0)
            count++;
    }
    if(2*count != len & len>2)
        rr++;
    
    return len>2? 2*count+rr: count+rr+1;
}