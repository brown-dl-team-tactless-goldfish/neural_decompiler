#define MIN(a,b) (((a)<(b))?(a):(b))

int minCostToMoveChips(int* chips, int chipsSize){
    int i,P=0,I=0;
    for(i=0; i<chipsSize; i++)
        chips[i] % 2 == 0 ? P++ : I++;  
    return MIN(P,I);
}
