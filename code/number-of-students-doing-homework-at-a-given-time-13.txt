int busyStudent(int* startTime, int startTimeSize, int* endTime, int endTimeSize, int queryTime){
    int i, total=0;
    for(i=0; i<startTimeSize; i++){
        if((startTime[i]-queryTime)*(endTime[i]-queryTime)<=0) total++;
    }
    return total;
}