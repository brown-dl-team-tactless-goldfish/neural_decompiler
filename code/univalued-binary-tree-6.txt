set<int> s;

void check(TreeNode* root)
{
    if(root == 0){
        
        return;
    }
    
    s.insert(root->val);
    check(root->left);
    check(root->right);
}
bool isUnivalTree(TreeNode* root) {
    
    check(root);
    
    return s.size( ) == 1;
    
}