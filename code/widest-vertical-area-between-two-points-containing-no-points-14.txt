class Solution {
public:
    int maxWidthOfVerticalArea(vector<vector<int>>& a) {
        int i,n=a.size(),maxi=0;
        sort(a.begin(), a.end());
        for(i=0;i<n-1;i++){
            maxi=max(maxi, a[i+1][0]-a[i][0]);
        }
        return maxi;
    }
};