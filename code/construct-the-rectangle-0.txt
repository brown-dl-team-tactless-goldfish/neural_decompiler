int* constructRectangle(int area, int* returnSize) {
    int* ReturnArray=calloc(2,sizeof(int));
    * returnSize=2;
    int i=sqrt(area);
    while(i){
    	if(!(area%i)){
    		*ReturnArray=area/i;
    		*(ReturnArray+1)=i;
    		break;
    	}
    	i--;
    }
    return ReturnArray;
}