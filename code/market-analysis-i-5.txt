
with cte as (
	select buyer_id, count(buyer_id) orders_in_2019
	from Orders
	where year(order_date) = 2019
	group by buyer_id )
select user_id  as buyer_id, join_date  , coalesce(orders_in_2019,0) orders_in_2019
from Users u left join cte c on u.user_id=c.buyer_id;
