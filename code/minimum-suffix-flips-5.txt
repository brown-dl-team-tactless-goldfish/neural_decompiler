class Solution {
public:
    int minFlips(string target) {
        char c = '0';
        int ans = 0;
        for(char ch : target) {
            if(ch == c) continue;
            c = c == '1' ? '0' : '1';
            ++ans;
        }
        return ans;
    }
};