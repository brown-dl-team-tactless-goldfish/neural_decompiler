char repeatedCharacter(char * s){
    int H,i,x;
    for(i=0;s[i]!='\0';i++){
        x=1;
        x=x<<(s[i]-97);
        if(x & H) return s[i];
        else
            H = H | x;
    }
    return s[i];
}