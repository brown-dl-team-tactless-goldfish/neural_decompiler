int findNumbers(int* nums, int numsSize){
    int c = 0, *n = nums, sz = numsSize;
    while (sz--)
        c += (*n == 100000) || (*n >= 10 && *n <= 99) || (*n >= 1000 && *n <= 9999), n++;
    return c;
}