int kthFactor(int n, int k){

    int i, index;

    index = 0;
    for(i=1; i<=n; i++) {

        if(n%i == 0)
            index++;

        if(index == k)
            return i;
    }

    return -1;
}