class Solution {
public:
    int minCostToMoveChips(vector<int>& chips) {
       vector<int> count(2, 0);
        for (auto & c : chips) {
            count[c&1]++;
        }
        return min(count[0], count[1]);
    }
};