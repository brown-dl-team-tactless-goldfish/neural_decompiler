class Solution {
public:
int findMiddleIndex(vector<int>& ar){
    int n=ar.size(),sum=0,tot=0;
    for(auto it:ar) tot+=it;
    for(int i=0;i<n;i++){
        sum+=ar[i];
        if(sum-ar[i]==(tot-sum)) return i;
    }
    return -1;
}
};