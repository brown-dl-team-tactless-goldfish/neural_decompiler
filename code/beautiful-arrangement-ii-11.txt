    vector<int> res;
    
    int a = ceil(k/2.0);
    
    int b = a+1;
    
    for(int i=0; i < n; i++)
    {
        if(i < k && k%2 != i%2)
        {
           res.push_back(a--) ;
        }
        else
        {
            res.push_back(b++);
        }
    }
    
    return res;
    
}