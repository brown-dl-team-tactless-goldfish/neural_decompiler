class Solution {
public:
    int distinctAverages(vector<int>& nums) {
        sort(begin(nums),end(nums));
		set<double> st;
        int end = nums.size()&1?nums.size()/2:nums.size()/2-1;
		for(int i=0; i<=end; ++i)
			st.insert((nums[i]+nums[nums.size()-i-1])/2.0);
		return st.size();
    }
};