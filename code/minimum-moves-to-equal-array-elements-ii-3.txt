int cmpfun(const void* a,const void* b)
{
    return *(int *)a-*(int *)b;
}
int minMoves2(int* nums, int numsSize){
    qsort(nums,numsSize,sizeof(int),cmpfun);
    int mid=nums[numsSize/2],ret=0;
    for(int i=0;i<numsSize;i++)
        ret+=mid-nums[i]>0?mid-nums[i]:nums[i]-mid;
    return ret;
}