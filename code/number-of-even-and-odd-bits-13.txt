class Solution 
{
public:
    vector<int> evenOddBit(int n) 
    {
        std::vector<int> res(2, 0);
        bool checker = false;
        while (n)
        {
            res[checker] += (n & 1);
            checker = !checker;
            n >>= 1;
        }

        return res;
    }
};