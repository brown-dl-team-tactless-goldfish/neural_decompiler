int minDeletionSize(char ** strs, int strsSize){
    
    int ct = 0;
    int j = 0;
    for(char* s = *strs; *s != '\0'; s++, j++)
    {
        for(int i = 0; i < strsSize-1; i++)
        {
            if( strs[i][j] > strs[i+1][j] )
            {
                ct++; break;
            }
        }
    } 
    return ct;
    
}