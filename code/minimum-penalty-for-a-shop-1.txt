int bestClosingTime(const char * a){
    int h = 0, m = 0;
    int s = 0;
    for (int i = 0; a[i]; i++) {
        s += (a[i] == 'Y') ? 1 : -1; // current profit
        if (s > m) { m = s; h = i+1; }
    }
    return h;
}