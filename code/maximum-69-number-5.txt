int maximum69Number (int num){
    int len, ret;
    char str[50];
    sprintf(str, "%d", num);
    len = strlen(str);
    for(int i = 0; i < len; i++)
    {
        if(str[i] == '6')
        {
            str[i] = '9';
            break;
        }
    }
    ret = atoi(str);
    return ret;
}