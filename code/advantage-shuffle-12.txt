    multiset<int> st(nums1.begin(),nums1.end());
    
    for(int i=0; i<nums1.size(); i++)
    {
        auto it = st.upper_bound(nums2[i]);
        
        if(it == st.end( ))
        {
            it = st.begin();
        }
        
        nums1[i] = *it;
        
        st.erase(it);
    }
    
    return nums1;
    
}