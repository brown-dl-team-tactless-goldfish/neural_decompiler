class Solution {
public:
    int minSwaps(string s) {
        int opening = 0, closing = 0;
        
        for(char c : s){
            if(c == '[') opening++;
            
            else{
                if(opening) opening--;
                else closing++;
            }
        }
        
        return (closing + 1) / 2;
    }
};