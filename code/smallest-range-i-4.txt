int smallestRangeI(int* A, int ASize, int K){

    int i,max=A[0],min=A[0];
    
    for(i=1;i<ASize;i++){
        if(A[i]>max) max = A[i];
        if(min>A[i]) min = A[i];
    }
    
    if(max-min-2*K<0) return 0;
    
    return max-min-2*K;
    
}