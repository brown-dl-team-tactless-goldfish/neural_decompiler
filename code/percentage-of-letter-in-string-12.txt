class Solution {
public:
    int percentageLetter(string s, char letter) {
        int m = 0, n = s.length();
        for (auto& c : s)
            if (c == letter)
                m++;
        return (m * 100 / n);
    }
};