
#define MAX(a,b) ((a)>(b)?(a):(b))

int maxDepth(struct TreeNode* root){
    int r=0,l=0;
    if (root == NULL)
        return 0;
    
    r = maxDepth(root->right);
    l = maxDepth(root->left);
    
    return MAX(r,l)+1;
}
