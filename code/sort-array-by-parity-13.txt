int* sortArrayByParity(int* A, int ASize, int* returnSize){
    *returnSize = ASize;
    int *B = (int*)malloc(ASize*sizeof(int));
    int i,n_even=0;
    
    for(i=0;i<ASize;i++){
        A[i] % 2 == 0 ? (B[n_even++]=A[i]) : (B[ASize-(i-n_even)-1]=A[i]);
    }
    return B;
}