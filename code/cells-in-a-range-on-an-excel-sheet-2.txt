pub fn cells_in_range(s: String) -> Vec<String> {
        let sb:Vec<char> = s.chars().collect();
        (sb[0]..=sb[3])
            .flat_map(|col| (sb[1]..=sb[4]).map(move |row| format!("{}{}", col, row)))
            .collect()
}