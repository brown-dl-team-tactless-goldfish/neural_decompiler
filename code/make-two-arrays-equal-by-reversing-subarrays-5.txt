
bool canBeEqual(int* target, int targetSize, int* arr, int arrSize){
    int j;
    for(int i=0; i<arrSize;i++){
        for(j=0;j<arrSize;j++){
            if(arr[i]==target[j]) {
                target[j]=0;
                break;
            }
        }
        if(j==arrSize) return false;
    }
    return true;
}
``

With the rules provided is essentially a bubble sort, 
this means that if the array contains the same elemets from any order we can arrive to any order. 
So we just have to check if the array contains the same elements.