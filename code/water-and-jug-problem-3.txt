int gcd(int a, int b){return a?gcd(b%a,a):b;}

bool canMeasureWater(int x, int y, int z)
{
    if (z > y + x) return false;
    if (z == y || z == x || z == 0 || z == x + y) return true;
    return !(z%gcd(x,y));
}