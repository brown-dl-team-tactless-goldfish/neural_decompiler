 int eliminateMaximum(vector<int>& dist, vector<int>& speed) {
	int min = 0, N = dist.size();
	for(int i=0; i<N; i++) {
		dist[i] = ceil(1.0*dist[i]/speed[i]);
	}
	sort(dist.begin(), dist.end());
	for(int i=0; i<N && dist[i] - min > 0; i++) min++;
	return min;
}