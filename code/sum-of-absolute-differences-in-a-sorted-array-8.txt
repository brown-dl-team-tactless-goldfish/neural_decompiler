    int[] ret = new int[nums.Length];   
        
    for (int i = 0; i < nums.Length; i++)
    {
        int n = nums[i];
        ret[i] = nums.Sum(x => Math.Abs(x - n));
    }

    return ret;
    
}