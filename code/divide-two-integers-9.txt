 long int divide(long long int dividend, long long int divisor)
{
    long long int s=dividend/divisor;
    if((divisor==-1 && dividend<0) && (dividend!=divisor))
    {
        return s-1;
    }
    return s;
}
        
