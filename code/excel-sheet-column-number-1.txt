

int titleToNumber(char * columnTitle){
/*  //>>> solution 1
    int len = strlen(columnTitle);
    int ans = 0;

    for(int i =0 ; i< len; i++){
        ans = ans*26 + (columnTitle[i] - 'A' + 1) ;
    }
    
    return ans;
*/ //<<< 
   int ans = 0;
    while(*columnTitle){
        ans = ans*26 + ((*columnTitle) - 'A' + 1) ;
        columnTitle++;
    }
    return ans;
}