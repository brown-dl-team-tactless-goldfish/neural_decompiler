int findTheDistanceValue(int* arr1, int arr1Size, int* arr2, int arr2Size, int d){
    for (int i = 0, j, c = 0 ; i < arr1Size || (d = c, 0) ; c += j == arr2Size, i++)
        for (j = 0 ; j < arr2Size && abs(arr1[i] - arr2[j]) > d ; j++);
    return d;  
}