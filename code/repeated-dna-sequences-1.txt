public class Solution {
    public IList<string> FindRepeatedDnaSequences(string s) 
    {
        Dictionary<string, int> seqCounts = new Dictionary<string, int>();
        string sequence;
        int bIndex, indexOfSeq;
        
        bIndex = 0;
        
        if (s.Length <= 10)
            return answer;
        
        while (bIndex + 9 <= s.Length - 1)
        {
            sequence = s.Substring(bIndex, 10);
            
            if (!seqCounts.ContainsKey(sequence))
                seqCounts.Add(sequence, 1);
            else
                seqCounts[sequence]++;

            bIndex++;
        }
        
        return seqCounts.Where(x => x.Value > 1).Select(x => x.Key).ToList();
    }
}