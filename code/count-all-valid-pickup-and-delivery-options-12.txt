    int countOrders(int n) {
        long count = 1, mod = 1e9 + 7;
        for(int i = 2; i <= n; i++) {
            long len = (i-1)*2;
            long t = count*(len+1 + (len+1)*len/2)%mod;
            count = t;
        }
        return (int)count;
    }