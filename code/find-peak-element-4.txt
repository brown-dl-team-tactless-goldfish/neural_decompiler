int
findPeakElement(const int *const nums, int numsSize)
{
	int *base, *p;

	base = nums;
	for (--numsSize; numsSize > 0; numsSize >>= 1) {
		p = base + (numsSize >> 1);
		if (*p < *(p + 1)) {
			base = p + 1;
			--numsSize;
		}
	}

	return base - nums;
}