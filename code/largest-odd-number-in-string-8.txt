char * largestOddNumber(char * num){
    int i=0,len=strlen(num)-1;
    while(len>=0)
    {
        if(num[len]%2!=0) break;
        else 
        {
            len--;
            num[len+1]='\0';
        }
        
    }
    return num;
}