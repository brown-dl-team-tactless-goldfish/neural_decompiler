int minSteps(char * s, char * t){
    int hmap[26] = {}, res = 0;
    
    while (*s) ++hmap[*s++ - 'a'];
    while (*t) --hmap[*t++ - 'a'];
    
    for (int i = 0; i < 26; ++i)
        res += (hmap[i] > 0) ? hmap[i] : -hmap[i];
    
    return res;
}