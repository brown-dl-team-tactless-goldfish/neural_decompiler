class Solution {
public:
    int minTimeToType(string word) {
        int time = 0;
        char curr = 'a';
        for (const auto& c : word) {
            const int diff = abs( c - curr );
            time += min( diff, 26 - diff ) + 1;
            curr = c;
        }
        return time;
    }
};