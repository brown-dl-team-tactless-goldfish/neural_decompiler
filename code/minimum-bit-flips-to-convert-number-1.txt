int minBitFlips(int start, int goal)
{
    int n = 0;
    
    while ( start > 0 || goal > 0)
    {
        if ( (start & 1u) ^ (goal & 1u))
        {
            n++;
        }
        
        start = start >> 1;
        goal = goal >> 1;

    }
    
    return n;

}