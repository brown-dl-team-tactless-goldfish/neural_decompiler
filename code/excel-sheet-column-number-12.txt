int titleToNumber(char* columnTitle)
{
    int ret = 0;
    for (int i = 0; i < columnTitle[i]; i++)
    {
        ret *= 26;//on subsequent chars, existing value increases x26
        ret += (columnTitle[i] - 64);//ascii adjust
    }
    return ret;
}