class Solution {
public:
    using VI = vector<int>;
    int numTimesAllBlue(VI& A, int max = 0, int ans = 0) {
        for (auto i = 0; i < A.size(); ++i) {
            max = std::max(max, A[i]);
            if (max == i + 1)
                ++ans;
        }
        return ans;
    }
};