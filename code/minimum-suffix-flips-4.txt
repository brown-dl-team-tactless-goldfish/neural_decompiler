class Solution {
public:
    using VI = vector<int>;
    int minFlips(string s, char cur = '0', int cnt = 0) {
        for (auto c: s)
            if (cur != c)
                cur = c, ++cnt;
        return cnt;
    }
};