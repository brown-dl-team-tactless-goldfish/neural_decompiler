int maxNumberOfFamilies(int n, vector<vector<int>>& a) {
	unordered_map <int, int> m;
	for (auto &z : a)
		m[z[0]] |= (1 << z[1]);
	int ret = n * 2;
	for (auto &z : m) {
		int &b = z.second;
		ret -= (((((b & 60) > 0) + ((b & 960) > 0)) | ((b & 240) > 0)) + 1) >> 1;
	}
	return ret;
}