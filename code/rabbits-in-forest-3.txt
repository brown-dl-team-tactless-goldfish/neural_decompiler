int numRabbits(int* answers, int answersSize){
    int count=0,arr[1000]={0};
    for(int i=0;i<answersSize;i++)
        arr[answers[i]]++;
    
    for(int i=0;i<1000;i++)
        if(arr[i]!=0)
            count+=(i+1)*((arr[i]-1)/(i+1)+1); //get the upper bound
    return count;
}