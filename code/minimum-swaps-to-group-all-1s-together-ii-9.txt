    int minSwaps(vector<int>& a) {
        int len = count(begin(a), end(a), 1), res = len, N = a.size();
        for(int i = 0, cnt = 0; i < N+len; i++) {
            cnt += a[i%N];
            if(i >= len) cnt -= a[i-len];
            res = min(res, len-cnt);
        }
        return res;
    }