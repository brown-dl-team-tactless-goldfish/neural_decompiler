int mirrorReflection(int p, int q){
 int l,h,num,wide,count,count1;
   l=q;h=p;
    while(h%l!=0)
    { 
      num=l;
      
      l=h%l;
      h=num;
    }
    wide=(p*q)/l;
    count=wide/q;
  
    if(count%2==0)
    {
      return(2);
    }
    count1=wide/p;
      if(count1%2==0)
         return(0);
      else
         return(1);
  
 
 }
   