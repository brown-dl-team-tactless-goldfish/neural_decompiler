int maxDepth(char * s) {
    int i, cnt = 0, max = 0;
    
    for (i = 0; i < strlen (s); i++) {
        if (s[i] == '(')
            cnt++;
        else if (s[i] == ')')
            cnt--;
        max = (cnt > max) ? cnt : max;
    }
    return max;
}