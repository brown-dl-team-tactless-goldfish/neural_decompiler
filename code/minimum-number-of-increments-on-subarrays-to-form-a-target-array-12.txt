int minNumberOperations(vector<int>& tg) {
    int n=tg.size();
    vector<int>dp(n,0);
   dp[0]=tg[0];
   for(int i=1;i<n;i++)
   {
     dp[i]=max(dp[i-1],dp[i-1]+tg[i]-tg[i-1]);
   }
  return dp[n-1];
}