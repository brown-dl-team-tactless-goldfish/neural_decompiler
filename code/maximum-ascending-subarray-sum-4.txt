int maxAscendingSum(int* nums, int numsSize){
   int max=nums[0],sum=nums[0];
    int i;
        for(i=1;i<numsSize;i++){
            if(nums[i]<=nums[i-1])
            {
                sum=0;
            }
            sum+=nums[i];
            if(sum>max) max=sum;
        }
    return max;

}