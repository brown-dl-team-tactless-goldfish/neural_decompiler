int compare_ints(const void *a, const void *b) {
    int *p1 = (int*)a;
    int *p2 = (int*)b;
    return (*p2 - *p1);
}
int minimumCost(int* cost, int costSize){
    if (cost == NULL || costSize <= 0) {
        return 0; 
    }    
qsort(cost, costSize, sizeof(int), compare_ints);
for(int i=0;i<costSize;i++){
    printf("%d",cost[i]);
}
int costsum=0;
for(int i=0; i<costSize; i++){
    if((i+1)%3==0){
        continue;
    }    
    else  costsum += cost[i];   
}
return costsum;
}