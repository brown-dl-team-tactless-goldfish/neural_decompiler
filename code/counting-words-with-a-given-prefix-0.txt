int prefixCount(char** w, int s, char *p)
{
    int c = 0;
    char *a, *b;

    while(s--) 
    {
        for(a = *w++, b = p; *b && *a; a++, b++) if(*a != *b) break;
        if(!*b) c++;
    }

    return c;
}