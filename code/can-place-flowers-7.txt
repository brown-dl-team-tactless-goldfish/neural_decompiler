bool canPlaceFlowers(vector<int>& flowerbed, int n) {
	int c = 0, l = flowerbed.size();
	for (int i = 0, t ; i < l ; i++)
		if (!((i && flowerbed[i - 1]) + flowerbed[i] + (i + 1 < l && flowerbed[i + 1])))
			c++, i++;
	return n <= c;        
}