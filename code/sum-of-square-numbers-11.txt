class Solution {
    public boolean judgeSquareSum(int c) {
        int i,k,j;
        for(i=0;i<46340 && i*i<=c;i++){
            k=(int)Math.sqrt((double)(c-i*i));
            if(k*k==(c-i*i))
                return true;
        }
        return false;
    }
}