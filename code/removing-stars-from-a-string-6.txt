char * removeStars(char * s){
    int left = 0, right = 0, len = strlen(s);
    while(right < len){
        if(s[right]!='*')
            s[left++] = s[right];
        else
            left--;
        right++;
    }
    s[left] = '\0';
    return s;
}