 int cnt=0;
    int findMinFibonacciNumbers(int k) {
        if(k == 0)
            return 0;
        int a=1, b=1,c=a+b;
        while(c<=k){
            c = a+b;
            a = b;
            b = c;
        }
        cnt+=1;
        findMinFibonacciNumbers(k-a);
        return cnt;
    }