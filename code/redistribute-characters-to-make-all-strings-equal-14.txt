	bool makeEqual(vector<string>& words) {
        int alp[26] = {};
        for(string w : words)
            for(char c : w)
                ++alp[c - 'a'];
        for(int i = 0; i < 26; i++)
            if(alp[i] % words.size()) 
                return false;
        return true;
    }