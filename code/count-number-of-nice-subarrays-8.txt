int numberOfSubarrays(int* nums, int numsSize, int k){
  int ret = 0;
  for (int left = 0, *start = nums; numsSize--; ret += left) {
    if ((*nums++ & 1) && (!k || !--k)) {
      for (left = 1; !(*start++ & 1); ++left)
        ;
    }
  }
  return ret;
}