    for(j=i+1;j<numsSize;j++){
        currentsum+=nums[j];
        if(currentsum>maxsum)
           maxsum=currentsum;
    }
    if(j==numsSize){
    for(int k=0;k<i;k++){
        currentsum+=nums[k];
         if(currentsum>maxsum)
             maxsum=currentsum;
    }
   }
}
return maxsum;
