struct TreeNode* trimBST(struct TreeNode* root, int L, int R){
    if(!root) return NULL;
    root->left = trimBST(root->left, L, R);
    root->right = trimBST(root->right, L, R);
    if(root) root = root->val < L ? root->right : root->val > R ? root->left : root;
    return root;
}