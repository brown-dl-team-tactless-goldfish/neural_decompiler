int checkcount(int *arr,int x,int n){
    int count=0;
    for(int i=0;i<n;i++){
        if(x==arr[i])
            count++;
    }
    printf("%d ",count);
    return count;
}
int findSpecialInteger(int* arr, int arrSize){
    int count=0;
    for(int i=0;i<arrSize;i++){
        if(checkcount(arr,arr[i],arrSize)>arrSize/4)//1>9/4
            return arr[i];
    }
    return 0;
}