class Solution {
public:
    int minElements(vector<int>& nums, int limit, int goal) {
        long long dif = goal - accumulate(nums.begin(), nums.end(), 0ll);
        return (abs(dif) + limit - 1) / limit;
    }
};