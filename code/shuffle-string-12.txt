char * restoreString(char * s, int* indices, int indicesSize){
    
    char *output = (char*)malloc(sizeof(char)*(indicesSize+1));
    output[indicesSize]='\0';
    
    for(int i;i<indicesSize;i++)
    {
        output[indices[i]]=s[i];
    }

    return output;
}