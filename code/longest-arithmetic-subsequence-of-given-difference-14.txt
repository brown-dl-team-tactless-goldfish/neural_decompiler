    for(i=0;i<n;i++)
    {
        if( hashmap.count(arr[i]-diff) )
        {
            int curr = hashmap[arr[i]-diff];
            hashmap[arr[i]] = curr+1;
            ans = max(ans,curr+1);
            
        }
        else
            hashmap[arr[i]] = 1;
    }
    
    return ans;
}