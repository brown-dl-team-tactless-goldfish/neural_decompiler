char * restoreString(char * s, int* indices, int indicesSize){
    char * res = (char*)malloc(indicesSize * sizeof(res));
	register unsigned char i = 0;
	while(s[i] != '\0') {
		res[indices[i]] = s[i];
		i++;
	}
	for(i = 0; i < indicesSize; i++)
		s[i] = res[i];
	return s;
}