if(ASize == 1)
{
    return 0;
}

for(i = 0; i < ASize; i++)
{
    if(A[i] > max)
    {
       max = A[i];
    }
    if(A[i] < min)
    {
        min = A[i];
    }     
}

gap = max - min;

if(gap <= K * 2)
{
    return 0;        
}

else
{
    return (gap - K * 2);
}