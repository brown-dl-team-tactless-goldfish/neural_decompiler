bool isSubsequence(char * s, char * t){
    if(*s == '\0' && *t == '\0') return true;
    for(char* c = t; *c != '\0'; c++) {
        if(*s == *c) s++;
        if(*(s) == '\0') return true;
    }

    return false;
}