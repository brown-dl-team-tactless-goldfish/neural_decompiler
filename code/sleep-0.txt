function a(n){
  return new Promise((res,rej)=>{
      setTimeout(()=>{
          res(1)
      } , n)
    })
}
/**
 * @param {number} millis
 */
async function sleep(m) {
  let c  = await a(m)
  return c
}

/** 
 * let t = Date.now()
 * sleep(100).then(() => console.log(Date.now() - t)) // 100
 */