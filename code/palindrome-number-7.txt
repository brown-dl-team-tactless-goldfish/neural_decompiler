bool isPalindrome(int x){
    int inv_x=0;
    if (x<0 || (x%10==0 && x!=0))
        return false;
    while (x>inv_x)
    {
        inv_x = inv_x*10+x%10;
        x = x/10;
    }
    return ((x==inv_x) || (x==inv_x/10));
}