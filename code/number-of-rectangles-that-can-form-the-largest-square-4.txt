  int result=0;
  int temp[rectanglesSize];
   int maxLen=0; 
    for(int i=0;i<rectanglesSize;i++){
        
        temp[i]=(rectangles[i][0]<rectangles[i][1])?rectangles[i][0]:rectangles[i][1];
        
        if(temp[i]>maxLen){
            maxLen=temp[i];
        }
    }

   for(int i=0;i<rectanglesSize;i++){
        
       
        if(temp[i]==maxLen){
            result++;
        }
    }

  return result; 