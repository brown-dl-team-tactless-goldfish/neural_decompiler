int numberOfSubarrays(int* nums, int numsSize, int k){

    int count[50001]={0};
    int i; 
    count[0]=1;
    int curr=0,ans=0;
    for(i=0;i<numsSize; i++) {
        curr += nums[i]%2;
        if(curr-k >=0) ans += count[curr-k];
        count[curr]++;
    }
    return ans;
}