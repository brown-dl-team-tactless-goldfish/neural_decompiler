    public long GetDescentPeriods(int[] prices) {
        var dp=new long[prices.Length];
        dp[0]=1;
        for(int i=1;i<prices.Length;i++)
        {
            if(prices[i]==prices[i-1]-1)
                dp[i]+=dp[i-1]+1;
            else
                dp[i]=1;
        }
        return dp.Sum();
    }