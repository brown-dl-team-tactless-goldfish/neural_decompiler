public class Solution
{
    public IList<int> Intersection(int[][] nums)
    {
        var set = new HashSet<int>(nums[0]);
        for(int i = 1; i < nums.Length; i++)
        {
            set.IntersectWith(nums[i].ToHashSet());
        }
        return set.OrderBy(x => x).ToList();
    }
}