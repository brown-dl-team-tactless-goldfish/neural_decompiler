SELECT sp1.name 
  FROM SalesPerson sp1 
  WHERE sp1.name NOT IN (SELECT sp.name 
                           FROM SalesPerson sp 
                           JOIN Orders o ON o.sales_id = sp.sales_id
                           JOIN Company c ON o.com_id = c.com_id
                          WHERE c.NAME = 'RED');