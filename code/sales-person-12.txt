select name from salesperson s where s.name not in (
		Select s.name from salesperson s inner join orders o on s.sales_id = o.sales_id
		inner join company c on c.com_id = o.com_id where c.name = 'Red' 
) 