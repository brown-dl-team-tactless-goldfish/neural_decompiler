        z: for (int i = 0; i < val.Length - 1; i++)
        {
            if (val[i] == val[i + 1])
            {  
               val.Remove(i, 2).ToString();
                goto z;
            }
        }
    return  val.ToString();        
}