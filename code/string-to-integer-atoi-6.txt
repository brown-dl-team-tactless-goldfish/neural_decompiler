//step 3
while(a>=0 && a<=9 && s)
{
    a=sign? a:-a;
    //boundary condition
    if(val>(INT_MAX)/10 || val==INT_MAX/10 && a>7)
    {return INT_MAX;}
    if(val<(INT_MIN)/10 || val==INT_MIN/10 && a<-8)
    {return INT_MIN;}
    //normal
    val=val*10+a;
    ++s;
    a=(*(s)-'0');
}
return val;        