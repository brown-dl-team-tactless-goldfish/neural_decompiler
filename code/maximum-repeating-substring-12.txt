int maxRepeating(string seq, string word) {
	int resCount = 0;
	string curWord = word;
	int numWords = seq.length() / word.length();
	for (int i = 0; i < numWords; i++) {
		if (seq.find(curWord) != string::npos) {
			curWord += word;
			resCount++;
		}
		else
			break;
	}
	return resCount;
}