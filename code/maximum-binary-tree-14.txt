if(numsSize<=0)
    return NULL;
int max=0;
struct TreeNode*root=NULL;
for(int i=1;i<numsSize;i++)
{
    if(nums[i]>nums[max])
        max=i;
}

root=create(nums[max]);
root->left=constructMaximumBinaryTree(nums,max);
root->right=constructMaximumBinaryTree(nums+max+1,numsSize-max-1);
return root;