int minimumMoves(char* s) {
    int len = strlen(s), cnt = 0;
    for (int i = 0; i < len; i++) {
        if (s[i] == 'X') {
            cnt++;
            for (int k = i; k < i + 2 && k < len; k++) {
                s[k] = 'O';
            }
            i += 2;
        }
    }
    return cnt;
}