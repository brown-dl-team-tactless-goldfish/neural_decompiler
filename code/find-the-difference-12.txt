char findTheDifference(char* s, char* t) {
char str[10000]; 
char ret =0;
int i = 0;

 memset(str,0,sizeof(str));
strcat(str,s);
strcat(str,t);

for(i=0;str[i];i++)
{
    ret = ret^str[i];
}
return ret;
}