    int longestDecomposition(string s) {
        for (int len = 1, n = s.size(); len*2 <= n; len++)
            if (s.substr(0, len) == s.substr(n-len)) return 2 + longestDecomposition(s.substr(len, n-len*2));
        return s.empty() ? 0 : 1;
    }