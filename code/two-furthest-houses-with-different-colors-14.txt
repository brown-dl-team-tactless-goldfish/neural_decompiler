int maxDistance(vector<int>& colors) {
	int maxd = 0;
	for(int i=0, size = colors.size(); i < size; ++i)
		maxd = max({maxd, (colors[i] != colors[0])*i, (colors.back() != colors[i])*(size - i -1)});
	return maxd;
}