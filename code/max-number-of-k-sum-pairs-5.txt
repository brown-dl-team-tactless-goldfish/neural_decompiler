int maxOperations(vector<int>& nums, int k) {
  int ret = 0;
  std::unordered_map<int, int> table;
  for (auto v : nums) {
    if (v < k) {
      if (table[k - v]) {
        --table[k - v];
        ++ret;
      } else {
        ++table[v];
      }
    }
  }
  return ret;
}