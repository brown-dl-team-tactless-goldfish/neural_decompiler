    long long beautifulSubarrays(vector<int>& A) {
        unordered_map<int, int> mp;
        mp[0] = 1;
        long long res = 0;
        for(int i = 0, bit_sum = 0; i < A.size(); i++) {
            bit_sum ^= A[i];
            res += mp[bit_sum]++;
        }
        return res;
    }