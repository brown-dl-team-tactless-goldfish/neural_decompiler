bool isMonotonic(int* A, int ASize){

    int i;
    int N = 0, tmp=0;
    for(i = 1; i < ASize ; i++){
        if(A[i]>A[i-1]) N++;
        if(A[i]<A[i-1]) N--;
        if(A[i]==A[i-1]) tmp++;
    }
    
    return abs(N) == ASize-1-tmp;
    
}