char repeatedCharacter(char * s){
    int len = strlen(s);
    int bitmask = 0;
    for(int i = 0; i < len; i++){
        if(bitmask & (1 << (s[i]-'a')))
            return s[i];
        bitmask |= (1 << (s[i]-'a'));
    }
    return -1;
}