class Solution {
public:
    bool winnerSquareGame(int n) {
        vector<int>v(n+1, 0);
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j*j <= i; j++) {
                v[i] |= !v[i-j*j];
            }
        }
        return v[n];
    }
};