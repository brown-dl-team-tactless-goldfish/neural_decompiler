class Solution {
public:
    int shortestSequence(vector<int>& rolls, int k) {
        int ans = 1;
        unordered_set<int> st;
        for(auto it : rolls){
            st.insert(it);
            if(st.size() == k) ans++, st.clear();
        }
        return ans;
    }
};