class Solution {
public:
    int countTriplets(vector<int>& a) {
        int sz = a.size(), c = 0;
        for (int i = 1; i < sz; i++) a[i] ^= a[i-1];
        for (int i = 0; i < sz; i++) for (int k = i + 1; k < sz; k++) if (!((!i ? 0 : a[i-1]) ^ a[k])) c += k - i;
        return c;
    }
};