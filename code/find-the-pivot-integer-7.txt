class Solution {
public:
    int pivotInteger(int n) {
        int rsum=0;
        int ans=-1;
        int total=n*(n+1)/2;

        for(int i=1;i<=n;i++)
        {
            rsum+=i;
            if(total - rsum + i == rsum)
            return i;
        }
        
        return -1;
    }
};
