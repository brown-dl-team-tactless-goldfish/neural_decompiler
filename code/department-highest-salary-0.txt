Select D.Name AS Department, E.Name AS Employee, E.Salary from Employee E, Department D,
(SELECT DepartmentId, max(Salary) as SMAX FROM Employee GROUP BY DepartmentId) as INR
where E.DepartmentId = INR.DepartmentId and E.Salary = INR.SMAX and E.DepartmentId = D.ID