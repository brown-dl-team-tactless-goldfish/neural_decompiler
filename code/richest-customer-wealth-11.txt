
int maximumWealth(int** accounts, int accountsSize, int* accountsColSize){
    int maxAmount = 0;
    
    for (int i = 0; i < accountsSize; ++i){
        int currSum = 0;
        for (int j = 0; j < accountsColSize[i]; ++j){
            currSum += accounts[i][j];
        }
        maxAmount = (int) fmax(maxAmount, currSum);
    }
    
    return maxAmount;
}