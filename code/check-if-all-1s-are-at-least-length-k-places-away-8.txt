bool kLengthApart(vector<int>& nums, int k) {
	int d = ++k, n = nums.size();
	for (int i = 0, l = -1 ; n-- && d >= k ; i++)
		nums[i] == 1 ? l != -1 ? d = i - l : 0, l = i : 0;
	return d >= k;        
}