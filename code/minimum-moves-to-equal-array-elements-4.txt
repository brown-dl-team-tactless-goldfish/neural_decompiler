class Solution {
public:
	int minMoves(vector<int>& nums) {
		long long sum=0;
		int MIN=INT_MAX;
		for(int i=0;i<nums.size();i++)
		{
			sum+=nums[i];
			MIN=min(MIN,nums[i]);
		}
		long n=nums.size();
		long long temp=n*MIN;
		return sum-temp; 
	}
};