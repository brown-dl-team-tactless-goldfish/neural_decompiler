class Solution {
	public:
		int n;
		vector<int> dp;

		int stoneGameVIII(vector<int>& arr) {
			n=arr.size();
			dp.resize(n);
			int s=0;
			for(int &i: arr) s+=i,i=s;
			dp[n-1]=arr[n-1];

			for(int i=n-2;i>0;i--) {
				dp[i]= max(arr[i]-dp[i+1],dp[i+1]);
			}
			return dp[1];
		}
	};