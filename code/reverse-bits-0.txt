uint32_t reverseBits(uint32_t n) {
    char* bits = (char*) malloc(sizeof(char) * 32);

    int j = 31;
    for (int i = 31; i >= 0; i--) {
        bits[j--] = ( ( ( ( (char) ( (n >> i ) << 3) ) >> 3) & 1) == 1) ? '1' : '0';
    }

    uint32_t res = 0;
    int k = 0;
    for (int i = 31; i >= 0; i--) {
        if (bits[i] == '1') {
            res += pow(2, k);
        }
        k++;
    }

    printf("\n%s", bits);

    return res;
}