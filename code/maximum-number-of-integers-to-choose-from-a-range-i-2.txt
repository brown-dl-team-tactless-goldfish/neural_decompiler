int maxCount(vector<int>& banned, int n, int maxSum) {
    int cnt = 0, sum = 0;
    unordered_set<int> st(banned.begin(), banned.end());
    for(int i=1;i<=n && i+sum <= maxSum;i++){
        if(!st.count(i)){
            cnt++;
            sum += i;
        }
    }
    return cnt;
}