int majorityElement(int* nums, int numsSize){
int curr=nums[0];
int count=1;
for(int i=1;i<numsSize;i++)
{
    if(nums[i]==curr)
    ++count;
    else--count;
    if(count==0)
    {
        curr=nums[i];
        count =1;
    }
}
return curr;
}