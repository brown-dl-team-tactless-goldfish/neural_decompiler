    vector<int> P = {1};
public:
    ProductOfNumbers() {
    }
    
    void add(int num) {
        if(num > 0) P.push_back(P[P.size()-1]*num);
        else P = {1};
    }
    
    int getProduct(int k) {
        int n = P.size();
        return k >= n ? 0 : P[n-1]/P[n-k-1];
    }