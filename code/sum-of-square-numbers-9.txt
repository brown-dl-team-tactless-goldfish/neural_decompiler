class Solution {
public:
  bool judgeSquareSum(int c) 
  {
    int t = 0;
    for (int i = 0; i <= sqrt(c); i++)
    {
      t= sqrt(c - pow(i, 2));
      if (pow(t, 2) == c - pow(i, 2))
        return true;
    }
    return false;
  }
};