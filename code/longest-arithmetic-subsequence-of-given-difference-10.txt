class Solution {
public:
    int longestSubsequence(vector<int> &A, int d, unordered_map<int,int> seen={}, int ans=0) {
        for (auto x: A) {
            seen[x] = seen[x - d] + 1;
            ans = max(ans, seen[x]);
        }
        return ans;
    }
};