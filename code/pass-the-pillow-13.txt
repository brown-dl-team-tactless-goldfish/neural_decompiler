class Solution {
public:
    int passThePillow(int n, int time) {
        int dir = 1;
        int p = 0;
        for(int i = 0; i < time; i++){
            p += dir;
            if(p == 0 || p == n-1) dir *= -1;
        }
        return p + 1;
    }
};