public class Solution {
    public int MaxDepth(string s) {
        int res = 0;
        int stack = 0;
        for(int i =0;i<s.Length;i++){
            if(s[i] == '(')stack++;
            else if(s[i] == ')')stack--;
            res = Math.Max(res,stack);
        }
        return res;
    }
}