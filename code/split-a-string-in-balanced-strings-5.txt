int balancedStringSplit(char * s)
{
        int c=0,i,mark=0;
        for(i=0;i<strlen(s);i++)
        {
                if(s[i]=='L')
                        mark++;
                else
                        mark--;
                if(mark==0)
                        c++;
        }
        return c;
}
