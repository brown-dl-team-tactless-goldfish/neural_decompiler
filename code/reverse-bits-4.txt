#define BIT_SET(a,b) ((a) |= (1UL<<(b)))
#define BIT_CHECK(a,b) (!!((a) & (1UL<<(b))))

uint32_t reverseBits(uint32_t n) {
    uint32_t result = 0;
    for (int i = 0; i < 32; i++) {
        if (BIT_CHECK(n, i)) {
            BIT_SET(result, 31 - i);
        }
    }
    
    return result;    
}