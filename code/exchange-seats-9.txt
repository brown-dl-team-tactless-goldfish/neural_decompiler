select 
case 
    when tmp.c % 2 = 1 and s.id = tmp.c
    then s.id
    when s.id % 2 = 1
    then s.id + 1
    else s.id - 1
    end as id,
s.student 
from seat s, 
(select max(id) as c from seat) as tmp
order by id;