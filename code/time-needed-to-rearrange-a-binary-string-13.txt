public class Solution {
    public int SecondsToRemoveOccurrences(string s) {
        string prior = s;
        
        for (int result = 0; ; ++result) {
            string next = prior.Replace("01", "10");
            
            if (next == prior)
                return result;
            
            prior = next;
        }
    }
}