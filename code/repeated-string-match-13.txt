    int n = b.size()/a.size()+2;
    int ans = 1;
    string c = a;
    
    for(int i = 0;i<n;i++){
        
        if (a.find(b) != string::npos) {
            return ans;
        }
        
        a+=c;
        ans++;
        
    }
    
    return -1;
}