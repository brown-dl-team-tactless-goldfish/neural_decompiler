int len = strlen(A);
char *S = malloc(sizeof(char) * (len+1));

for(int i = 1; i < strlen(A); i++){
    strncpy(S,A+i,len-i);
    strncpy(S+len-i,A,i);
    S[len] = '\0';
    if(!strcmp(S,B)){
        return true;
    }
}

return false;