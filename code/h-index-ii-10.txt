class Solution {
public:
	int hIndex(vector<int>& A) {
		if(A.empty()) return 0;
		int n=A.size();
		int low=0;
		int hi=n-1;
		while(low<hi)
		{
			int mid=low+(hi-low)/2;
			if(A[mid]>=n-mid)
				hi=mid;
			else
				low=mid+1;
		}
		if(A[low]<n-low) return 0;
		return n-low;
	}
};