    return ans;
}

int countGoodNumbers(long long n) 
{
       long long int a = ceil(n/2.0);
       long long int b = n/2;
       long long int x = find(5,a);
       long long int y =  find(4,b);
       long long int res = (x*y)%mod;
       int ans = res;
       return res;
}