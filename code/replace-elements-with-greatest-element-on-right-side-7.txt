int* replaceElements(int* arr, int arrSize, int* returnSize){
    *returnSize=arrSize;
    for(int i=arrSize-1,tmp=0,max=-1;i>=0;i--){
        tmp=arr[i];
        arr[i]=max;
        max=(max>tmp)?max:tmp;
    }
    return arr;
}