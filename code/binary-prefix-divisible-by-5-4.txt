bool* prefixesDivBy5(int* A, int ASize, int* returnSize){
    int value = 0, i = 0;
    bool *retArr = calloc(ASize , sizeof(bool));
    
    while(i<ASize) {
        value = (value<<1 | A[i]) % 5;
        retArr[i++] = (value==0);
    }
    *returnSize = ASize;
    return retArr;
}