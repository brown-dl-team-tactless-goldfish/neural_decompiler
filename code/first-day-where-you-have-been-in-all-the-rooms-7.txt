    int mod = 1e9+7;
    int n = arr.size();
    vector<int> dp(n );
    
    // the only way to visit room i is through i - 1
    
    for(int i = 1; i<n; i++)
    {
        dp[i] = ( 2*dp[i-1] - dp[arr[i-1]] + 2 + mod)%mod;
    
    }
    
    return dp[n-1];
    
    
    
}