int maxAbsoluteSum (int* nums, int numsSize) {
	int i, min = 0, max = 0, sum = 0;

	for (i = 0; i < numsSize; i++) {
		sum += nums[i];
		if (min > sum) {
			min = sum;
		} else if (max < sum) {
			max = sum;
		}
	}

	return max - min;
}