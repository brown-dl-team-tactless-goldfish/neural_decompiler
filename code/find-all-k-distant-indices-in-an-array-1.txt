def findKDistantIndices(self, nums, key, k):
	index = [i for i,c in enumerate(nums) if c==key]
	s = set()
	for i in index: s.update([i for i in range(i-k,i+k+1) if i>=0 and i<len(nums)])
	return list(sorted(s))