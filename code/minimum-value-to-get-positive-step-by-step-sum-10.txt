int 
minStartValue(int * nums, int numsSize)
{
    int result = *nums;
    
    for (int i = 1; i < numsSize; ++i)
    {
        nums[i] += nums[i - 1];
        
        if (nums[i] < result)
            result = nums[i];
    }
    
    return (result = 1 - result) > 0 ? result : 1;
}