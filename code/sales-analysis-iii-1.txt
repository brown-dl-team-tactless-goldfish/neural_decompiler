select a.product_id, a.product_name from product a where '2019-01-01'<=
(select min(b.sale_date) from sales b where b.product_id = a.product_id) and 
(select max(c.sale_date) from sales c where c.product_id = a.product_id)<='2019-03-31'