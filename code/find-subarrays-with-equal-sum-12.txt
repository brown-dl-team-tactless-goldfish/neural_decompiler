class Solution {
public:
    bool findSubarrays(vector<int>& nums) {
        unordered_map<int, int> ump;
        for(int i=1; i<nums.size(); i++) {
            if(ump[nums[i]+nums[i-1]] != 0) return true;
            else ump[nums[i]+nums[i-1]]++;
        }
        return false;
    }
};