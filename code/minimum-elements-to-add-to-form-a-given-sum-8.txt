
```public:
    int minElements(vector<int>& nums, int limit, int goal) {
     long long int i,sum=0,x,y;
        for(i=0;i<nums.size();i++)
        {
            sum=sum+nums[i];
        }
        x=abs(sum-goal);
        y=(x/limit);
        if(x%limit!=0)
        {
            y++;
        }
        return y;
    }
};