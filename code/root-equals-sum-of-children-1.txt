/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */


bool checkTree(struct TreeNode* root){
    bool is_root = false;
    is_root = root->left->val + root->right->val == root->val ? true : false;
    return
}