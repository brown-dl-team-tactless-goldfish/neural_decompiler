    sort(vec.begin(),vec.end(),comp);
    
            // no car can overtake any other car so all cars whose time is less than first car from forward can become one fleet else they will form seprate fleet and for rest car behind that firstcarinfleet got updated to new maxtime as current car cannot catch forward car becoz its time > next car tim
    int fleet=0;
    int firstcarinfleet=vec.size()-1;
    for(int i=vec.size()-2;i>=0;i--){
        if(vec[i].time<=vec[firstcarinfleet].time)
        {
            continue; 
            //as this car will merge together
            
        }
        else
        {
             firstcarinfleet=i;
             fleet++;
            cout<<fleet;
        }

    
    }

    return fleet+1;
}