int* constructRectangle(int area, int* returnSize) {
    int* ans = malloc(sizeof(int)*2);
    int W = sqrt(area);
    
    while (area % W)
    {
        W--;
    }
    
    ans[0] = area / W;
    ans[1] = W;
    *returnSize = 2;
    return ans;
}