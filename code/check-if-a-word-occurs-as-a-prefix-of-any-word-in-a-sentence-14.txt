int isPrefixOfWord(char * sentence, char * searchWord){
    signed char c = -1, b[50] = " ", *s = sentence, *w = searchWord;
    if ((c = strncmp(s, w, strlen(w)) ? -1 : 1) == -1)
        for (char n = 1, *p = strstr(s, strcat(b, w)) ; p && p >= s ; c = n += *p-- == ' ');
    return c;
}