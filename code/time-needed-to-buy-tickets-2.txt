if (counter == 0)
    return 0;
int fix = 0;
for(int i = 0;i < ticketsSize;i++)
{
    if(tickets[k] == 0)
        fix = 1;
    if (tickets[i] >= counter)
    {
       ans = ans + counter - fix;
       tickets[i] -= counter;
    }
    else
    {
        ans += tickets[i];
        tickets[i] = 0;
    }
}
return ans;