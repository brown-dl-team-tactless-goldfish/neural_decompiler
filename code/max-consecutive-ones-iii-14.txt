class Solution {
public:
    int longestOnes(const vector<int>& A, const int K) {
        int res = 0, i = 0, j = 0;
        for(int size = A.size(), c = 0; j < size && size-i+1 > res; ++j) {
            c += !A[j];
            c -= (c > K) && !A[i++];
        }
        return j-i;
    }
};