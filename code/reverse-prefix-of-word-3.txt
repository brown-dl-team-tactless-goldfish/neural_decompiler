while(word[index]){        
    if(word[index] == ch){        
        while(start < index){
            tmp = word[start];
            word[start++] = word[index];
            word[index--] = tmp;
        }   
        break;
    }
    index++;
}

return word;