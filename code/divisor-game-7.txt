bool divisorGame(int N){
        bool Alice = true;
        int x = 1;
        
        while (N > 0) {
                while (x < N && N % x)
                        x++;
                
                N = N - x;
                Alice = !Alice;
        }
        
        if (Alice)
                return true;
        else
                return false;
}