int maxDepth(char * s)
{
	int i = 0, depth = 0, max = 0;
    while (s[i] != '\0')
    {
        if (s[i] == '(')
        {
            depth++;
            if (depth > max)
                max = depth;
        }
        else if (s[i] == ')')
            depth--;
        i++;
    }
    return max;
}