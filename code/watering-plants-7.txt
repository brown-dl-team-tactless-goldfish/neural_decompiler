int wateringPlants(vector<int>& plants, int capacity) 
{
	int result=0,c=capacity,n=plants.size(); // c is the remaining water
	for(int i=0;i<n;i++)
	{
		if(c-plants[i]<0) result+=i*2,c=capacity; // go back and forth to fill it
		
		c-=plants[i];
		result++;
	}
	return result;
}