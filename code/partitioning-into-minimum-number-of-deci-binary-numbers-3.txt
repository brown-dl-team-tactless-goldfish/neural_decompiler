int minPartitions(char * n){
    int maxDigit = 0;

    int i = 0;
    while (n[i] != NULL)
    {
        int c = n[i++] - '0';
        if (maxDigit < c) {
            maxDigit = c;
        }
    }
    
    return maxDigit;
}