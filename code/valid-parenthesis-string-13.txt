class Solution {
public:
    bool checkValidString(string s) {
      int left = 0, right = 0;
      for (char c : s) {
	left = c == '(' ? left + 1 : left - 1;
	right = c == ')' ? right - 1 : right + 1;
	left = max(left, 0);
	if (right < 0)
	  return false ;
      }
      return left == 0; 
    }
};