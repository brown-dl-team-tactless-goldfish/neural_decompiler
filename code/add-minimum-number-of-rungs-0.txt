int addRungs(int* rungs, int rungsSize, int dist){
    int ret = 0 ;
    ret += (rungs[0]-1) / dist ;
    for(int i = 1; i < rungsSize; i++){
        int gap = rungs[i] - rungs[i-1] ;
        ret += (gap-1)/dist ;
    }
    return ret ;
}