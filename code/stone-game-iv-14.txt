public class Solution 
{
    public bool WinnerSquareGame(int n) 
    {
        var dp = new bool[n + 1];
        for(int i = 1; i <= n; i++)
            for(int j = 1; j*j <= i; j++)
                dp[i] |= !dp[i - j * j];
        return dp[n];
    }
}