int minTimeToVisitAllPoints(int** points, int pointsSize, int* pointsColSize){
    int d = 0, **p = points - 1, z = pointsSize, d1, d2;
    while (--z && ++p && (d += (d1 = abs(p[0][0] - p[1][0])) > (d2 = abs(p[0][1] - p[1][1])) ? d1 : d2));
    return d;
}