class Solution(object):
    def averageWaitingTime(self, customers):
        ft = wt = 0.0
        for c in customers:
            if ft <= c[0]: ft = c[0] + c[1]
            else: ft += c[1]
            wt += ft - c[0]
            
        return wt / len(customers)