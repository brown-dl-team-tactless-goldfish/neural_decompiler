int findPoisonedDuration(int* timeSeries, int timeSeriesSize, int duration){

    int ct = 0;
    for(int i = 0; i < timeSeriesSize - 1; i++)
    {
        ct = ct + fmin(duration, timeSeries[i+1] - timeSeries[i]);
    }
    return ct + duration;
    
}