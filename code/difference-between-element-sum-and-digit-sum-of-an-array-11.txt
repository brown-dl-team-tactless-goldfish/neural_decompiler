int differenceOfSum(int* nums, int numsSize)
{
    int esum=0, dsum=0, i;
    for(i=0;i<numsSize;i++)
    {
        esum=esum+nums[i];
        while(nums[i]!=0)
        {
            dsum=dsum+nums[i]%10;
            nums[i]=nums[i]/10;
        }
    }
    return esum-dsum;
}