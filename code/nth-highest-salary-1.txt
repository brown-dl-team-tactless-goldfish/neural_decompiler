BEGIN
    DECLARE C INT;
    SET C = (Select count(Distinct Salary) From Employee);
    If C < N Then
        RETURN null; 
    
    Else
        RETURN (
          # Write your MySQL query statement below.
          Select min(Salary) 
          From (
              Select distinct Salary
              From Employee
              Order by Salary desc
              Limit N
              ) as a
        );
    End if;
END