class Solution {
public:
	int longestContinuousSubstring(string s) {
		int i=0,ans=1,t=1;
		while(i<s.length()){
			char c = s[i];
			i+=1;
			c++;
			while(i<s.length()&&s[i]==c){
				i+=1;
				c++;
				t+=1;
			}
			ans = max(t,ans);
			t=1;
		}
		return ans;
	}
};