int sumOfUnique(int* nums, int numsSize){
    int h[101]={0};
    for(int i=0;i<numsSize;i++)
        h[nums[i]]++;
    int sum=0;
    for(int i=0;i<numsSize;i++)
    {
        if(h[nums[i]]==1)
            sum+=nums[i];
    }
    return sum;
}