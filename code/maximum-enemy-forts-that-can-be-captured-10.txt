class Solution {
public:
    int captureForts(vector<int>& A) {
        int res = 0;
        for(int i = 0, prev = -1; i < A.size(); i++) {
            if(prev != -1 && A[i]*A[prev] == -1) res = max(res, i-prev-1);
            if(A[i] != 0) prev = i;
        }
        return res;
    }
};