int countAsterisks(char * s){
    bool in=true;
    int len=strlen( s), out=0;
    for( int i=0; i<len; i++){
        if( s[i]=='|'){
            in=true^in;
        }
        else if( in && s[i]=='*'){
            out++;
        }
    }
    return out;
}