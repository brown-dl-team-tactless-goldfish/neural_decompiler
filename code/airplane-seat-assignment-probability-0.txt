double nthPersonGetsNthSeat(int n){
    if(n == 1)
        return 1;
    else
        return 0.5;
/*
    if(n == 1)
        return 1;
    double* dp = malloc((n+1) * sizeof(double));
    dp[0] = 0;
    dp[1] = 1;
    double sum = 0;
    for(int i = 2; i <=n; i++){
        sum += dp[i-1];
        dp[i] = sum/i;
    }
    double  ans =  dp[n];
    free(dp);
    return ans;
*/
}