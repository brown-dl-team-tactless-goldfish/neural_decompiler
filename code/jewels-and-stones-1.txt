int numJewelsInStones(char * J, char * S){
    int len1, len2, i, j, jewels=0;
    len1 = strlen(J);
    len2 = strlen(S);
    for(i=0;i<len2;i++)
        for(j=0;j<len1;j++)
            if(S[i]==J[j])
                jewels++;
    return jewels;
}