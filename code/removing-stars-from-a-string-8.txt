char* removeStars(char* s){
    
    char *src_ptr = s, *dst_ptr = s;

    do{
        if(*src_ptr++ == '*')
            --dst_ptr;
        else
            ++dst_ptr;
        
        *dst_ptr = *src_ptr;

    }while(*dst_ptr != '\0');

    return s;
}