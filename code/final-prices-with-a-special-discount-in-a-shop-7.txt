 *returnSize= pricesSize;
  
 int *result=(int*)malloc(pricesSize*sizeof(int));
 
 int count=0,i,j;

for(i=0;i<pricesSize;i++)
{
    for(j=i+1;j<pricesSize;j++){
        
        if(prices[j]<=prices[i]){
            
            result[count]=prices[i]-prices[j];
            count++;
            break;
        }
    }
    
       if(j==pricesSize){
           result[count]=prices[i];
            count++;
       }
}

 