int minAddToMakeValid(char* S) {
    int len = strlen(S);
    int miss_par = 0;
    int c = 0;  
    for (int i = 0; i < len; ++i) {
        *(S+i) == '(' ? c++ : c--;
        c < 0 ? (miss_par++, c++) : 1;
    }
    miss_par += c;
    return miss_par;
}