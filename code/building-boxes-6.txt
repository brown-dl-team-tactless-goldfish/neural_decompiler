class Solution {
public:
    int minimumBoxes(int n) {
        int r = 0;
        for (int i = 0, j = 1; i <= n; n -= i, r = i, i += j++)
        ;
        for (int i = 1; n > 0; n -= i++, ++r)
        ;
        return r;
    }
};