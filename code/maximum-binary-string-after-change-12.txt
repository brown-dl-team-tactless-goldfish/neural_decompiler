    string maximumBinaryString(string s) {
        int num0 = count(s.begin(), s.end(), '0');
        if(num0 == 0) return s;
        int n = s.size(), i = s.find('0');               
        string res(n, '1');
        res[i + num0 - 1] = '0';
        return res;
    }