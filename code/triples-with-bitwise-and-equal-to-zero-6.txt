    int countTriplets(vector<int>& A) {
        unordered_map<int,int> m;
        for(int x:A)
            for(int y:A) ++m[x&y];
        int sum = 0;
        for(auto x:A)
            for(auto [y, cnt]:m) if((x&y)==0) sum += cnt;
        return sum;
    }