public int MinMoves2(int[] nums)
{
	Array.Sort(nums);
	var n = nums.Length;
	var median = nums[n / 2];
	if (n % 2 == 0) median = (nums[n / 2 - 1] + nums[n / 2]) / 2;
	return nums.Select(x => Abs(x - median)).Sum();
	int Abs(int val) => val >= 0 ? val : -val;
}