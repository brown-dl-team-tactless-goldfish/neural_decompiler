class Solution {
public:
    int maxDepth(Node* root) {
        if(!root)
        {
            return 0;
        }
        int depth{0};
        
        for(Node* node: root->children)
        {
            depth = max(maxDepth(node), depth);
        }
        
        return depth + 1 ;
    }
};