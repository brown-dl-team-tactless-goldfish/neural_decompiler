class Solution {
public:
    int minimumTime(string s) {
        const int n = s.length();
        int r = 0, ans = n;
        for (int i = n - 1; i >= 0; --i) {
          r = min(n - i, r + (s[i] == '1' ? 2 : 0));
		  ans = min(ans, i + r);
        }
        return ans;
    }
};