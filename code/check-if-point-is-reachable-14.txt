class Solution {
public:
    bool isReachable(int targetX, int targetY) {
        if (targetX == targetY)
            return (targetX & (targetX - 1)) == 0;
        int q = __gcd(targetX, targetY);
        if( (q & (q - 1)) == 0)
            return 1;
        return  q == 1;
    }
};
