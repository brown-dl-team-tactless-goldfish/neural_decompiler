public class Solution {
    public long NumberOfWeeks(int[] milestones)
    {
        long sum = milestones.Sum(x => (long)x), max = milestones.Max();
        return Math.Min(sum, (sum-max)*2+1);        
    }
}