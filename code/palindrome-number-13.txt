bool isPalindrome(int x){
    long int rev = 0;
    int temp = x;
    while( x > 0 ){
        rev = rev * 10 + (x % 10);
        x /= 10;
    }
    
    if(rev == temp)
        return 1;
    else
        return 0;
}