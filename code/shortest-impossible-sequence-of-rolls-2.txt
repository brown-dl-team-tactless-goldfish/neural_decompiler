    int res = 0;
    
    for(auto &x: rolls)
    {
        st.insert(x);
        
        if(st.size() == k)
        {
            res++;
            
            st.clear();
        }
        
    }
    
    return ++res;
}