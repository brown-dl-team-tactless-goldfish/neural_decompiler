bool checkAlmostEquivalent(string word1, string word2) {
    vector<int>arr(26);
    for(char c : word1)
        arr[c - 'a']++;
    for(char c : word2)
        arr[c - 'a']--;
    for(int i : arr)
        if(abs(i) > 3)
            return false;
    return true;
}