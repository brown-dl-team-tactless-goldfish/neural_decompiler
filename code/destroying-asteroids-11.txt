class Solution {
public:
    bool asteroidsDestroyed(int mass, vector<int>& a) {
        sort(a.begin(), a.end());
        long long s = mass;
        for(int i : a) {
            if(s < i) return false;
            s += i;
        }
        return true;
    }
};