    for(int i=0;i<nums.size()-1;i++){
            if(nums[i]<nums[i+1]){
                res++;
                printf("curr:%d  res: %d\n  ",curr,res);
                curr=max(res,curr);
            }
            else res=1; 
        }
        return max(res,curr);
}