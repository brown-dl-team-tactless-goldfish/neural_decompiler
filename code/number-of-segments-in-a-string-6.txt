int countSegments(char* s) {
    char* token;
    const char dim[2] = " ";
    int ans = 0;
    token = strtok(s, dim);
    if (!token) return 0;
    else
    {
        while(token != NULL)
        {
            ans++;
            token = strtok(NULL, dim);
        }
    }
    return ans;
}