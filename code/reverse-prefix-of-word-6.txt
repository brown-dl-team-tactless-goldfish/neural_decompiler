char* temp=malloc(sizeof(char)*(strlen(word)+1));
temp[strlen(word)]='\0';
for(int i=0;i<strlen(word);i++)
{
    if(word[i]==ch)
    {
        for(int j=0;j<=i;j++)
        {
            temp[j]=word[i-j];
        }
        for(int j=i+1;j<strlen(word);j++)
        {
            temp[j]=word[j];
        }
        return temp;
    }
}

return word;