with collection as ((select * from Employees) union (select * from Salaries))

select c.employee_id
from collection c 
where c.employee_id not in (select e.employee_id from Employees e join Salaries s on e.employee_id = s.employee_id) order by 1