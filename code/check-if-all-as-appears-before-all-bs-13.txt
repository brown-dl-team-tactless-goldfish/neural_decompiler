bool checkString(char * s){
    int length = strlen(s);
    for (int i=0;i<length-1;i++){
        int hash = (i*i)%length;
        if (s[i]=='b'&&s[i+1]=='a'){
            return false;
        }
        if (s[hash]=='b'&&s[hash+1]=='a'){
            return false;
        }
    }
    return true;
}