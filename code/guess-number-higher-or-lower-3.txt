int guessNumber(int n){
    int l=1,r=n,m;
	if(n==1)
        return 1;
    else
        while( l<=r){
            m=l+(r-l)/2;
            if(guess(m)==0)
                return m;
           
            if(guess(m)==-1)
                r=m-1;
            else
            if(guess(m)==1)
                 l=m+1;
           
            
        }
    return -1;
}