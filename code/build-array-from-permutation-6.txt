#include <stdio.h>
#include <stdlib.h>

void buildArray(int nums[], int numsSize, int returnSize[]);

int main (void) {
  int nums[] = {0,2,1,5,3,4};
  int returnSize[6] = {};
  int numSize = 6;

  buildArray(nums,numSize,returnSize);

  return 0;
}

  void buildArray(int nums[], int numsSize, int returnSize[]) {
    
    int i = 0;
    int new_index = 0;
    int to_return;
    
    while (i < numsSize) {
        new_index = nums[i];
        returnSize[i] = nums[nums[i]];
        to_return = returnSize[i];
        printf("%d", to_return);
        i++;
    }

}