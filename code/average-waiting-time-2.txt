
#define max(x,y) x>y ? x:y;

double averageWaitingTime(int** cust, int row, int* customersColSize){
        double wait=0;
	double curr=0;
	for(int i=0;i<row;i++){
		curr=max(curr,cust[i][0]);
		curr+=cust[i][1];
		wait+=curr-cust[i][0];
	}
	double r=wait/row;
	return r;
}