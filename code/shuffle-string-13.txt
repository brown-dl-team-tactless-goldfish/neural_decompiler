char * restoreString(char * s, int* indices, int indicesSize){
    char *sr = (char*)malloc((indicesSize+1)*sizeof(char));
    for(int i=0;i<indicesSize;i++)
        sr[indices[i]]=s[i];
    sr[indicesSize]='\0';
    return sr;
}