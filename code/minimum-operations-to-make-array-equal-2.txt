int minOperations(int n)
{
    int arr[n],avg,sum=0;
    for(int i=0;i<n;i++)
        arr[i]=(2*i)+1;
    if(n%2==0)
        avg=(arr[n/2]+arr[n/2-1])/2;
    else
        avg=arr[n/2];
    for(int i=0;i<n/2;i++)
        sum+=avg-arr[i];
    return sum;
}