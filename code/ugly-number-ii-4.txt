    int* result = (int*) malloc(sizeof(int) * n);
    
    int i = 0, j = 0, k = 0;
    
    result[0] = 1;

    for(int m = 1; m < n ; m++)
    {
        result[m] = fmin(result[i] * 2, fmin(result[j] * 3, result[k] * 5));
        
        printf ("result[%d] = %d\n", m, result[m]);
        if (result[m] == result[i] * 2) i++;
        if (result[m] == result[j] * 3) j++;
        if (result[m] == result[k] * 5) k++;
    }

    return result[n - 1];