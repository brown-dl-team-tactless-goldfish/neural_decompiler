int findKthPositive(int* arr, int arrSize, int k){
    int base = 0;
    for(int i = 0; i < arrSize;i++)
    {
        k -= arr[i] - base - 1;
        if(k <= 0)
        {
            return k + arr[i]  - 1;
        }
        base = arr[i];
        
    }
    return base + k;
}