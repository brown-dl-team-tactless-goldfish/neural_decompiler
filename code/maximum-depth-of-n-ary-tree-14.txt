class Solution {
public:
    int maxDepth(Node* cur) {
        if (cur == NULL) {
            return 0;
        }
        
        int result = 1;
        for (Node* child : cur->children) {
            result = max(1 + maxDepth(child), result);
        }
        
        return result;
    }
};