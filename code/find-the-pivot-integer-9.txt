int pivotInteger(int n)
{
    int sum = (n*(n+1))/2, temp = -1;
    for(int i = 1; i<=n; i++)
    {
        int mid = (i*(i+1))/2;
        if(mid == sum)
        {
            temp = i;
            break;
        }
        sum = sum-i;
    }
    return temp;
}