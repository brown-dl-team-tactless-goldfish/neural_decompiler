class Solution {
public:
    
    int subsetXORSum(vector<int>& nums) {
        int bitsor = 0;
        for(auto x : nums)
            bitsor |= x;
        return bitsor << (nums.size() - 1);
    }
};