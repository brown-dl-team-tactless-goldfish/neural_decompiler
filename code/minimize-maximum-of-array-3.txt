
int minimizeArrayValue(int* nums, int numsSize)
{
   double x = 0, y = 0;
   for( int j = 0; j < numsSize; j++ )
   {
       y += nums[j];
       double a = ceil(y/(j+1));
       if(x < a ){ x = a; }
   }
   return (int)x;
}