int minimumCost(vector<int>& cost) {
	int res = 0, carry = 0, price = 100, cnt[100] = {};
	for (int c : cost) ++cnt[price - c];
	for (int c : cnt) {
		res += (c - c / 3 - (c % 3 + carry) / 3) * price--;
		carry = (c + carry) % 3;
	}
	return res;
}