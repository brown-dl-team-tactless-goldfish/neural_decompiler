int findPeakElement(int* nums, int numsSize){
    
    int i;
    int max = INT_MIN;
    int idx=0;
    for(i=0; i <= numsSize-1; i++)
    {
        if(nums[i] > max)
        {
            max = nums[i];
            idx = i;
        }
    }

    return idx;
}