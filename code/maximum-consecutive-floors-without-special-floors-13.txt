    int maxConsecutive(int bottom, int top, vector<int>& sp ) {
        sort(sp.begin(), sp.end());
        int res = max(top - sp.back(), sp.front() - bottom);
        for(int i=0; i<sp.size()-1;i++) res = max(res, sp[i+1] - sp[i] -1);
        return res;        
    }