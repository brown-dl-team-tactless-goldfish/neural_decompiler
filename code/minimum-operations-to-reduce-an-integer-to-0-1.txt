int minOperations(int n){
    int count = 0;
    while (n != 0){
        int exp = (int)(log(n)/log(2));
        n = fmin(n - pow(2, exp), pow(2, exp+1) - n);
        count++;
    }
    return count;
}