bool isGoodArray(std::vector<int>& nums) 
{  
    
    int gcd_tmp = nums[0];
    
    for(int i = 1; i < nums.size(); i++)
    {
        gcd_tmp = gcd_fast(gcd_tmp, nums[i]);
        
        if (gcd_tmp == 1) break;
    }
    
    return gcd_tmp == 1;
}