bool hasAlternatingBits(int n){
    if (n == 1) return true;
    int prev = -1;
    while (n) {
        int cur = n & 1;
        if (prev != -1 && cur == prev) {
            return false;
        }
        
        prev = cur;
        n >>= 1;
    }
    
    return true;
}