string greatestLetter(string s) {
	set<char> unique;
	for (const char& c : s)
		unique.insert(c);
	for (int i = 25; i >= 0; i--)
		if (unique.count((char)(97 + i)) && unique.count((char)(65 + i)))
			return string(1, (char)(65 + i));
	return "";
}