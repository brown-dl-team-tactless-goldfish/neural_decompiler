int tupleSameProduct(vector<int>& nums) {
	map<int, int> mFreq;
	for (int i = 0; i < nums.size(); i++)
		for (int j = i + 1; j < nums.size(); j++)
			mFreq[nums[i] * nums[j]]++;

	int result = 0;
	for (auto it : mFreq)
		result += (it.second * (it.second - 1) / 2) * 8;
	return result;
}