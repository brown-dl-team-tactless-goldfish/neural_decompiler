class Solution {
public:
    string vowels = "aeiou";
    long long countVowels(string word) {
        long long l = 1, r = word.size(), ans = 0;
        for (auto& c : word){
            if (vowels.find(c) != -1) ans += l * r;
            ++l, --r;
        }
        return ans;
    }
};