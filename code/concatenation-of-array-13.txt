int* getConcatenation(int* nums, int numsSize, int* returnSize){
    int* ans = malloc(numsSize * sizeof(int) * 2 + sizeof(int));

    for(int i = 0; i < numsSize; ++i) {
        ans[i] = nums[i];
        ans[i + numsSize] = nums[i];
    }
    
    *returnSize = numsSize * 2;
    return ans;
}