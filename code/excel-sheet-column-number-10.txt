int titleToNumber(char * s){
    
    int len = strlen(s);
    long long int p = 1;
    int base = 26;
    long long int sum = 0;
    
    for(int i = len -1; i >= 0; i--){
        int d = s[i] - 'A' + 1;
        sum += d * p;
        p *= base;
    }
    
    return (int)sum;

}