

bool areOccurrencesEqual(char * s){
int arr[26]={0};
    for(int i=0;i<strlen(s);i++)
    {
       arr[s[i]-97] ++;
        
    }
int *a=(int *)malloc(sizeof(int)*26);
    int k=0;
    for(int i=0;i<26;i++)
    {
        if(arr[i]!=0)
        {
            a[k]=arr[i];
            k++;
            
            
        }
    }
  for(int i=0;i<k-1;i++)
  {if(a[i]!=a[i+1])
      return false;
      
  }
    return 1;
    
    
}