class Solution {
public:
	vector<int> cnt;
	int sum=0;
	Solution(vector<int>& w) {
		for(int i=0;i<w.size();i++)
		{
			sum+=w[i];
			cnt.push_back(sum);
		}

	}

	int pickIndex() {
		int ran=rand()%sum+1;
		return lower_bound(cnt.begin(),cnt.end(),ran)-cnt.begin();
	}
};