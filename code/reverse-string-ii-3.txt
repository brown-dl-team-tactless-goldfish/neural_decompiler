for(i = Len; i >= 0; i -= 2*k)
{
    pStart = s + j * 2 * k;
    if(i >= k) pEnd = pStart + k - 1;
    else pEnd = pStart + i - 1;
    j++;
    while(pEnd - pStart > 0)
    {
        Tmp = *pStart;
        *pStart = *pEnd;
        *pEnd = Tmp;
        pStart++;
        pEnd--;
    }
}
return s;