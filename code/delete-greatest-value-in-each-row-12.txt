int deleteGreatestValue(vector<vector<int>>& grid) 
{
	int result=0,m=grid.size(),n=grid[0].size();
	for(int i=0;i<m;i++) sort(grid[i].rbegin(),grid[i].rend());
	for(int j=0;j<n;j++)
	{
		int c=0;
		for(int i=0;i<m;i++) c=max(c,grid[i][j]);
		result+=c;
	}
	return result;
}