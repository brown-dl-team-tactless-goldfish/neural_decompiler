    public int CountDistinctIntegers(int[] nums)
    {
        HashSet<int> hs = new HashSet<int>();
        foreach  (int num in nums)
        {
            hs.Add(num);
            hs.Add(int.Parse(string.Concat(num.ToString().Reverse())));
        }
        return hs.Count();
    }