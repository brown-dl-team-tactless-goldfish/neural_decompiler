public class Solution 
{
    public int PassThePillow(int n, int time) 
    {
        int div = time / (n-1);
        
        int mod = time % (n-1);
        if (div % 2 == 0)
            return mod + 1;
        else
            return n - mod;
    }
}