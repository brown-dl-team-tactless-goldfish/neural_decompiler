

int prefixCount(char ** words, int wordsSize, char * pref){
    int i,count=0;
    
    for(i=0;i<wordsSize;i++)
    {
        if(strstr(words[i],pref)==words[i])         //check for each word that is starting with prefix and increment count
            count++;
    }
    return count;

}