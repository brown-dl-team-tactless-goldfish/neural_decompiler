#define ll long long
class Solution {
public:
    bool isReachable(int x, int y) {
        while(x%2==0)x=x/2;
        while(y%2==0)y=y/2;
        if(__gcd(x,y)==1) return true;
        return false;
        
    }
};