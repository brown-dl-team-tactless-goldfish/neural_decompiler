bool check(int* nums, int numsSize){
	int i,det = 0;
	for(i = 0; i < numsSize; i++)
	{
		if(i == numsSize - 1)
		{
			if(nums[i] > nums[0])
				det++;
		}
		else
		{
			if(nums[i] > nums[i + 1])
				det++;
		}
		
	}
	if(det <= 1)
		return 1;
	else
		return 0;
}