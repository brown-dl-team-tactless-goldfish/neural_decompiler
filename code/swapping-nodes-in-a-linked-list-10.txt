/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode* swapNodes(struct ListNode* head, int k){

    struct ListNode* pointer1 = head;
    struct ListNode* pointer2 = head;

    int size=0;
    
    int x=1;
    //find the the pointer in the k th position fro the swap
     while(x<k)
     {
    pointer2=pointer2->next;
     x++;
     }
    //find the size
    while(pointer1)
    {
    pointer1=pointer1->next; 
    size++;
    }
    //return the pointer1 to the begining
    pointer1=head;
    // take the pointer to k th position from end to begining
    for(int i=1;i<=(size-k);i++){
        pointer1=pointer1->next;
    }
    //change the value between the two pointers
        int c = pointer1->val; 
        pointer1->val= pointer2->val;
        pointer2->val= c;
    
return head;
}