public int XorAllNums(int[] A, int[] B) =>
    Array.Empty<int>()
        .Concat(B.Length % 2 == 0 ? Array.Empty<int>() : A)
        .Concat(A.Length % 2 == 0 ? Array.Empty<int>() : B) 
        .Aggregate(0, (x, y) => x ^ y);