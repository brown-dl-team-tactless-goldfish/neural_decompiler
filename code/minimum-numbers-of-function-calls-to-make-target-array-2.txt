int minOperations (int *nums, int numsSize) {
	int i, max = 0, op = 0;

	for (i = 0; i < numsSize; i++) {
		op += __builtin_popcount(nums[i]);
		if (max < nums[i]) max = nums[i];
	}

	if (max) op += __builtin_clz(1) - __builtin_clz(max);

	return op;
}