class Solution {
public:
    int maxRepeating(string sequence, string word) {
      int c = 0;
      int l = sequence.length();
        string str = word;
      while(sequence.find(str) != string :: npos )
      {c++;
       str += word; // grow it to check double of it.
      }
        return c;
    }
};