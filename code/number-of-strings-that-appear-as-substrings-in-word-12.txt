class Solution {
public:
    int numOfStrings(vector<string>& patterns, string word) {
        int count=0;
        for(auto i:patterns){
          auto  found=word.find(i);
            if(found!=string::npos){
                count++;
            }
        }
        return count;
    }
};