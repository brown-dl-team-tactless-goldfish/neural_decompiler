int numIdenticalPairs(int* nums, int numsSize){
	register int i, j, pairs = 0;
	for(i = 0; i < numsSize; i++)
		for(j = 0; j < numsSize; j++)
			if(nums[i] == nums[j] && i < j)
				pairs++;
	return pairs;
}