int countAsterisks(char * s){
    int i, count = 0, asterisks = 0;
    for (i = 0; i < strlen(s); ++i) {
        if (s[i] == '|') ++count;
        if (count % 2 == 0 && s[i] == '*') ++asterisks;
    }
    return asterisks;
}