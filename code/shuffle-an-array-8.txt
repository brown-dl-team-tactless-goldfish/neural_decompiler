int* solutionShuffle(Solution* obj, int *returnSize) {
    *returnSize=obj->size;
    if(obj->size==0)
        return NULL;
    srand(time(NULL));
    for(int i=0;i<obj->size;i++){
        int r1 = rand()%obj->size;
        swap(obj->nums,i,r1);
    }
    return obj->nums;
}