class Solution:
    def beautifulSubarrays(self, nums: List[int]) -> int:
        ans = prefix = 0 
        freq = Counter({0 : 1})
        for i, x in enumerate(nums): 
            prefix ^= x
            ans += freq[prefix]
            freq[prefix] += 1
        return ans 