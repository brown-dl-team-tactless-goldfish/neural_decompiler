int minEx=arrSize*0.25;

for(int i=0;i<arrSize;i++){
      int count=0;
    for(int j=i;j<arrSize;j++){
        
         if(arr[i]==arr[j]){
             
              count++;
         }
        
        else{ break;}
    }
    
    if(count>minEx){return arr[i];}
}