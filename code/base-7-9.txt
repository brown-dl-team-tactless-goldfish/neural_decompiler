char* convertToBase7(int num) {
    int base = 1;
    long ans = 0;
    while(num != 0)
    {
        ans += num % 7 * base;
        num = num / 7;
        base *= 10;
    }
    char* sans;
    asprintf(&sans, "%ld", ans);
    return sans;
}