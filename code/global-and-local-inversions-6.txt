class Solution {
public:
    bool isIdealPermutation(vector<int>& A) {
        
        int n = A.size();
      
        for (int i = 0; i < n; i++) {
            if (i-1 > A[i] || i+1 < A[i])
                return false;
        }
        
        return true;
    }
};