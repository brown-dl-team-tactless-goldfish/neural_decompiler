int findFinalValue(int* nums, int numsSize, int original){

    for (int i =0; i < numsSize; i++)
    {
        if (nums[i] == original)
        {
            original = original * 2;
            original = findFinalValue(nums, numsSize, original);
        }
    }

    return original;
}