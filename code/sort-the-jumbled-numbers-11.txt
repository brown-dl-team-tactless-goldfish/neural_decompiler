    helper.push_back({stoi(ans),j});
        j++;
        
    }
sort(helper.begin(),helper.end());

    for(auto it:helper)final.push_back(nums[it.second]);
// for(auto it:final)cout<<it<<" ";
return final;
    
}