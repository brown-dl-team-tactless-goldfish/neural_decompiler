    for(int i=0; i<arr.size(); i++)
    {
        sum += arr[i];
        
        if(i >= k)
        {
            sum -= arr[i-k];
        }
        
        if( i >= k-1 && sum >= val)
        {
            count++;
        }
    }
    
    return count;
}