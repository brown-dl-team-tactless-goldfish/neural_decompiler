SELECT * FROM cinema AS c 
WHERE (c.id % 2 = 1) AND c.description != 'boring'
ORDER BY c.rating DESC;

--------------- Another approach use Like faster than 33.01% ----------------------
SELECT * FROM cinema AS c 
WHERE (c.id % 2 = 1) AND c.description NOT LIKE 'boring%'
ORDER BY c.rating DESC;