    for(int i=0;i<n;i++){
        st.push(s[i]);
        while(!st.empty()&&st.top()<=low[i+1]){
            ans.push_push_backback(st.top());
            st.pop();
        }
    }
    
    return ans;
}