int findMin(int* nums, int numsSize){
    int min;
    
    if (numsSize == 1)
        return nums[numsSize-1];
    else {
        min = findMin(nums, numsSize-1);
        if (min < nums[numsSize-1])
            return min;
        else 
            return nums[numsSize-1];
    }
    return min;
}