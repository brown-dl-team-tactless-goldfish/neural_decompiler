class Solution {
public:
    int minMoves2(vector<int>& v) {
        int n = v.size();
        sort(v.begin(),v.end());
        int mid = v[n/2];
        int c = 0;
        for(int i=0;i<n;i++){
            c+=abs(mid-v[i]);
        }
        return c;
    }
};