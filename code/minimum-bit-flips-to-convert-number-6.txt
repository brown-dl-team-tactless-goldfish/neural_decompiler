int minBitFlips(int start, int goal){
    int x = start ^ goal;   // has 1 at each bit where start and goal are opposites
    int cnt = 0;
    while (x > 0) 
    {
        x &= (x - 1);       // unset rightmost set bit in x
        cnt++;
    }
    return cnt;
}