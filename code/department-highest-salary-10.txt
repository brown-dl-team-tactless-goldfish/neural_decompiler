with cte as (
select max(Salary) as Sal,d.Id as Dep_id ,d.name from Employee e join Department d on e.DepartmentId=d.id   group by DepartmentId)
select c.name as Department , e.name as employee, e.Salary as Salary from Employee e, cte c where e.salary=c.Sal and e.DepartmentId=c.Dep_id ;