bool canThreePartsEqualSum(vector<int>& A) {
	int sum = accumulate(A.begin(),A.end(),0);
	if (sum % 3 != 0) return false;

	int curSum=0, cnt=0, third = sum/3;
	for (int num : A) {
		curSum += num;
		if (curSum == third) {
			cnt++;
			curSum=0;
		}
	}

	return cnt==3 || (sum==0 && cnt > 3);
}