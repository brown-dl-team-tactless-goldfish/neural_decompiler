int numJewelsInStones(char * J, char * S){
    int alpha[123] = {0};
    int jewels = 0;
    
    while (*J)
        alpha[*J++]++;
    
    while (*S)
        jewels += alpha[*S++];
    
    return jewels;
}
