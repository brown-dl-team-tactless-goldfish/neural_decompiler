class Solution {
public:
    int fillCups(vector<int>& amount) {
        int ans=0;
        while(1) {
            sort(amount.begin(), amount.end());
            if (amount.back()<=0)
                break;
            
            amount[1]--, amount[2]--;
            ans++;
        }
        return ans;
    }
};