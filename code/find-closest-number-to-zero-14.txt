class Solution {
public:
    int findClosestNumber(vector<int>& ints) {
       if (nums.size()==0) return 0;
    int min= ints[0];   //a random value initialisation 
    for (int k=0;k<nums.size();k++) {
        // if a positive value is matched it is prioritized              
        if (nums[k]==abs(min) || abs(nums[k])<abs(min)) 
            min=nums[k];            
    }
    return  min;
    }
};
If Helpful Please Like and upvoke