string removeDigit(string n, char d) {
	string res = string(n.size() - 1, '0');
	for (int i = 0; i < n.size(); i++)
		if (n[i] == d)
			res = max(res, n.substr(0, i) + n.substr(i + 1));
	return res;
}