int maxDepth(struct Node *root)
{
    if(root==NULL)
        return 0;
    int max_child=0;
    for(int i=0,tmp;i<root->numChildren;++i)
        if((tmp=maxDepth(root->children[i]))>max_child)
            max_child=tmp;
    return max_child+1;
}