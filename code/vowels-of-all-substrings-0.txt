long long countVowels(char * word){
    long long ans = 0;
    int n = strlen(word);
    for(int i = 0; i < n; i++){
        if(word[i] == 'a' || word[i] == 'e' || word[i] == 'i' || word[i] == 'o' || word[i] == 'u')
            ans +=  (long long)(i + 1) * (n - i);
    }    
    return ans;
}