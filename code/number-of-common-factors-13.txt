int commonFactors(int a, int b)
{
    int count=0;
    int ncount=0;    
    for (int i=1,j=1;i<=a,j<=b;i++,j++)
    {
        if((a%i==0)&&(b%j==0))
        count=count+1;
    }
   return count;
    
}