public string DestCity(IList<IList<string>> paths) {
	var dict = paths.ToDictionary(x => x[0], x => x[1]);
	var dest = dict.First().Value;
	while (dict.ContainsKey(dest)) dest = dict[dest];
	return dest;
}