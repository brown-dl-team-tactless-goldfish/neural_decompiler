int maxCoins(vector<int> &piles)
{
    int cont_1 = 0;
    sort(piles.begin(), piles.end());

    if (piles.size() == 3)
    {
        return piles[1];
    }

    for (int i = piles.size() / 3; i < piles.size(); i++)
    {
        cont_1 += piles[i];
        i++;
    }

    return cont_1;
}