char** findWords(char** words, int wordsSize, int* returnSize) {


	char **result;
	int last=0,k=0;
	result = (char **)malloc(sizeof(*result)*wordsSize);

		for(int i=0;i<wordsSize;i++)
		{
			int j=0,count=0;
			int n = strlen(words[i]);

			last = Judge(*(*(words+i)+j));
			while(*(*(words+i)+j) != '�')
			{
				if(last == Judge(*(*(words+i)+j)))
					count++;

				if(count==n)
				{
					*(result+k) = (char *)malloc(sizeof(*result)*n);
					strcpy(*(result+k) , *(words+i));
					k++;
				}    		
				j++;
			}    	
		}

	*returnSize = k;
	return  result;