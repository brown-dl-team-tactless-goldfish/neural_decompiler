

bool isToeplitzMatrix(int** matrix, int matrixSize, int* matrixColSize){
    for(int i = 0; i < matrixSize - 1;i++)
    {
        if(memcmp(matrix[i],(matrix[i+1] + 1),(*matrixColSize - 1)*sizeof(int)))
        {
            return false;
        }
        
    }
    return true;
}