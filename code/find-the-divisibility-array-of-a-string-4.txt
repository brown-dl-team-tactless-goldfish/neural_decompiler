class Solution {
public:
    vector<int> divisibilityArray(string word, int m) {
        vector<int> res;
        long long num = 0;
        for(char c : word) {
            num = num * 10 + (c - '0');
            num %= m;
            res.push_back(num == 0);
        }
        return res;
    }
};