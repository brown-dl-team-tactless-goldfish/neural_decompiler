if(numsSize==0)
    return NULL;

if(numsSize==1){
    sprintf(str,"%d",nums[0]);
    return str;
}
        
qsort(nums, numsSize, sizeof(int), catcmp);
for (i = 0; i < numsSize; i++){
	sprintf(str,"%d", nums[i]);
	strcat(ans,str);
}
ans[i]='\0';
// printf("%s",ans);
// putchar('\n');
return ans;