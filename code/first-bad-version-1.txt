int firstBadVersion(int n) {
    int i=0;
    int j=n;
    while(j>(i+1))
    {
        int k= i+ (j-i)/2;
        if (isBadVersion(k))
            j = k;
        else
            i = k;
        
    }
    return(j);
}
