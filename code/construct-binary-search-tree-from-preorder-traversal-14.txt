if(preorderSize > 0)
{
   struct TreeNode* rt =  createN(preorder[0]);        
   int root = preorder[0];
   int i =1;
   while(i<preorderSize)
   {
       if(preorder[i]>root)
       {
           break;
       }
       i++;
   }
   
   rt->left =  bstFromPreorder(preorder+1, i-1);        
   rt->right =  bstFromPreorder(preorder+i, preorderSize-i);
    
    return rt;
}

return NULL;