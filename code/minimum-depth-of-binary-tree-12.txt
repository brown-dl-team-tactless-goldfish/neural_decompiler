int specMin(const int a, const int b) // minimum non-zero (except both zero)
{
  return (!a || !b) ? a+b : (a<b ? a : b);
}

int minDepth(struct TreeNode *root)
{
  return !root ? 0 : 1 + specMin(minDepth(root->left), minDepth(root->right));
}