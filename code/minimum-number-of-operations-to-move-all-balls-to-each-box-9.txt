int ansSize=strlen(boxes);
int *ans=(int*)malloc(ansSize*sizeof(int)),i,j;
for(i=0;i<ansSize;i++)
{
    ans[i]=0;
    for(j=0;j<ansSize;j++)
    {
        if(boxes[j]=='1' && i!=j)
        {
            ans[i]+=abs(i-j);
        }
    }
}
*returnSize=ansSize;
return ans;