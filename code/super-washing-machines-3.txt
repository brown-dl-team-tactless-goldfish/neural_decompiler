    sum = accumulate(machines.begin(),machines.end(), 0);
    
    mid = sum / n;
    
    if(sum % n != 0)
    {
        return -1;
    }
    
    for(auto &x: machines)
    {
        extra += x - mid;
        
        ans = max(ans,max(x-mid, abs(extra)));
    }
    
    return ans;
    
    
}