class Solution:
    def maxRepeating(self, sequence: str, word: str) -> int:
        res = 0
        
        tmp = word
        
        while tmp in sequence:
            # print ("tmp = ", tmp)

            tmp += word
            res += 1
            # print ("res = ", res)
            
        return res