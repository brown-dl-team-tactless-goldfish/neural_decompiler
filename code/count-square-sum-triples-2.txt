public int CountTriples(int n) 
{
	int result = 0;
	for(int a = 1; a <= n; a++)
	{
		for(int b = 1; b <= n; b++)
		{                
			int c = (int)Math.Sqrt(a* a + b * b);
			if(c * c == a * a + b * b && c <= n)
			{
				result++;
			}
		}            
	}

	return result;
}