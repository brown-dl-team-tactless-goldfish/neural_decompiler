vector<int> powerfulIntegers(int x, int y, int bound, unordered_set<int> st = {}) {
	for (int i = 1; i < bound; i = x == 1 ? bound : i * x)
		for (int j = 1; i + j <= bound; j = y == 1 ? bound : j * y)
			st.insert(i + j);
	return vector<int>(begin(st), end(st));
}