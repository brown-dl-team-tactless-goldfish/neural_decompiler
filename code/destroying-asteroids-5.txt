class Solution {
public:
  bool asteroidsDestroyed(int mass, vector<int>& as) {
    long m = mass;
    sort(as.begin(), as.end());
    for(auto a: as)
      if(long(a) > m) return false;
      else m += a;
    
    return true;
  }
};