int reverse(int x) 
{
    int m=x;
    int t=0;
    int q=0;//save the last t
    int n=0;
    while(m!=0)
    {   q=t;
        n = m % 10;
        t=t*10+n;
        m=m/10;
        if((t-n)/10!=q) return 0;//check overflow
    }
    
    return t;    
}