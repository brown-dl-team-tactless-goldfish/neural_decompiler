class Solution {
public:
    int countPrefixes(vector<string>& words, string s) {
        int cnt = 0;
        
        for(auto c  : words) {
            for(int i = 1; i <= s.size(); i++) {
                if(c == s.substr(0,i)) cnt++;
            }
        }
        
        return cnt;
    }
};