    int longestSubarray(vector<int>& nums) {
        int res = 0, len = 0, xmax = *max_element(nums.begin(), nums.end());
        for(auto x:nums){
            x == xmax ? len++ : len = 0;
            res = max(res, len);            
        }
        return res;
    }