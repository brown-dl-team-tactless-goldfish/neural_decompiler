for (int i = 1; i < stones.size(); i++)
{
    int c = stones[i];
    for (auto j : g[c])
    {
        g[c + j].insert(j);
        g[c + j - 1].insert(j - 1);
        g[c + j + 1].insert(j + 1);
    }
}
if (g[stones.back()].size() == 0)
    return false;
return true;