void reverseString(char* s, int sSize) {
    static int c = 0;
    if(c == (sSize/2))
    {
        return;
    }
    
    // swap s[c] and s[sSize-c-1]
    char temp = s[c];
    s[c] = s[sSize-c-1];
    s[sSize-c-1] = temp;
    c = c + 1;
    
    reverseString(s,sSize);
}