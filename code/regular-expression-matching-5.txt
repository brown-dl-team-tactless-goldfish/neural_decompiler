bool isMatch(char * s, char * p){
    if (!*p) return !*s;
    if (*(p + 1) == '*') {
        do {
            if (isMatch(s, p + 2)) return true;
        } while (*s && (*s++ == *p || *p == '.'));
    }
    if (*s && (*p == '.' || *p == *s)) return isMatch(s + 1, p + 1);
    return false;
}