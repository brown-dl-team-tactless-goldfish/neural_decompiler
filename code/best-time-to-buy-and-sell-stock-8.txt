int maxProfit(int* prices, int pricesSize){
    int min = INT_MAX, profit = 0, i;
    
    for(i = 0; i<pricesSize; i++){
      if(prices[i]<min) min = prices[i];
      else if(prices[i]-min > profit) profit = prices[i]-min; 
    }
    return profit;
}