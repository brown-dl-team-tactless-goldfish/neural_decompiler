int countTriples(int n){
    int ans = 0;
    for(int i = 1; i < n; i++){
        for(int j = i+1; j < n; j++){
            int val = i*i +j*j;
            int t = sqrt(val);
            if(t*t == val && t <= n)
                ans += 2;
        }
    }
    return ans;
}