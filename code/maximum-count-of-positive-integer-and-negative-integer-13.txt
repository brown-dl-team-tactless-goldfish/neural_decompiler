class Solution {
public:
    int maximumCount(vector<int>& nums) {
        int x=0,y=0;
        for(int it:nums){ 
            if(it<0)x++;
            else if(it>0)y++; 
        }
        return max(x,y);
    }
};