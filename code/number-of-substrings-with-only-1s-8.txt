int numSub(string s) {
	int res = 0;
	long long curOnes = 0;
	int mod = 1000000007;
	for (char c : s) {
		if (c == '1')
			curOnes++;
		else {
			res += (curOnes * (curOnes + 1) / 2) % mod;
			curOnes = 0;
		}
	}
	if (curOnes)
		res += (curOnes * (curOnes + 1) / 2) % mod;
	return res;
}