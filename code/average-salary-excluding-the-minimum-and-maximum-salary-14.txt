int cmp(const void *a, const void *b){
    return (*(int *)a - *(int *)b);
}
double average(int* salary, int salarySize){
    int n = salarySize, sum = 0;
    qsort(salary, salarySize, sizeof(int), cmp);
    for(int i = 1; i < n-1; i++)
        sum += salary[i];
    return sum / (double)(n-2);
}