

int minOperations(int n){
    int median=n,steps=0;
    int arr[n],i;
    
    for(i=0;i<n;i++)    arr[i]=(2*i)+1;
    
    for(i=0;i<n/2;i++)
    {
        steps+=(median-arr[i]);
    }
    return steps;
}