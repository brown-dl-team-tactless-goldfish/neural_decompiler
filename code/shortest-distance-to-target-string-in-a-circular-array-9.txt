class Solution {
public:
#define loop for(int i = 0; i < n; i++) 
    int closetTarget(vector<string>& words, string target, int startIndex) {
        int n = size(words), ans = INT_MAX;
        
            loop 
                if(words[i] == target) {
                    int disBetweenStartIndexToTarget = abs(startIndex - i);
                    int disBetweenStartIndexToTargetReverseOrder = abs(n - abs(startIndex - i));
                    ans = min(ans, min(disBetweenStartIndexToTarget,disBetweenStartIndexToTargetReverseOrder)); 
                }
        
        return ans == INT_MAX ? -1 : ans;
    }
};