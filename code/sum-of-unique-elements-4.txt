class Solution(object):
    def sumOfUnique(self, nums):
        sum = 0
        for i in range(0,len(nums)):
            count = 0
            count = nums.count(nums[i])
            if count ==1:
                sum = sum+ nums[i]
        return sum