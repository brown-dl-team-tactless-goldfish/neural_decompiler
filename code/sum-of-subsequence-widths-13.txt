    int sumSubseqWidths(vector<int>& a) {
        sort(a.begin(),a.end());
        long long n=a.size(),i,ans=0,x=1,m=1e9+7;
        for(i=0;i<n;i++)
        {
            x%=m;
            ans=(ans+(x*a[i])%m-(x*(a[n-i-1]))%m)%m;
            x*=2;
        }
        
        return (ans+m)%m;
    }