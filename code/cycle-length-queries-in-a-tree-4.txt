    def cycleLengthQueries(self, n, queries):
        res = []
        for x,y in queries:
            res.append(1)
            while x != y:
                x, y = min(x, y), max(x, y) // 2
                res[-1] += 1
        return res