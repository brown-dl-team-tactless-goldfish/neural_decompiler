        print("s = ", s)
        num = int(s, 2)
        print("num = ", num)

        def helper(curInt, curSteps):
            if (curInt == 1):
                return curSteps
            elif (curInt % 2 == 0):
                return helper(curInt // 2, curSteps + 1)
            else:
                return helper(curInt+1, curSteps + 1)

        res = helper(num, 0)

        print("res = ", res)

        return res