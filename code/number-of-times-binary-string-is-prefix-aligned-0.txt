int numTimesAllBlue(int* flips, int flipsSize){
    int max = -1;
    int num = 0;
    int ans = 0;
    for(int i = 0; i < flipsSize; i++){
        num++;
        if(flips[i] > max){
            max = flips[i];
        }
     if(max == num)
        ans++;
    }

    return ans;
}