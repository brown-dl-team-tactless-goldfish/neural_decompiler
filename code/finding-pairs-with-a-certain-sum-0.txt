void add(int index, int val) 
{
    p[index] = p[index] + val;    
}

int count(int tot) 
{
   map<int,int> MAP;
    for (auto i : v)
        MAP[i]++;
    int c = 0;
    for (auto j : p)
        if (MAP[tot-j])
            c = c + MAP[tot - j];
    
    return c;
}