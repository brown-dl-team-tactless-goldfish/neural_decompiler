int i, max = 0, hold = 0;

for(i = 0; i < gainSize; i++)
{
 
    if(i == 0)
    {
       
        if(hold + gain[i] > max)
            max = hold + gain[i];
        
        hold += gain[i];
    }
    
    else
    {
        
        if(hold + gain[i] > max)
            max = hold + gain[i];
    
        hold += gain[i];
    }
        
}
return max;