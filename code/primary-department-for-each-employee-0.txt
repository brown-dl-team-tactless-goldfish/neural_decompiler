SELECT e.employee_id, e.department_id
FROM Employee e JOIN (SELECT employee_id, COUNT(*) AS cnt FROM Employee GROUP BY employee_id) c 
ON e.employee_id = c.employee_id
WHERE c.cnt = 1 OR e.primary_flag = 'Y' ORDER BY e.employee_id