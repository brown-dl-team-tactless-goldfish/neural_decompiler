bool judgeSquareSum(int c){
    int bound = sqrt(c);
    for(int a = 0; a <= bound; a++){
        int A = a*a;
        int b = sqrt(c - A);
        if(b*b == (c-A))
            return true;
    }
    
    return false;
}