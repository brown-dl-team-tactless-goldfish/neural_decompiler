int minimumMoves(char * s){
    int ansCnt = 0, idx = 0;
    int len = strlen(s);
    while(idx < len)
    {
        if(s[idx++]=='X')
        {
            ansCnt++;
            idx += 2;
        }
    }
    return ansCnt;
}