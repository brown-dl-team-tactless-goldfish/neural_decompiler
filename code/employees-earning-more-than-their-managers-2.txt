with cte as (
    select id, salary
    from Employee
    where id in (select managerId from Employee))

select e.name Employee
from Employee e
left join cte c on c.id=e.managerId
where e.salary>c.salary