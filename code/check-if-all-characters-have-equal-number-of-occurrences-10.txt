
bool areOccurrencesEqual(char * s){
    int count[26];                  
    memset(count,0,sizeof(int)*26); 
    for(int i=0;i<strlen(s);i++){
        count[s[i]-97]++;        
    }
    int first=count[s[0]-97];       
    for(int i=0;i<26;i++){
       if(count[i]==0||count[i]==first){
       } 
        else{
            return false;
        }
    }
    return true;
}