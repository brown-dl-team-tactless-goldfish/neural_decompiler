int repeatedNTimes(int* A, int ASize) {
    int hash_table[10000] = {0};
    for (int i = 0; i < ASize; i++) {
        hash_table[A[i]]++;
        if (hash_table[A[i]] > 1)
            return A[i];
    }
    return 0;
}