
int largestAltitude(int* gain, int gainSize){
    int currAlt = 0, maxAlt = 0;
    for (int i = 0; i < gainSize; ++i){
        currAlt += gain[i];
        maxAlt = (int) fmax(currAlt, maxAlt);
    }
    return maxAlt;
}