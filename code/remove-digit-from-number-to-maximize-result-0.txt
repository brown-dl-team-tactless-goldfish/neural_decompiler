char * removeDigit(char * number, char digit)
{
   int max=INT_MIN;
    int index=0;
for(int i=0;i<strlen(number);i++)
{
    if(number[i]==digit)
    {
       if(number[i]>number[i+1])
       {
           index=i;
        }
        else if(number[i]<number[i+1])
        {
           index=i;
            break;
        }
    }
   
    
 }
   
    for(int i=index;i+1<=strlen(number);i++)
{
    number[i]=number[i+1];
    }
   
    
 
    return number;
}