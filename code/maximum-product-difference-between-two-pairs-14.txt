class Solution:
    def maxProductDifference(self, nums: List[int]) -> int:
        data = nums
        data.sort()
        # print ("data = ", data)
        
        if (len(data) < 4):
            return 0
        
        return data[-1]*data[-2] - data[0]*data[1]