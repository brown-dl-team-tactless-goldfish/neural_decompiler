	int i;
    int c = s[0];
    int count = 1;
    int maximum = 1;
    for(i=1;i<s.length();i++)
    {
        if( (c+1) == s[i])
        {
            count++;
            c = s[i];
        }
        
        else
        {
            c = s[i];
            maximum = max(count,maximum);
            count = 1;
        }
    }
    
     maximum = max(count,maximum);
    
    
    
    return maximum ;