bool makeEqual(char ** words, int wordsSize) {
    int hist[26] = {0};
    for (int i = 0; i < wordsSize; i++)
        for (int j = 0; words[i][j] != '\0'; j++)
            hist[words[i][j] - 'a']++;
    for (int i = 0; i < 26; i++)
        if (hist[i] % wordsSize) return false;
    return true;
}