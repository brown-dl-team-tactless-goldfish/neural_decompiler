char * removeStars(char * s){
    int top=0;
    for(int i=0; s[i]!='\0'; i++){
        if(s[i]!='*'){
            s[top]=s[i];
            top++;
        }
        else{
            if(top!=0)
                top--;
        }
    }
    s[top]='\0';
    return s;
}