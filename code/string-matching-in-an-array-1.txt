    vector <string> v;
    for(auto x:words)
    {
        for(int i=0;i<words.size();i++)
        {
              if(x!=words[i]  && words[i ].find(x)!=string::npos)
              {   v.push_back(x); break; }
        }
    }
    return v;
}