char* largestOddNumber(char* num) {
    int len = strlen(num), cnt = 0, nums = num[len - 1 - cnt] - '0';
    while (nums % 2 == 0 && cnt < len) {
        cnt++;
        if (len - 1 - cnt >= 0) nums = num[len - 1 - cnt] - '0';
    }
    num[len - cnt] = '\0';
    return num;
}