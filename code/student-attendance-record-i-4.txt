bool checkRecord(char * s){
    int a = 2;
    int l = 3;
    int len = strlen(s);
    for(int i=0; i<len; i++){
        if(s[i] != 'L') l = 3;
        if(s[i] == 'A' && !--a) return false;
        if(s[i] == 'L' && !--l) return false;
    }
    return true;
}