int pairSum(struct ListNode* head){
struct ListNode *p=head;
    int c=0;
    
    while(p!=NULL)
    {
        c++;
        p=p->next;
    }
    printf("%d\n",c);
   
    int *arr=(int *)malloc(sizeof(int)*c);
   p=head;
    int i=0;
    while(p!=NULL)
    {
        arr[i]=p->val;
        i++;
        p=p->next;
        
    }
    int max=arr[0]+arr[c-1];
    for(int i=0;i<c/2;i++)
    {
        if((arr[i]+arr[c-i-1])>max)
            max=arr[i]+arr[c-i-1];
    }
    
    return max;
    
}