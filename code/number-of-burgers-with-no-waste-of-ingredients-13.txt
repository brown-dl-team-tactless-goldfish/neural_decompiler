class Solution {
public:
    using VI = vector<int>;
    VI numOfBurgers(int a, int b, int c=0) {
        while (a > 0 && b > 0 && a != 2 * b)
            a -= 4, --b, ++c;
        if (a == 2 * b)
            return {c,b};
        else
            return {};
    }
};