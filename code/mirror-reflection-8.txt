int gcd(int p, int q) {
  int r;
  while (r = p % q) {
    p = q;
    q = r;
  }
  return q;
}

int mirrorReflection(int p, int q){
  int d = gcd(p, q);
  p /= d;
  q /= d;
  
  if ((p & 1) && (q & 1)) {
    return 1;
  } else if (p & 1) {
    return 0;
  } else {
    return 2;
  }
}