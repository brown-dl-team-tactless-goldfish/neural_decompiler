class Solution {
public:
    int numTimesAllBlue(vector<int>& light) {
        long long int i,n=light.size(),s=0,sum=0,ans=0;
        for(i=0;i<n;i++){
            s+=light[i];
            sum=(i+1)*(i+2)/2;
            if(s==sum) ans++;
        }
        return int(ans);
    }
};