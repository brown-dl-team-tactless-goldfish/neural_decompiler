    while(n)
    {
        int rem = n % (-2);
        
        n = n / (-2);
        
        if(rem < 0)
        {
            rem += 2; // absolute val of negBase
             
            n++;
            
        }
        
        str = to_string(rem)  + str;
    }
    
    return str == ""? "0":str;
}