char * temp = malloc(sizeof(char)*count_letter);
int index =0;
for(int i =0;i <len;i++){
    if(!(is_not_letter(s[i]))){
    temp[index] = s[i];
    index++;
}
}

index --;

for(int i= 0;i <len;i++){
    if(result[i] == ' '){
        result[i] = temp[index--];
    }
}

for(int i=0;i<len;i++){
    s[i] = result[i];
}
free(result);
return s;    