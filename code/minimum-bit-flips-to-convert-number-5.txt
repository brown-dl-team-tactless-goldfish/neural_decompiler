int minBitFlips(int start, int goal){
    int diff, count = 0;
	//after xor operation, 1 means the different between start and goal
    diff = start^goal;
    while(diff){
        diff &= (diff-1);  //x & (x-1) will erase the last bit
        count++;
    }
    return count;
}