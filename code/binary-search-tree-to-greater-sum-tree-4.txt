int bst(struct TreeNode* now, int num)
{
    if(now==NULL)return num;
    now->val+=bst(now->right, num);
    num=bst(now->left, now->val);
    return num;
}

struct TreeNode* bstToGst(struct TreeNode* root){
    bst(root, 0);
    return root;
}