int alternateDigitSum(int n){
    int sum = 0, op = 1, count=0;
    while(n>0){
        sum += (n%10) * op;
        n/=10;
        op *= -1;
        count++;
    }
    if (count % 2 == 0) return sum * -1;
    return sum; 
}