class Solution {
public:
    double trimMean(vector<int>& arr) {
        double t = 0;
        sort(arr.begin(), arr.end());
        int sz = arr.size() / 20, start = sz, adjsize = arr.size() - sz;
        for (int i = sz; i < adjsize; i++) t += arr[i];
        return t/(adjsize - sz);
    }
};