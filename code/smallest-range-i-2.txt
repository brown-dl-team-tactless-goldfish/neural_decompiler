int smallestRangeI(int* A, int ASize, int K){
int max;
int min;
int range;
int i;
if (ASize<2) return 0;
max=A[0];
min=A[0];
for (i=0; i<ASize; i++)
    if (max<A[i]) max=A[i];
      else if (min>A[i]) min=A[i];
range=abs(max-min) - 2*K;
if (range<0) range=0;
return range;
}