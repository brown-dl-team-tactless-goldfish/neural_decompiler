int nowSize=1;
int sum=0;

while(nowSize<=arrSize)
{
    for(int i=0;i<=arrSize-nowSize;i++)
    {
        for(int j=0;j<nowSize;j++)
        {
            sum=sum+arr[j+i];
        }
        
    }
    nowSize=nowSize+2;
}


return sum;