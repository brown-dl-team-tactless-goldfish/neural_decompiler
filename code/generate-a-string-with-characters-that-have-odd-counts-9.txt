public class Solution {
    public string GenerateTheString(int n) {
        char[] c = new char[n];
        
        for(int i = 0; i < n; i++)
            c[i] = 'a';
        
        if((n & 1) == 0)
            c[0] = 'b';
        
        return new string(c, 0, c.Length);
    }
}