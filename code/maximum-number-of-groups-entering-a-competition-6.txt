    public int MaximumGroups(int[] grades)
    {
        int n = grades.Length;
        int index = 0;
        int group = 0;
        while (index < n)
        {
            group++;
            if (n - index < group)
                return group - 1;
            index += group;
        }
        return group;
    }