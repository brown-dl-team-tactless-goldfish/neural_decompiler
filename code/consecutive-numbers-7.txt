select distinct a.Num as ConsecutiveNums from Logs as a # only want to know distinct Num
left join Logs as b on a.Id = b.Id -1 # second concessive
left join Logs as c on b.Id = c.Id -1 # third concessive 
where a.Num = b.Num and b.Num = c.Num # three Num are the same