    int largestCombination(vector<int>& candidates) {
        vector<int> cnt(30, 0);
        for(auto x:candidates){
            bitset<30> b(x);
            for(int i=0;i<30;i++) cnt[i] += b[i];
        }
        return *max_element(cnt.begin(), cnt.end());
    }