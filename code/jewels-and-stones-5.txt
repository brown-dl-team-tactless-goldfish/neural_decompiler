int numJewelsInStones(char * J, char * S){
    int j_len, s_len, i, j, t;
    for(i=0, j_len=strlen(J), s_len=strlen(S), t=0; i<s_len; i++)
        for(j=0; j<j_len; j++)
            if (J[j] == S[i])
                t++;
    return t;
}