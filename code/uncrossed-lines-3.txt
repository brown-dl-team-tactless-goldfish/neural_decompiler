for (; i < nums1Size; i++)
{
    for (; k < nums2Size; k++)
    {
        if (nums1[i] == nums2[k])
        {
            line_ctr++;
            matchfound_flag = 1;
            printf("Matching pair #%d, is: %d \n", line_ctr, nums1[i]);
            last_MatchedElement = k;
        }
    }
    k = last_MatchedElement;
}

return(line_ctr);