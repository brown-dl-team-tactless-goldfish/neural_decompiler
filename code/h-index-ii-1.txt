int hIndex(int* cit, int len) {
    int step = len - 1, low = 0;
    while(step > 0)
    {
         while(low + step > len || ( step && cit[len - low -1 - step] <= low + step))
            step >>= 1;
        low += step;
    }
    if(!len || cit[len-1] < 1)
        return 0;
    return low+1;
}