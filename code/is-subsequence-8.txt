

bool isSubsequence(char * s, char * t){
    int i=0,j=0;
    int l1=strlen(s);
    int l2=strlen(t);
    while(s[i])                             //checking the characters of first string in second string
    {
        while(t[j]!=s[i] && j<l2)           //skip the mismatch character
            j++;
        if(j>=l2)                           //if we reach to end then return false
            return false;
        i++;                                //move forward
        j++;
    }
    return true;
}