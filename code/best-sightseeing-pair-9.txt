    int n = a.size(), ans = INT_MIN;
    
    int val = a[0] + 0;
    
    for(int j=1; j<n; j++)
    {
       ans = max(ans, val + a[j] - j) ;
    
       val = max(val, a[j] + j);
    }
    
    return ans;
    
}