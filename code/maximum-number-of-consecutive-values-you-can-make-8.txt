class Solution {
public:
    int getMaximumConsecutive(vector<int>& A) {
        sort(begin(A), end(A));
        int x = 0;
        for(auto e : A) {
            if(e > x+1) break;
            else x += e;
        }
        return x+1;
    }
};