int maximumValue(vector<string>& strs) {
	int res = 0;
	for (const string& s : strs) {
		bool number = true;
		for (const char& c : s) {
			if (abs(c - 'a') <= 25) {
				number = false;
				break;
			}
		}
		res = max(res, number ? stoi(s) : (int)s.size());
	}
	return res;
}