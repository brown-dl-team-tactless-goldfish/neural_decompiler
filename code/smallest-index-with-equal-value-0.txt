int smallestEqual(int* nums, int numsSize) {
    int min = INT_MAX;
    for(int i = 0; i < numsSize; i++) {
        if(i % 10 == nums[i]) min = i < min ? i : min;
    }
    return min == INT_MAX ? -1 : min;
}