int sign = 1, digitsCounter=0;

double num = 0;

while(*s == ' '){
    s++;
}

if(*s == '-' || *s == '+'){
    if(*s == '-'){
        
        sign = -1;
    }
    s++;
}

while(*s >= '0' && *s <= '9' && digitsCounter <= 11 ){
    
    num*=10;
    num += (int)(*s -'0');
    if(*s!='0'){
        digitsCounter++;
        
    }
    s++;
}

num=num*sign;