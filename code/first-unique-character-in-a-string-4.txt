int firstUniqChar(char * s){
    
    int n = strlen(s);
    
    int* cnt = calloc(26, sizeof(int));
    
    for (int i=0;i<n;i++){    
        cnt[s[i]-'a']++;
    }
   
    for (int i=0;i<n;i++) {
        if (cnt[s[i]-'a']==1) return i;
    }
    
    return -1;
}