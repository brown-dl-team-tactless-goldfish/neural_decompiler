void reverseString(char* s, int sSize){
    int mid = sSize / 2;
    for (int i = 0; i < mid; i++)
    {
        int temp = s[i];
        int end = sSize - i - 1;
        s[i] = s[end];
        s[end] = temp;
    }
}