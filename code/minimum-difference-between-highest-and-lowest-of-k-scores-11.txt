int minimumDifference(vector<int>& nums, int k) {
    sort(begin(nums), end(nums));
    return transform_reduce(cbegin(nums) + k - 1, cend(nums), cbegin(nums), numeric_limits<int>::max(), [](int x, int y) { return min(x, y); }, std::minus<>{});
}