class Solution {
public:
	int minimumCost(vector<int>& cost) {
		sort(cost.begin(), cost.end(), greater<>());
		int c = 1, sum  = 0;
		for(int i = 0; i < cost.size(); i++){
			if(c % 3 != 0){
				sum += cost[i];
			}
			c++;
		}
		return sum;
	}
};