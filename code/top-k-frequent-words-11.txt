for(int j=0;j<500;j++){
    count[j]=0;
}
printf("start");
*returnSize=k;
struct Trie* root=malloc(sizeof(struct Trie));
Initnode(root);
for(int i=0;i<wordsSize;i++){
    fixtrie(root,words[i]);
}

for(int i=0;i<26;i++){
   if(root->next[i]!=NULL){
       printf(" %d",i);
   }
}
printf("/");

char ** result=calloc (k,sizeof(char*));

dfs(root,result,k,0);

printf("happy");

return result;