int lvl=height(root);//find height
*returnSize=lvl;
int **arr=malloc(sizeof(int*)*lvl);
int col=0;
*returnColumnSizes=(int *)malloc(sizeof(int)*lvl);
for(int i=0;i<lvl;i++)
{
    arr[i]=malloc(sizeof(int)*i+500);
    col=0;
    inorder(root,arr,i,&col,0);//storing values into array
    (*returnColumnSizes)[i]=col;//saving column values
}