char * destCity(char *** paths, int pathsSize, int* pathsColSize){
    for (int i = 0, j = 0 ; i < pathsSize && j < pathsSize ; j == pathsSize ? paths += i : 0, i++)
        for (j = 0 ; j < pathsSize && strcmp(paths[i][1], paths[j][0]) ; j++);
    return (*paths)[1];
}