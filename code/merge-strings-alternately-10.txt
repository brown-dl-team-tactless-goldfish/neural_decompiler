char * output= (char *)malloc(sizeof(char)*(length1+length2+1));
while(*word1!='\0' && *word2!='\0')
{
    output[index++]=*word1;
    output[index++]=*word2;
    word1++;
    word2++;
    if(*word1=='\0')
        while(*word2!='\0')
        {
            output[index++]=*word2;
            word2++;
        }
     else if(*word2=='\0')
        while(*word1!='\0')
        {
            output[index++]=*word1;
            word1++;
        }
    
}
output[index]='\0';
return output;