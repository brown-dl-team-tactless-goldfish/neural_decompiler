int specialArray(vector<int>& nums) {
	sort(nums.begin(),nums.end());
	int cnt=1,n=nums.size();
	for(int i=n-1;i>=1;--i,cnt++)
		if(nums[i]>=cnt && cnt>nums[i-1]) return cnt;
	if(cnt<=nums[0]) return cnt;
	return -1;
}