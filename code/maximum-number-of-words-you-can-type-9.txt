int canBeTypedWords(char * text, char * brokenLetters){
    char *p=text,word[10000];
    int i,flag,count=0;
    
    while(sscanf(p,"%s%n",word,&i)>0){
        flag = 1;
        p+=i;
        for(int j=0;j< strlen(brokenLetters);j++){
            for(int k=0;k< strlen(word) && flag==1;k++){
                if(brokenLetters[j]==word[k])
                    flag = 0;
            }
        }
        if(flag==1)
            count++;
    }
    
    return count;
}