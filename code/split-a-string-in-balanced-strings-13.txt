

int balancedStringSplit(char * s){
    int num_R=0,num_L=0,Ans=0,c;
    for(int i=0;*(s+i)!='\0';i++)
    {
       (*(s+i)=='R')?num_R++:num_L++;
        if(num_R==num_L)
        {
            Ans++;
            num_R=0;
            num_L=0;
        }
    }
    return Ans;
}