public class Solution {
    public int MinTimeToType(string word) {
        var res = 0;
        var p = 'a';
        foreach(var c in word){
            var m = Math.Min(Math.Abs(c-p), 26- Math.Abs(c-p));
            p = c;
            res += m+1;
        }
        return res;
    }
}