int numSteps(char * s){
    int n = strlen(s), i = n;
    while (s[--i] == '0');
    if (i == 0) return n - 1;
    while (--i) n += '1' - s[i]; // count number of '0' between first and last '1's
    return n + 1;
}