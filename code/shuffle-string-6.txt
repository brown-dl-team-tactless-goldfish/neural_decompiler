class Solution {
public:
    string restoreString(string s, vector<int>& indices) {
        char shuffled[indices.size()+1];
        for (int i = 0; i < indices.size(); i++) {
            shuffled[indices[i]] = s[i];
        }
        shuffled[indices.size()] = 0;
        return shuffled;
    }
};