int maxPower(char * s){
    int maxLen = 0, cnt = 1;
    for(int i = 0; i < strlen(s)-1; i++)
    {
        if(s[i]!=s[i+1]){
            if(cnt>maxLen) 
                maxLen = cnt;
            cnt = 0;
        }
        cnt++;
    }
    return (cnt>maxLen)? cnt : maxLen;
}