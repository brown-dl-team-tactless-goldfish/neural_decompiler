int maximumDifference(int* nums, int nums_len) {
  int res = -1;
  int min = INT_MAX;
  for (short i = 0; i < nums_len; ++i) {
    min = (min > nums[i]) ? nums[i] : min;
    if (nums[i] > min) {
      res = (res > nums[i] - min) ? res : nums[i] - min;
    }
  }
  return res;
}