class Solution {
public:
    int minImpossibleOR(vector<int>& nums) {
        unordered_map <int,int> ump;
        for(auto &it:nums)
            ump[it]++;
        int i = 1;
        while(ump[i]>0)
            i *= 2;
        return i;
    }
};