int maxDepth(struct TreeNode* root){
    
if (root==NULL)
    return 0;
int depth1= 1+maxDepth( root->left);
 int depth2= 1+maxDepth(root->right);
    if (depth1>depth2)
        return depth1;
    else
        return depth2;
}