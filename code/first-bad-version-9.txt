// The API isBadVersion is defined for you.
// bool isBadVersion(int version);

int firstBadVersion(int n) {
    int s = 1, m;
    while(s < n)
    {
        m = s + (n-s) / 2;
        if(!isBadVersion(m)) {
            s = m + 1;
        } else {
            n = m;
        }
    }
    return s;
}