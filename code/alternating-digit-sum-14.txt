public class Solution {
    public int AlternateDigitSum(int n) {
        int sign = 1, ans = 0;

        foreach(var digit in n.ToString()) {
            ans += (sign * (digit-'0'));
            sign *= -1;
        }
        
        return ans;
    }
}