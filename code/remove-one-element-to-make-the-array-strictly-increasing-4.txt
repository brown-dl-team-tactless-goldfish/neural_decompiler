class Solution:
    def canBeIncreasing(self, nums: List[int]) -> bool:
        n = len(nums)
        
        stack = []
        
        for num in nums:
            idx = bisect.bisect_left(stack, num)
            if (idx == len(stack)):
                stack.append(num)
            else:
                stack[idx] = num
                
        print ("stack == ", stack)
        
        print ("len(stack) +1 = ", len(stack) +1)
        
        return len(stack) +1 >= n