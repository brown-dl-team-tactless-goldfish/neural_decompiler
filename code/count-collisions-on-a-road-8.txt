	public int CountCollisions(string directions) {
        var count = directions.Count(c => c != 'S');
        count -= directions.TakeWhile(c => c == 'L').Count(); // delete all leftmost L
        count -= directions.Reverse().TakeWhile(c => c == 'R').Count(); // delete all rightmost R
        return count;
    }