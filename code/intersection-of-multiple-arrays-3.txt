
    public IList<int> Intersection(int[][] nums) 
	{        
        int[] arr = nums[0];
        for(int i=1; i<nums.Length; i++)
        {
            arr = arr.Intersect(nums[i]).ToArray();
        }
        
        Array.Sort(arr);
        
        return arr;
    }