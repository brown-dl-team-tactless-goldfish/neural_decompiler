char * restoreString(char * s, int* indices, int indicesSize){
    int i;
    char* newString = (char*) malloc((indicesSize+1)*sizeof(char));
    for (i=0;i<indicesSize;i++){
        int t = indices[i];
        newString[t] = s[i];
    }
    newString[indicesSize] = '\0';
    return newString;
}