for(i = 0 ; i < arrSize ; i++) {
    for(j = i + 1 ; j < arrSize ; j++) {
        for(k = j + 1 ; k < arrSize ; k++) {
            if((abs(arr[i] - arr[j]) <= a) && (abs(arr[j] - arr[k]) <= b) && (abs(arr[i] - arr[k]) <= c))
                triplet++;
        }
    }
}
return triplet;