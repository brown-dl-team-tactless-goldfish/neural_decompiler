class Solution {
public:
    int appendCharacters(string s, string t) {
        int j = 0,k = 0,ln = 0;
        while(j<t.length() && k<s.length()){
            if(t[j] == s[k])
                ln++,j++;
            k++;
        }
        return t.length() - ln;
    }
};