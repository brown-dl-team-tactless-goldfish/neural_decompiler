    int len = s.Length;

	if (len%k != 0)
	{
		s = s.PadRight((len / k  + 1)* k, fill);
		len =s.Length;
	}                       

	string[] rets = new string[len / k];

	int i = 0;
	for (int j=0; j< len / k; j++)
	{
		rets[i++] = s.Substring(j*k, k);
	}

	return rets;
    
}