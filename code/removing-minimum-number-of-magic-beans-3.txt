long long minimumRemoval(vector<int>& beans) {
    long n = beans.size();
    long long x = 0,c = LONG_MAX;
    sort(beans.begin(),beans.end());
    for(auto y:beans) x+=y;
    for(int i=0;i<n;i++){
       c = min(c,x-(n-i)*beans[i]);
    }
    return c;
}