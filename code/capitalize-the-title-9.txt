string capitalizeTitle(string title) {
	string result, word;
	stringstream ss(title);
	while (ss >> word) {
		for(char& c : word)
			c = tolower(c);
		if (word.size() > 2)
			word[0] = toupper(word[0]);
		result += word + " ";
	}
	result.pop_back();
	return result;
}