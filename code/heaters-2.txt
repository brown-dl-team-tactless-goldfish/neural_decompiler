qsort(houses, housesSize, sizeof(int), cmpfunc);
qsort(heaters, heatersSize, sizeof(int), cmpfunc);

int minimum = 0;
    int j = 0;
    for(int i = 0 ;i< housesSize ;i++){
        while( j < heatersSize - 1 && (Absnum(heaters[j] - houses[i]) >= Absnum(heaters[j+1] - houses[i])))
            j ++;
        
        minimum = Maxnum(minimum,Absnum(heaters[j] - houses[i]));
    }
    return minimum;