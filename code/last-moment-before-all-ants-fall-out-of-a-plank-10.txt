public class Solution {
    public int GetLastMoment(int n, int[] l, int[] r) {
        int lMax = l.Length == 0 ? 0 : l.Max();
        int rMax = r.Length == 0 ? 0 : n - r.Min();
        return Math.Max(lMax, rMax);
    }
}