int maxDepth(char * s){
    int lc=0,rc=0,mc=0;
    while(*s){
        if(*s=='(') lc++;
        if(*s==')'){
            mc = (lc-rc)>mc?lc-rc:mc;
            rc++;
        }
        s++;
    }
    return mc;
}