int minDepth(struct TreeNode* root){
    int l, r;
    
    if(root == NULL)
        return 0;
    
    l = minDepth(root->left);
    r = minDepth(root->right);
    
    if(l == 0) 
        return r + 1;
    if(r == 0) 
        return l + 1;
    
    return (r < l ? r : l)  + 1;
}