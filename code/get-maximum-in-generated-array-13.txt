class Solution:
    def getMaximumGenerated(self, n: int) -> int:
        
        if n==0:
            return 0;
        
        v = [0,1]
        
        for i in range(1,int((n-1)/2)+1):
            v.append(v[i])
            v.append(v[i]+v[i+1])

        return max(v);
