    vector<int> findLonely(vector<int>& nums) {
        unordered_map<int,int> m;
        for(auto x:nums) m[x]++;
        vector<int> res;
        for(auto x:nums)
            if(m[x] == 1 && m[x-1] == 0 && m[x+1] == 0) res.push_back(x); 
        return res;
    }