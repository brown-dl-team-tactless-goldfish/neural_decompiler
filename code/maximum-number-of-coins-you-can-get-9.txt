int cmp(const void * a,const void * b){    return *(int*) a - *(int*) b;}
int maxCoins(int* p, int pile){
    qsort(p,pile,sizeof(int),cmp);
    int i=(pile)/3;
    int sum=0;
	for(i;i<pile;i+=2)    sum+=p[i];
    return sum;
}