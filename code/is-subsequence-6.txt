bool isSubsequence(char* s, char* t) {
    if (*s=='\0'){
        return true;
    }
    while(*s!=*t){
        if(*t=='\0'){
            return false;
        }
        t = t + 1;
    }
    
    
    return isSubsequence(s+1, t+1);
    
}