long long zeroFilledSubarray(int* nums, int numsSize){
    long long ans = 0, zeroCnt = 0;
    for(int i = 0; i < numsSize; i++){
        if(!nums[i])
            zeroCnt++;
        else
            zeroCnt = 0;
        
        if(zeroCnt)
            ans += zeroCnt;
    }
    return ans;
}