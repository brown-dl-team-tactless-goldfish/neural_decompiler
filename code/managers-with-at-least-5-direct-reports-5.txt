WITH eligible_managers AS (
  SELECT
    E1.ManagerId
  FROM
    Employee E1
  GROUP BY
    E1.ManagerId
  HAVING
    COUNT(E1.ManagerId) >= 5
)
SELECT
  E2.Name
FROM
  eligible_managers EM
  INNER JOIN Employee E2 ON EM.ManagerId = E2.Id;