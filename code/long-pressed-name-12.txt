bool isLongPressedName(char * name, char * typed){
    char* ch = name;
    while (*ch) {
        if (*ch != *typed) return false;
        if (*ch == *(ch+1) && *typed == *ch) { typed++; ch++; continue; }
        while(*ch == *typed) typed++;
        ch++;
    }
    return true;
}