class Solution {
public:
    bool canMeasureWater(int x, int y, int z) {
        
        if (z == 0 || ((x + y >= z) && (z % __gcd(x, y) == 0)))
            return true;
        
        return false;
    }
};