char * removeStars(char * s){
    int len = strlen(s);
    int idx = 0;
    int i;
    for (i=0; i<len; ++i)
    {
        if (s[i] != '*')
        {
            s[idx++] = s[i];
            continue;
        }
        idx = ((idx>0)?(idx-1):idx);
    }
    s[idx] = 0;

    return s;
}