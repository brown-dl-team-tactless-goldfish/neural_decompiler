int arr[100],i=-1;
void tra(struct TreeNode* root) {
    if(root) {
        tra(root->left);
        tra(root->right);
        arr[++i] = root->val;
    }
}

int* postorderTraversal(struct TreeNode* root, int* returnSize){
    tra(root);
    *returnSize = i+1;
    i=-1;
    return arr;
}