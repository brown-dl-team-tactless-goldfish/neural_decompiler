

int triangularSum(int* nums, int numsSize){
    int i,j;
    while(numsSize)
    {
        for(i=0;i<numsSize-1;i++)
        {
            nums[i]=(nums[i]+nums[i+1])%10;
            
        }
        numsSize--;
    }
    return nums[0];
}