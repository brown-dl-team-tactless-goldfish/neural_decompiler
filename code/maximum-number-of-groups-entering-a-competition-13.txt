class Solution {
public:
    int maximumGroups(vector<int>& g) {
        sort(g.begin(),g.end());
        int a = g.size(), k=1, c=0;
        while(a>0){a=a-k;k++;c++; if(k>a) break;}
        return c;
    }
};