struct TreeNode*  searchBST(struct TreeNode* root, int val){
if(root==NULL)
    return NULL;
if(root->val==val)
    return root;
    struct TreeNode*  t=searchBST(root->left,val);
        if(t)
            return t;
     
   return searchBST(root->right,val);
    
}