int removeElement(int* nums, int numsSize, int val){
   int low, high;
   for(low = 0, high = numsSize - 1; low <= high; low++) {
      if(nums[low] == val){
         nums[low] = nums[high];
         low--;
         high--;
      }
   }
   return high + 1;
}