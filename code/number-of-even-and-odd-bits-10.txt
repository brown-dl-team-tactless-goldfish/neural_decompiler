class Solution {
public:
    vector<int> evenOddBit(int n) {
        bitset<32> b(n);
        vector<int> ans(2, 0);
        for(int i = 0; i < b.size(); i++){
            if(b[i]) ans[i % 2]++;
        }
        return ans;
    }
};