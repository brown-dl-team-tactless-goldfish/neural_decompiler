bool escapeGhosts(int** ghosts, int ghostsRowSize, int *ghostsColSizes, int* target, int targetSize) {
    int step,i;
    step=abs(target[0])+abs(target[1]);
    for(i=0;i<ghostsRowSize&&abs(target[0]-ghosts[i][0])+abs(target[1]-ghosts[i][1])>step;++i);
    return i==ghostsRowSize;
}