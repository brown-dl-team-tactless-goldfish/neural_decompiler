public class Solution 
{
    public bool EscapeGhosts(int[][] ghosts, int[] t) 
    {
        foreach(var g in ghosts)
            if(Math.Abs(g[0] - t[0]) + Math.Abs(g[1] - t[1]) <= Math.Abs(t[0]) + Math.Abs(t[1]))
                return false;
        return true;
    }
}