int temp=strlen(word);
if(strlen(sequence)<temp)return 0;
char *cpy=calloc(strlen(sequence)+1,sizeof(char));
int count=0;
for(int i=0;i<strlen(word);i++)
{
    cpy[i]=word[i];
}


while(strstr(sequence,cpy)!=NULL)
{
    
    count++;
    if(strlen(sequence)-strlen(cpy)<strlen(word))break;
    sprintf(cpy+(count*temp),"%s",word);

}

free(cpy);
return count;