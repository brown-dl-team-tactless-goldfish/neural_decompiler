vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries, vector<int> res = {}) {
	for (int i = 1; i < arr.size(); ++i) arr[i] ^= arr[i - 1];
	for (auto &q : queries) res.push_back(arr[q[1]] ^ (q[0] ? arr[q[0] - 1] : 0));
	return res;
}