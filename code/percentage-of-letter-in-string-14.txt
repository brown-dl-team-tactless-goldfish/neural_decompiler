public class Solution {
    public int PercentageLetter(string s, char letter) {
        var a = s.ToCharArray().Count(c => c == letter);
        var b = s.Length;
        
        return a * 100 / b;
    }
}