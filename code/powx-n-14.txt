double myPow(double x, int n){
  
   long N = n;
   double curProd = x;
   double ans = 1;
   if(N<0){
       curProd = 1/x;
       N = -N;
   }
   while(N){
       if(N&1) ans = ans*curProd;
       curProd = curProd*curProd;
       N = N>>1;
   }
   return ans;
}