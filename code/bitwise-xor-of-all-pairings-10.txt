    int xorAllNums(vector<int>& nums1, vector<int>& nums2) {
        auto sum1 = 0, sum2 = 0;
        for(auto x:nums1) sum1^=x;
        for(auto x:nums2) sum2^=x;
        return ((nums1.size() % 2 == 0) ? 0 : sum2) ^ ((nums2.size() % 2 == 0) ? 0 : sum1);
    }