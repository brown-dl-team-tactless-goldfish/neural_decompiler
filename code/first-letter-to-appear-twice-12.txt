#define N 26
char repeatedCharacter(char * s){
    int map[N] = {0};
    
    for (int i = 0; s[i] != '\0'; i++) {
        map[s[i] - 'a']++;
        if (map[s[i] - 'a'] == 2)
            return s[i];
    }
    
    return ('\0');
}