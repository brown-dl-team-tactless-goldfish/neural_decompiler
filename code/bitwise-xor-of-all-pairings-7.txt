class Solution {
public:
    int xorAllNums(vector<int>& n1, vector<int>& n2) {
        return (n2.size()%2 * accumulate(begin(n1), end(n1), 0, [](auto sum, auto e) {return sum ^e;}))
        ^(n1.size()%2 * accumulate(begin(n2), end(n2), 0, [](auto sum, auto e) {return sum ^e;}));
    }
};