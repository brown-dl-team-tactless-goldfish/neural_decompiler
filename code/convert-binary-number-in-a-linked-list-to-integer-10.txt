class Solution:
    def getDecimalValue(self, head: ListNode) -> int:
        def rec(head, res):
            if not head:
                return res
            res = res<<1
            res+=head.val
            return rec(head.next, res)
        
        return rec(head, 0)