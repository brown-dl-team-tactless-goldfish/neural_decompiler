class Solution {
public:
    int minPatches(vector<int>& nums, int n) {
        long ans = 0, mx = 0;
        for (int i = 0; mx < n;) {
            if (i < nums.size() && nums[i] <= mx + 1) mx += nums[i++];
            else mx += mx + 1, ans++;
        }
        return ans;
    }
};