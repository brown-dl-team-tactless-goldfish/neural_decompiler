class Solution {
public:
    int minOperations(vector<int>& nums, vector<int>& div) {
        int gcd=0,ans=0;
        for(auto & i:div)
            gcd=__gcd(i,gcd);
        sort(begin(nums),end(nums));
        for(auto &i:nums){
            if(gcd%i==0)
                break;
            ans++;
        }
        return ans==num.size()?-1:ans;
    }
};