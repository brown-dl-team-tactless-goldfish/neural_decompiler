int Traversal(struct TreeNode* root) {
  if (root == NULL) return;

  return  root->val;
}

bool checkTree(struct TreeNode* root){
    
    
    int a = Traversal(root->left);
    int b = Traversal(root->right);

    if( a+b  == root->val) return true;
    return false;

}