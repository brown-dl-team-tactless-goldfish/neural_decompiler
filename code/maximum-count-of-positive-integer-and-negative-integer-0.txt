int maximumCount(int* nums, int numsSize){
    int posi = 0;
    int neg = 0;
    for(int i = 0; i < numsSize; i++){        
        if(nums[i] < 0)
            neg++;
        if(nums[i] > 0)
            posi++;
    }
    return fmax(posi, neg);
}