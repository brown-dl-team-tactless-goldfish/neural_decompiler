int cmp(const void *x, const void *y)
{
    return (*(int*)x-*(int*)y);
}
int minMoves(int* a, int n)
{
    int s=0;
    qsort(a, n, sizeof(int), cmp);
    for(int i=1;i<n;i++)s+=abs(a[0]-a[i]);
    return s;
}