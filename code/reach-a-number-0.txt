int reachNumber(int target) {
    target = abs(target); 
    int sum = 0;
    int i = 1;
    
    while(1)
    {
        sum += i;
        if(sum >= target && (sum % 2 == target % 2)) return i;
        i++;
    }
    
    return 0;
}