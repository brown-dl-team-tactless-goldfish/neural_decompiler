#include <stdio.h>

int reachNumber(int target) {
    
    int sum = 0;
    int i = 1;
    if (target < 0)
        target = -target;
    
    while(sum<target) {
        sum += i;
        i++;
    }

    while ( (sum-target)%2 != 0 ) {
        sum += i;
        i++;
    }
    return i-1;
    
}

