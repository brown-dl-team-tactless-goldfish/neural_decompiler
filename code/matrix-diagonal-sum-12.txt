int sump=0,sums=0,digonalsum=0;

    for (int i = 0; i < matSize; ++i) 
        {
            sump= sump + mat[i][i];
        
           if(i!=(matSize-i-1))// for excluding overlapping element
            sums= sums + mat[i][matSize- i - 1];
        }

   return digonalsum=sums+sump;