vector<vector<int>> largestLocal(vector<vector<int>>& grid) 
{
	int n=grid.size();
	vector<vector<int>> result(n-2,vector<int>(n-2));
	for(int i=0;i<n-2;i++)
		for(int j=0;j<n-2;j++)
			for(int u=0;u<3;u++)
				for(int v=0;v<3;v++) result[i][j]=max(result[i][j],grid[i+u][j+v]);

	return result;
}