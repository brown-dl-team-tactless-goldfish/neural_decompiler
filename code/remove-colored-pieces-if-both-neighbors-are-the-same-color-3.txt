class Solution {
public:
    bool winnerOfGame(string colors) {
        int cnt[2]{}, rm[2]{};
        for (auto c : colors) {
            if (++cnt[c - 'A'] > 2) rm[c - 'A']++;
            cnt[1 ^ (c - 'A')] = 0;
        }
        return rm[0] > rm[1];
    }
};