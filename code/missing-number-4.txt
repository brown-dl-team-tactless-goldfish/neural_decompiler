int missingNumber(int* nums, int numsSize){
  int i, j, count;
      for(i=0; i<=numsSize; i++){
          count=0;
          for(j=0;j<numsSize;j++){
              if(i==nums[j])
              count++;
          }
          if(count==0)
          break;
      }
      return i;
}