int minElements(vector<int>& nums, int limit, int goal) {
    long long sum = accumulate(nums.begin(), nums.end(), 0LL);
    long long required = goal - sum;
    return int(ceil(abs(required)/(double)abs(limit)));
}