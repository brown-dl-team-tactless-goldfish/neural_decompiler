class Solution {
public:
	long long getDescentPeriods(vector<int>& a) {
		int n=a.size();
		a.push_back(1e6);
		long long res=0;
		for(int i=0;i<n;i++)  {
			int c=2;
			while(i<n&&a[i]==a[i+1]+1) {
				res+=c;
				c++,i++;
			}
			res++;
		}
		return res;
	}
};