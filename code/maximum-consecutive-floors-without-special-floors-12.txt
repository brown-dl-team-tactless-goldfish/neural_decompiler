class Solution {
public:
	int maxConsecutive(int bottom, int top, vector<int>& sp) {
		sort(begin(sp),end(sp));
		int res = sp[0]-bottom;
		for(auto num:sp){
			if(num-bottom>1) res = max(res,num-bottom-1);
			bottom = num;
		}
		return = max(res,top-bottom);
	}
};