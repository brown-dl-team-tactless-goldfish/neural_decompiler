         select count(distinct a.player_id) as pl
         FROM   activity a
         group by player_id having min(a.event_date)+1 IN 
                ( 
                       SELECT b.event_date 
                       FROM   activity b)) c , activity b