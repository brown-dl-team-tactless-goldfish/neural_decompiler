    def decodeCiphertext(self, s, n):
        m = len(s) / n
        res = []
        for i in xrange(m):
            while i < len(s):
                res.append(s[i])
                i += m + 1
        return ''.join(res).rstrip()