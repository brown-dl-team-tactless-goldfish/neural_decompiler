int* sumZero(int n, int* returnSize) {
    int* res = (int*) calloc(n, sizeof(int));
    int start = (n % 2 == 1) ? 1 : 0, num = 1;
    for (int i = start; i < n; i += 2) {
        res[i] = num;
        res[i+1] = -num;
        num++;
    }
    *returnSize = n;
    return res;
}