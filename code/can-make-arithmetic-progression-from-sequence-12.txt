int i,j,temp,k;
k=0;temp=0;
for(i=0;i<arrSize;i++)
    for(j=0;j<(arrSize-i-1);j++)
    {
        if(arr[j]>arr[j+1])
        {
            temp=arr[j];
            arr[j]=arr[j+1];
            arr[j+1]=temp;
        }
    }
    j=0;
    temp=arr[1]-arr[0];
    
    for(i=0;i<arrSize-1;i++)
    {
        if((arr[i+1]-arr[i])==temp)
            j=1;
        else
         k=1;
    }
    if(j==1 && k==0)
    return true;
    else if(j==1 && k==1)
    return false;
    else
    return false;