class Solution {
public:
    vector<int> divisibilityArray(string word, int m) {
        vector<int> ans;
        long long s = 0;
        for(char c : word) {
            s = (s * 10 + (int)c - '0') % m;    
            ans.push_back(!s ? 1 : 0);
        }
        return ans;
    }
};