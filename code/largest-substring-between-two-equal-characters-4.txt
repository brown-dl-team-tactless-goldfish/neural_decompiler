int maxLengthBetweenEqualCharacters(char * s){

    int ans=-1;
    int t[26]={0};
    
    for(int i=0;i<strlen(s);i++)
    {
        if(t[s[i]-'a']==0)
            t[s[i]-'a']=i+1;
        else
            ans=fmax(ans,i-t[s[i]-'a']);
    }
    
    return ans;
}