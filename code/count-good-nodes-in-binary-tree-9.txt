public class Solution {
    
    public int GoodNodes(TreeNode root, int max = int.MinValue) =>
        root==null ? 0 : GoodNodes(root.left, Math.Max(max, root.val)) + GoodNodes(root.right, Math.Max(max, root.val)) + (root.val >= max ? 1 : 0);
        
}