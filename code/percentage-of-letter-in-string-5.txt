int percentageLetter(char * s, char letter){
  
 int count=0;
int l=strlen(s);
  
 
for(int i=0;s[i]!='\0';++i)
  {
  if(s[i]==letter)
   ++count;
}
  count*=100;
  float percentage=(count/l);
  
  return round(percentage);
              
  
  }