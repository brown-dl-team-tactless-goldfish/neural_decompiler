int i;
*returnSize = 2;
int *ret = malloc(sizeof(int)*2);

for(i=1;i<n;i++)
{
    if(checkZero(i) && checkZero(n-i))
    {
        ret[0] = i;    
        ret[1] = n-i;
        return ret;
    }
}

return ret;