class Solution {
public:
    int findLUSlength(string a, string b) {
       if(a == b) return -1; //if the strings are same return -1
        else{
            return max(a.size(), b.size()); // check the max of both of the strings
        }
    }
};