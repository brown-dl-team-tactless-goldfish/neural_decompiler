char * reversePrefix(char * word, char ch){
    char *ptr;
    ptr=strchr(word,ch);
    if(ptr==NULL) return word;
    int j=ptr-word,i=0;
    while(i<j)
    {
        char temp=word[j];
        word[j]=word[i];
        word[i]=temp;
        i++;
        j--;
    }
    return word;

}