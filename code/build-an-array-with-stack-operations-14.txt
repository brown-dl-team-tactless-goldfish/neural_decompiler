vector<string> buildArray(vector<int>& t, int n) {
    vector<string> res;
    for (auto k = 1, i = 0; k <= t.back(); ++k) {
        res.push_back("Push");
        if (k != t[i])
            res.push_back("Pop");
        else
            ++i;
    }
    return res;
}