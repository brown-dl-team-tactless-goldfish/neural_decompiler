int subtractProductAndSum(int n){
int r,sum=0,product=1, result;
while(n!=0)
{
      r=n%10;
      n=n/10;

      product=product*r;
      sum=sum+r;
}
result=product-sum;
printf("%d %d %d",result, sum,product);

return result;
}