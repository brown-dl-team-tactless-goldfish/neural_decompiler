int empBottle = 0;
int ans = 0;
while (numBottles > 0) {		
	ans += numBottles;
	empBottle += numBottles;
	numBottles = 0;
	int newBottle = (empBottle / numExchange);
	numBottles += newBottle;
	empBottle -= newBottle * numExchange;

}
return ans;