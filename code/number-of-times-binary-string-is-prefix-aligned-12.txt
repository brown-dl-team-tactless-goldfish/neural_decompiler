    int numTimesAllBlue(vector<int>& light) {
        int res = 0;
        for(int i = 0, maxP = -1; i < light.size(); i++) {
            maxP = max(light[i], maxP);
            if(maxP == i + 1) res++;
        }
        return res;
    }