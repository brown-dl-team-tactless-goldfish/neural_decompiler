class Solution {
public:
    int minSwaps(string s) {
        int minLevel = INT_MAX, level = 0;
        for (char c : s) {
            level = (c=='[') ? level+1 : level-1;
            minLevel = min(minLevel, level);
        }
        return abs((minLevel-1)/2);
    }
};