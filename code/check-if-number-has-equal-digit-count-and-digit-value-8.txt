bool digitCount(char * s){
    int h[10]={0};
    int c=0;
    for(int i=0;i<strlen(s);i++)    
        h[s[i]-48]++;
    for(int i=0;i<strlen(s);i++)
    {        
        if(h[i]==s[i]-'0')
            c++;
    }        
        
    return c==strlen(s);
}