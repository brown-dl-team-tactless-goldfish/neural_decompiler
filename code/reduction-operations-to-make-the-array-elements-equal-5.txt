    def reductionOperations(self, A):
        A.sort()
        res = k = 0
        for i in xrange(1, len(A)):
            if A[i] > A[i - 1]:
                k += 1
            res += k
        return res