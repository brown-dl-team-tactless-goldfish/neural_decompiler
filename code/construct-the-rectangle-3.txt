int* constructRectangle(int area, int* returnSize){
    *returnSize = 2;
    int *rect = calloc(*returnSize, sizeof(int));
    for (int w = sqrt(area); w > 0; w--) {
        if ((area % w) != 0) continue;
        rect[0] = area / w;
        rect[1] = w;
        break;
    }
    return rect;
}