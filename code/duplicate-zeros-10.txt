void
shift(int *a, int i, int s) {
    while (s --> i) a[s] = a[s-1];
}

void duplicateZeros(int* arr, int arrSize){
    int i=0;
    while ( i < arrSize ) i += (arr[i]) ? 1 : (shift(arr,i+1,arrSize), 2);
}