class Solution {
public:
    int maximumSum(vector<int>& arr) {
        int a = arr[0], b = a, c = a;
        for (int i = 1; i < arr.size(); ++i) {
            b = max(a, b + arr[i]);
            a = max(arr[i], a + arr[i]);
            c = max(c, max(a, b));
        }
        return c;
    }
};