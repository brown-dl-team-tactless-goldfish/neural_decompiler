#include <math.h>
int pivotInteger(int n){
    //sum of consecutive integers  = n(2a+(n-1))/2 or n(a+l)/2
    int sum=(1+n)*n/2;
        int t=(int)sqrt(sum);
        if(sum==t*t){
            return t;
        }
        return -1;
}