int guessNumber(int n){
	long lo = 1, hi = n;
    while(lo <= hi){
        int mid = (lo + hi) / 2;
        if(guess(mid) == 0)
            return mid;
        else if(guess(mid) == -1)
            hi = mid - 1;
        else
            lo = mid + 1;
    }
    return 0;
}