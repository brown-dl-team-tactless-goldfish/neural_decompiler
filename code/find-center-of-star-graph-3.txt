  unordered_map<int,int> mp;
  for(int i=0;i<edges.size();i++)
  {
    mp[edges[i][0]]++;
    mp[edges[i][1]]++;
  }
        
  for(int i=1;i<=edges.size()+1;i++)
  {
    if(mp[i]==edges.size())
      return i;
  }
    return 1;
}
'''