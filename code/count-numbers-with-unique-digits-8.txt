class Solution {
public:
    int countNumbersWithUniqueDigits(int n) {
        if (n == 0) return 1; 
        int prev = 10;
        int cur = 9;
        for (int i = 2, c = 9; i <= min(10, n); ++i, --c) {
            cur *= c;
            prev += cur;
        }
        return prev;
    }
};