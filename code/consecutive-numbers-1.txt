select distinct Num from (
select Id,
       Num,
       @count := IF(@prev = Num, @count+1, 1) as n,
       @prev := Num
       from Logs, (select @count := 0, @prev := NULL) as i
) as t  where t.n >= 3;