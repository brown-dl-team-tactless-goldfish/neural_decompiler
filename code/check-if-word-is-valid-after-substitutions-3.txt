bool isValid (char *s) {
	int  i, top = -1;
	char stack[20001];

	for (i = 0; s[i]; i++) {
		if (s[i] == 'a') {
			stack[++top] = s[i];
		} else if (top >= 0 && s[i] == stack[top] + 1) {
			if (s[i] == 'c') top--;
			else stack[top] = s[i];
		} else {
			return false;
		}
	}

	return top == -1;
}