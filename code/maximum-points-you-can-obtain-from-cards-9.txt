int score=0,i,j;
for(i=0;i<k;i++)
{
    score+=cardPoints[i];
}
i--;
int cur=score;
for(j=cardPointsSize-1;j>=cardPointsSize-k;j--)
{
    cur=cur-cardPoints[i--]+cardPoints[j];
    if(cur>score)score=cur;
}
if(cur>score)score=cur;
return score;