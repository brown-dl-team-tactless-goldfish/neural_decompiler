
int countMatches(char *** items, int itemsSize, int* itemsColSize, char * ruleKey, char * ruleValue){

    int count = 0;
    int col = 0;
    col = (*ruleKey == 't')? 0:(*ruleKey == 'c')? 1:2;

    for(int i = 0;i<itemsSize;i++)
    {
        if(strcmp(items[i][col] ,ruleValue ) == 0)
            count++;
    }
    return count;
}