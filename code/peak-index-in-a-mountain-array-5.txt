

int peakIndexInMountainArray(int* A, int ASize){

    if(ASize == 1)
    {
        return 0;
    }
    int i=0;
    int lastnum = A[0];
    for(i=1;i<ASize;i++)
    {
        
        if(A[i]<lastnum)
        {
            return i-1;
        }
        lastnum = A[i];
    }
    return 0;
}

