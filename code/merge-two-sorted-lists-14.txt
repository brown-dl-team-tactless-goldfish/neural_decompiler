while(l1||l2)
{
if(l1==NULL)
{
    create(&head3,l2->val);
    
    l2=l2->next;
}
else if(l2==NULL)
{
    create(&head3,l1->val);
    l1=l1->next;
}
 else
 {
     if(l1->val>l2->val)
     {
         create(&head3,l2->val);
         l2=l2->next;
     }
     else
     {
create(&head3,l1->val);
         l1=l1->next;
         
     }
 }


}
return head3;