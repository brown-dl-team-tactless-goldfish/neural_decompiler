    int maximumTop(vector<int>& nums, int k) {
        if(nums.size() == 1 && k % 2 == 1) return -1;
        int res = -1;
        for(auto x:nums){
            if(k==0 || k>1) res = max(res, x);
            k--;
        }
        return res;
    }