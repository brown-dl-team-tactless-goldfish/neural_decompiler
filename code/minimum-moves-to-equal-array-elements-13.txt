int minMoves(vector<int>& nums) {
    long long int i,sum=0,x,n;
    for(i=0;i<nums.size();i++)
    {
        sum=sum+nums[i];
    }
    x=*min_element(nums.begin(),nums.end());
    n=nums.size();
    return sum-(x*n);
}