int maxPower(char * s){
    int highest = 0;
    const char * start = s;
    
    while(*(s++) != '\0'){
        if(*(s - 1) != *s){
            if(highest < (s - start))
                highest = (s - start);
            
            start = s;
        }
    }
    
    return highest;
}