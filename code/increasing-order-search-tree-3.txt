TreeNode* increasingBST(TreeNode* root) {
	auto p = &root;
	auto n = root;
	while (*p) {
		while ((*p)->left) {
			n = (*p)->left;
			(*p)->left = n->right;
			n->right = *p;
			*p = n;
		}
		p = &(*p)->right;
	}
	return root;
}