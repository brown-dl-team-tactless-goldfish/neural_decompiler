int heightChecker(int* heights, int heightsSize){
    int m[101] = { 0 }, n = 0, *h = heights, sz = heightsSize, nz = sz;
    while (nz-- && ++m[h[nz]]);
    for (int i = 0, c = 0 ; i < sz ; m[c++] ? --m[--c], n += c != h[i++] : 0);
    return n;
}