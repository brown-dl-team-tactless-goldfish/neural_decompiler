int minImpossibleOR(int* nums, int numsSize){
    int mask = 0, ans = 1;
    for(int i = 0; i < numsSize; i++)
        if(__builtin_popcount(nums[i]) == 1)
            mask |= nums[i];
    mask = ~mask;
	
    while(!(mask & ans))
        ans <<= 1;
    
    return ans;
}