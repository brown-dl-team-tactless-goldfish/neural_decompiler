WITH cte AS (
SELECT a.PID 
FROM Insurance a
WHERE a.TIV_2015  IN (SELECT b.TIV_2015 FROM Insurance b WHERE a.PID != b.PID) 
AND (a.LAT, a.LON) NOT IN (SELECT LAT, LON FROM Insurance c WHERE a.PID != c.PID)
)

SELECT SUM(TIV_2016) AS TIV_2016 FROM Insurance 
WHERE PID IN (SELECT PID FROM cte)