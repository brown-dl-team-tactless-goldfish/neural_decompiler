#define max(a, b) a > b ? a : b
int minimizeArrayValue(int* nums, int numsSize) {
    long long result = 0, preSum = 0;
    for (int i = 0; i < numsSize; i++) 
    {
        preSum += nums[i];
        result = max(result, (preSum + i) / (i + 1));
    }
    return result;
}
