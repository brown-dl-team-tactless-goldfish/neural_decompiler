Runtime: 19 ms, faster than 5.62% of C online submissions for Odd Even Linked List.
Memory Usage: 6.9 MB

/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */

typedef struct ListNode node;

struct ListNode* oddEvenList(struct ListNode* head){
    node *oddHead = head;
    if (head==NULL)
        return NULL;
    if(head -> next == NULL)
        return head;
    node *temp;
     node *fixEvenHead = oddHead->next;
    node *evenHead = oddHead->next;
    printf("FixEvenHead is at %d", evenHead->val);
    int count=1;
    while(evenHead != NULL  )
    {
        if(evenHead->next == NULL)
        {
            break;
        }
        printf("\nEvenHead is at %d", evenHead->val);
        printf("\nOddHead is at %d", oddHead->val);
        count++;
        oddHead ->next = evenHead->next;
        evenHead->next =  oddHead->next->next;
        oddHead =  oddHead ->next;
        evenHead = evenHead->next;
    }
    oddHead->next=fixEvenHead;
        
    return head;
}
