int percentageLetter(char * s, char letter){
    int len = strlen(s);
    int k = 0;
    for(int i = 0; i < len; i++){
        if(s[i] == letter){
            k++;
        }
    }
    int per = (100  * k) / len;
    return per;
}