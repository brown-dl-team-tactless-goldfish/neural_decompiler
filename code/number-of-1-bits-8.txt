int hammingWeight(uint32_t n) {
    int count = 0;

    uint32_t mask = 1;  // 00000000000000000000000000000001

    for(int i=0;i<32;i++){
        count = (mask == ((n & (mask << i)) >> i)) ? count+1 : count;
    }
    return count;
}