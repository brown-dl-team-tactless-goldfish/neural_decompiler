    sort(arr.begin(), arr.end());
    
    int N = arr.size();
    
    if (N < 20)
        return 0.0;
    
    int remove = 0.05 * N; double ans = 0;
    
    // cout << "N: " << N << endl;
    // cout << "remove: " << remove << endl;
    
    // for (int i = remove; i < N-remove; i++)
    // {
    //     ans += arr[i];
    // }
    
    return accumulate(arr.begin()+remove, arr.end()-remove, 0.0) / (N-2*remove);
}