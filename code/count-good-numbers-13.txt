public class Solution {
    const int mod = 1_000_000_007;
    
    public int CountGoodNumbers(long n) =>
        (int)(BigInteger.ModPow(4, n / 2, mod) * BigInteger.ModPow(5, n - n / 2, mod) % mod);
}