import math
class Solution:
    def twoEggDrop(self, k: int) -> int:        
        a = 1
        b = 1
        c = -k*2
        # calculate the discriminant
        d = (b**2) - (4*a*c)
        sol2 = (-b+sqrt(d))/(2*a)
        return math.ceil(sol2)      