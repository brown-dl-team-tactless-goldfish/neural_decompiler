bool areOccurrencesEqual(char * s){
    int hash[26]={0};
    int count;
    while(*s) {
        hash[*s-'a']++;
        s++;
    }
    count = hash[s[-1]-'a'];
    for(int i=0; i<26;i++){
        if(hash[i]!=0 && hash[i]!=count)
            return false;
    }
    return true;
}