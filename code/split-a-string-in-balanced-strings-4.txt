public int BalancedStringSplit(string s) {
        int cnt = 0, ans = 0;
        foreach (var ch in s)
        {
            cnt = cnt + (ch == 'L' ? 1 : -1);
            ans = ans + (cnt == 0 ? 1 : 0);
        }
        
        return ans;
    }