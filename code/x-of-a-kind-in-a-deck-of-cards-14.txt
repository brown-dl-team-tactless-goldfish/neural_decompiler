class Solution {
public:
    bool hasGroupsSizeX(vector<int>& deck) {
        unordered_map<int, int> mp;
        for(int x: deck) mp[x]++;
        int g = mp[deck[0]];
        for(auto x: mp) g = gcd(g, x.second);
        if(g > 1) return true;
        else return false;
    }
};