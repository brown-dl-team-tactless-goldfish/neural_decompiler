bool isCircularSentence(string sntnc) {
	char firstCh = sntnc[0];
	stringstream ss(sntnc);
	string prev, last;
	while (ss >> last) {
		if (!prev.empty())
			if (prev.back() != last.front())
				return false;
		prev = last;
	}
	return last.back() == firstCh;
}