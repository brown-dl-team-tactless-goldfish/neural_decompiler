class Solution {
public:
    int maximumScore(int a, int b, int c) 
    {
        if (a < b)
            return maximumScore(b, a, c);
        if (b < c)
            return maximumScore(a, c, b);
        if (b == 0) 
            return 0;
        else 
            return 1 + maximumScore(a - 1, b - 1, c); 
    }
};
// This version has been taken from someone else's code