//1234
int i=0;
int* count_num=(int*)malloc(sizeof(int)*strlen(num));
for (i=0;i<strlen(num);i++) count_num[i]=0;
int flag=1;
for (i=0;i<strlen(num);i++)
{   
    if( (num[i]-'0')<strlen(num))
    {
        count_num[num[i]-'0']++;
    }
    

}
for (i=0;i<strlen(num);i++)
{  
    if(num[i]-'0'!=count_num[i])
    {
        flag=0;
        break;
    }
    
}
return flag;