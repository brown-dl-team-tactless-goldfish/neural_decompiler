#include <string.h>
char * restoreString(char * s, int* indices, int indicesSize){
    char* res=(char* )malloc(strlen(s)+1);
    int i = 0;
    while(i<indicesSize){
        *(res+i)=*(s+*(indices+i));
        i++;
    }
    *(res+i)='\0';
    return res;
}