int singleNumber(int* nums, int numsSize){
// we use XOR operation 
// x^0=0      x^x=0(ie if both the nos are same)
    int x=0;
    if(numsSize==0)
        return nums[0];
    for(int i=0;i<numsSize;i++)
        x^=nums[i];
    return x;
        

}