int longestPalindrome(char * s){
    bool map[128];
    memset(map, 0, sizeof(map));
    int cnt = 0;
    int ans = 0;
    for (int idx = 0; s[idx] != '\0'; ++idx) {
        cnt += 1;
        ans += map[s[idx]];
        map[s[idx]] = !map[s[idx]];
    }
    return ans * 2 + (ans * 2 != cnt);
}