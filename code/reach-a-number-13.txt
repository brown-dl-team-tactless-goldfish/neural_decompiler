public class Solution 
{
    public int ReachNumber(int target) 
    {
        target = Math.Abs(target);
        int res = 0, sum = 0;
        while (sum < target || (sum - target) % 2 == 1) sum += ++res;
        return res;
    }
}