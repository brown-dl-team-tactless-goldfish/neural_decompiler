class Solution {
public:
   string kthLargestNumber(vector<string>& nums, int k) {
    sort(nums.begin(), nums.end(), [](string &a, string &b)->bool {return a.size() < b.size() ? true : a.size() == b.size() ? a < b : false;});
    return nums[nums.size() - k];
  }
};