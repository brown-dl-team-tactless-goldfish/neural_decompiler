vector<bool> prefixesDivBy5(vector<int>& A) {
    
    int n = A.size();
    vector<bool>v;
    
    int val = 0;
    for (int i = 0; i < n; i++) {
        val = (2*val + A[i])%5;
        v.push_back(!val);
    }
    
    return v;
    
}