   public:
   map<int,int> m;
NumberContainers() {
    }

void change(int index, int number) {
    m[index]=number;
}

int find(int number) {
    for(auto it=m.begin();it!=m.end();it++){
        if(it->second==number)return it->first;
    }
    return -1;
}