char * breakPalindrome(char * s) {
    short n = strlen(s);

    if(n == 1) return "";
	
	for(char i = 0; i < n / 2; i++) {
        if(s[i] != 'a') {
            s[i] = 'a';
            return s;
        }   
    }
    s[n - 1] = 'b';
    return s;
}