ListNode* _head;
Solution(ListNode* head) : _head(head) { }
int getRandom() {
    auto v = 0, cnt = 0;
    for (auto n = _head; n != nullptr; n = n->next)
        if (rand() % ++cnt == 0)
            v = n->val;
    return v;
}