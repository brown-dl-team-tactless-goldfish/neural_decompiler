int* buildArray(int* nums, int numsSize, int* returnSize){
    int *r, i;
    *returnSize = numsSize;
    
    r = malloc(sizeof(int) * numsSize);
    
    for(i=0; i<numsSize; i++) {
        r[i] = nums[nums[i]];
    }
    
    return r;
}