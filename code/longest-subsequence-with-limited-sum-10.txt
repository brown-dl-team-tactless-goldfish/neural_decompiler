		sort(nums.begin(),nums.end());
        vector<int>res;
        for(auto it:queries){
            int target=it;
            int size=0;
            for(int i=0;i<nums.size();++i){
                if(target-nums[i]<0){
                    break;
                }
                else{
                    size++;
                    target-=nums[i];
                }
            }
             res.push_back(size);
        }
        return res;