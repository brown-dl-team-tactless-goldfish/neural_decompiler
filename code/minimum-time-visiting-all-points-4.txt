
int minTimeToVisitAllPoints(int** points, int pointsSize, int* pointsColSize){
    int i, sec=0, l, r;
    for(i=0;i<pointsSize-1;i++){
        l= abs(points[i][0]-points[i+1][0]);
        r= abs(points[i][1]-points[i+1][1]);
        sec+= l<r?r:l;  
    }
    return sec;
}