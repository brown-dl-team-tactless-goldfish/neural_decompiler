char * removeStars(char * s){
    int i, j, sl = strlen(s);
    for (i = 0, j = 0; i < sl; ++i) {
        if (s[i] == '*') {
            --j;
        } else {
           s[j++] = s[i];
        }
    }
    s[j] = '\0';
    return s;
}