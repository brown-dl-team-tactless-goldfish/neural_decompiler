int peakIndexInMountainArray(int* arr, int arrSize){
   int i;
   int max=arr[0];
   int k=0;
   for(i=0;i<arrSize;i++)
   {
     if(max<arr[i])
     {
         max=arr[i];
         k=i;
     }
   }
   return k;
}