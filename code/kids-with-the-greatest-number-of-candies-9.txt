 bool* result = malloc(candiesSize*sizeof(int));
* returnSize = candiesSize;
    
for (int i=0;i<candiesSize;i++)
{
    result[i]=true;
    for(int j=0;j<candiesSize;j++)
    {
        if(candies[i]+extraCandies<candies[j])
        {
            result[i]=false;
            break;
        }
    }
}
return result;