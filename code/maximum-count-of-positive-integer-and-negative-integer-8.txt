class Solution {
public:
    int maximumCount(vector<int>& nums) {
        int i=0,j=0;
        for(;i<size(nums);++i)
            if(nums[i]>=0)break;
        j=i;
        while(i<size(nums) && nums[i]==0)++i;
        return max(j,(int)size(nums)-i);
    }
};