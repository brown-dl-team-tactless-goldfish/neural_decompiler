int guessNumber(int n){
	long left = 1, right = n, mid;
    
    while(left <= right) {
        mid = left + (right - left) / 2;
        if(guess(mid) == -1) 
            right = mid - 1;
        else if (guess(mid) == 1) 
            left = mid + 1;
        else return mid;
    }
    return -1;
}