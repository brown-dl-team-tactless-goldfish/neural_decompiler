int alternateDigitSum(int n) {
	register int len_n = floor(log10(abs(n))) + 1, sum = 0;
	bool flag = false;
	if(len_n % 2 != 0)
		flag = true;

	while(n > 0) {
		if(!flag)
			sum += (n % 10) * -1;
		else
			sum += (n % 10);
		
		flag = !flag;
		n /= 10;
	}
	return sum;
}