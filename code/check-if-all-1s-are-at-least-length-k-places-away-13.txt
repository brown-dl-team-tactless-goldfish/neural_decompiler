public bool KLengthApart(int[] nums, int k) {
	var ones = nums
		.Select((x, i) => (x, i))
		.Where(x => x.x == 1)
		.Select(x => x.i)
		.ToArray();

	for (var i = 1; i < ones.Length; i++)
		if (ones[i] - ones[i - 1] <= k)
			return false;

	return true;
}