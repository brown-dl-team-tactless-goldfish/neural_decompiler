    int n = nums.size(), i, maxi = 0;
    
    if(n < 2)
    {
        return 0;
    }
    
    sort(nums.begin(), nums.end());
    
    for(i=n-1; i>=1; i--)
    {
        int d = nums[i] - nums[i-1];
        
        maxi = max(d,maxi);
    }
    
    return maxi;
    
}