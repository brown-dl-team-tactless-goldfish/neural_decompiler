public class Solution {
    public int DeleteGreatestValue(int[][] grid) {
        foreach (var line in grid)
            Array.Sort(line);

        int result = 0;

        for (int c = 0; c < grid[0].Length; ++c) 
            result += grid.Max(line => line[c]);

        return result;
    }
}