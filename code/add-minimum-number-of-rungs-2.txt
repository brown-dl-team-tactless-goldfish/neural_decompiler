public class Solution {
    public int AddRungs(int[] rungs, int dist) {
        int extraRungsRequired=(rungs[0]-1)/dist;
        for(int i=1;i<rungs.Length;i++)
            extraRungsRequired+=(rungs[i]-rungs[i-1]-1)/dist;
        return extraRungsRequired;
        
    }
}