int sums(int nums, int count)
{
    int i, ret =1;
    for(i = 0; i < count; i++)
        ret = ret * nums;
    return ret;
}
int titleToNumber(char * s){
    int fact_len = strlen(s) - 1;
    int tmp = 0, sum = 0;
    while(*s){       
        sum = sum + (*s - 'A' + 1) * sums(26, fact_len);
        *(s++);
        fact_len--;
    }
    return sum;
}