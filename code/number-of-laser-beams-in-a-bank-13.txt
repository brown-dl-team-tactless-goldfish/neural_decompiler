    public int NumberOfBeams(string[] bank) {
        
        var countPerRow = bank.Select(x => x.Count(c => c == '1'))
                         .Where(a => a > 0);
        
        return countPerRow
            .Skip(1)
            .Zip(countPerRow, (prev, curr) => prev * curr)
            .Sum();

    }