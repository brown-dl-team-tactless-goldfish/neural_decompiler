# Write your MySQL query statement below
SELECT T.num as ConsecutiveNums from (SELECT DISTINCT A.num From Logs A LEFT JOIN 
Logs B on A.id = B.id - 1
LEFT JOIN Logs C on C.id = B.id + 1 WHERE A.num = B.num AND B.num = C.num) T;