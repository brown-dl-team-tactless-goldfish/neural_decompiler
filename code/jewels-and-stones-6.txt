int numJewelsInStones(char * J, char * S){
int len1 = strlen(S);
    int len2 = strlen(J);
    int i,j,count;
    count=0;
        for(i=0;i<len1;i++){
            for(j=0;j<len2;j++){
            if(S[i]==J[j]){
                count++;
            }
        }
    }
    return count;
}