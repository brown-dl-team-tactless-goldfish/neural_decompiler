char* defangIPaddr(char* address) {
    char* ans = calloc(22, 1);
    for (unsigned int i = 0; i < strlen(address); i++) {
        if (address[i] == '.') {
            strncat(ans, "[.]", 3);
        } else {
            strncat(ans, &address[i], 1);
        }
    }
    return ans;
}