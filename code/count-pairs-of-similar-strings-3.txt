class Solution {
public:
    int similarPairs(vector<string>& W) {
        unordered_map<int, int> mp;
        int res = 0;
        for(auto & s : W) {
            int v = 0;
            for(auto c : s) v |= 1 << (c-'a');
            res += mp[v]++;
        }
        return res;
    }
};