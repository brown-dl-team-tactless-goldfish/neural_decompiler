string longestPrefix(string s) {
    
      int n = s.length() ;
      vector<int> arr(n,0) ;
      int len=0 , i=1;
      while(i<n)
          if(s[i]==s[len]) arr[i++]=(++len);
          else len ? len=arr[len-1] : arr[i++]=0;
    return s.substr(0,arr[n-1]) ;
	
}