
int addDigits(int num)
{
    int sum = 0;
    int r;
    int d;
    while (num > 0)
    {
        r = num % 10;
        sum = sum + r;
        num = num / 10;
    }
    if (sum > 9)
    {
        d = addDigits(sum);
    }
    else
    {
        return sum;
    }

    return d;
}