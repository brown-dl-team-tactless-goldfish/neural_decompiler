if (root==NULL)
    return true;
else if(root->left==NULL && root->right==NULL)
    return true;
else if(root->left!=NULL && root->right!=NULL)
{ struct TreeNode*p=root->left;
 struct TreeNode*q=root->right;
 return mirror(p,q);
}
   