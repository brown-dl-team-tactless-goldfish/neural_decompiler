int numWaterBottles(int numBottles, int numExchange)
{
 int ans = numBottles, full = numBottles, empty = 0;
 while(full > 0)
 {
  full = numBottles/numExchange;
  empty = numBottles%numExchange;
  ans += full;
  numBottles = full + empty; //for next round
 } 
 return ans;
}