#define max(a,b) a>b?a:b
int rob(int *num, int numSize) {
    int p2=0,p1=0;
    
    for(int i=0;i<numSize;i++){
        int tmp=p1;
        p1=max(p1,p2+num[i]);
        p2=tmp;
    }
    return p1;
}
