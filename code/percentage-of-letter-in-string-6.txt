int percentageLetter(char * s, char letter){
    int counterOfLetter = 0, percentage = 0;
    
    for (int i = 0; i < strlen(s); i++) {
        if (s[i] == letter) counterOfLetter++;
    }
    
    percentage = (counterOfLetter * 100) / strlen(s);
    return percentage;
}