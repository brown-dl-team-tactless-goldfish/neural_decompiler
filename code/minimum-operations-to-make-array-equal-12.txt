public class Solution {
    public int MinOperations(int n) {
             
        int avg = n, cnt = 0, max = 2 * n - 1, num = 1;

        while(num <= max)
        {
            cnt += Math.Abs(num - avg);
            num += 2;
        }
        
        return cnt / 2;
    }
}