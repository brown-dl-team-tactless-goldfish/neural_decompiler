public class Solution {
    public int MinMoves(int[] nums) {
            long sum = 0;

            foreach (var num in nums)
                sum += num;

            sum -= nums.Min() * nums.Length;

            return (int) sum;
    }
}