int minSteps(int n) {
    ssize_t m = 1, c = 0, x = 0;
    
    while (m < n) {
        if (m != c && !(n % m))
            c = m;
        else
            m += c;

        ++x;
    }
    
    return x;
}