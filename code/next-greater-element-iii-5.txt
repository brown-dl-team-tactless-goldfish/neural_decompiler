public:
    int nextGreaterElement(int n) {
    string number = to_string(n);
    next_permutation(number.begin(),number.end());
     if(number.length() >= 10 && number > "2147483647")    return -1;
     int ans = 0;
     for(char  c : number)
         ans = ans*10 + c-'0';

    return ans<=n?-1:ans;    
    }
};