select 
    u.user_id buyer_id, 
    join_date,
    ifnull(t.c, 0) orders_in_2019
from 
	Users u
	left join (
		select buyer_id, count(*) c from Orders
		where year(order_date) = 2019
		group by buyer_id
	) t
	on u.user_id=t.buyer_id