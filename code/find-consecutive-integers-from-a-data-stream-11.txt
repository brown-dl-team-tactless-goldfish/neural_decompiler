class DataStream {
public:
    DataStream(int value, int k): val(value), last(k) {}
    
    bool consec(int num) {
        num==val? ++curr: curr=0;
        return curr<last? false: true;
    }
    
private:
    const int val, last;
    int curr=0;
};