with c as
(
with b as 
(
with a
as
(
select  salary ,dense_rank() over(order by salary desc) as rn from employee 

)
select case when rn= 2 then salary else null end as secondhighestsalary from a 

)
select secondhighestsalary , dense_rank() over(order by secondhighestsalary desc) as rnx from b 
)
select distinct secondhighestsalary from c 
where rnx = 1