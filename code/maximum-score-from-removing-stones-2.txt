int max (int a, int b) {
    return a > b ? a : b;
}

int min (int a, int b) {
    return a < b ? a : b;
}

int maximumScore(int a, int b, int c){
    int sum = a + b + c;
    int mx = max (a, max (b, c));

    return min (sum - mx, sum / 2);
}