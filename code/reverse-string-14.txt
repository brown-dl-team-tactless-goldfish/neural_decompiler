

void reverseString(char* s, int sSize){
    char temp;
    for(int i=0;i<sSize/2;i++)          //iterate till half
    {
        temp=s[i];                      //swap the ith and (sSize-1-i)th element 
        s[i]=s[sSize-1-i];
        s[sSize-1-i]=temp;
    }
}