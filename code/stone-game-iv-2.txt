bool winnerSquareGame(int n){
    int i,k, *dp = (int*)calloc(sizeof(int), (n+1));
    for(i=1; i<=n; ++i)
        for(k=1; (k*k)<=i; k++)
            if(dp[i - (k*k)] == 0){
                dp[i] = 1;
                break;
            }
    return dp[--i];
}