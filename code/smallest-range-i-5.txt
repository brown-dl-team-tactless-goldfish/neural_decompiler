int smallestRangeI(int* A, int ASize, int K) {
    int an;
    int mn = INT_MAX;
    int mx = INT_MIN;
    
    for (int i = 0; i < ASize; ++i) {
        mn = mn < A[i] ? mn : A[i];
        mx = mx > A[i] ? mx : A[i];
    }

    an = mx - mn - 2*K;
    return an > 0 ? an : 0;    
}