class Solution {
public:
    int getMaximumConsecutive(vector<int>& coins) {
        int ans = 1, req = 1;
        sort(coins.begin(), coins.end());
        
        for(int i=0;i<coins.size();i++){
            int coin = coins[i];
            if(coin > req){
                break;
            }
            req = coin + req;   
        }
        
        return req;
    }
};