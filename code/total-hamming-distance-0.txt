int totalHammingDistance(int* nums, int numsSize){
    int ans = 0;
    for(int i = 0; i < 31; i++){
        int one = 0;
        for(int j = 0; j < numsSize; j++){
            if(nums[j] & (1 << i))
                one++;
        }
        ans += one * (numsSize - one);
    }
    return ans;
}