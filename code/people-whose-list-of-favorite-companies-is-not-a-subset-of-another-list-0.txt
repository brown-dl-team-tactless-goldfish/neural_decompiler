class Solution:
    def peopleIndexes(self, favoriteCompanies: List[List[str]]) -> List[int]:
        companies_set = []
        for companies in favoriteCompanies:
            tmpSet = set(companies)
            # print ("tmpSet = ", tmpSet)
            companies_set.append(tmpSet)
            # companies_set.
            # print ("1 companies = ", companies)
            # companies_set.append(tmpSet)
            
        print ("companies_set = ", companies_set)
        
        res = []
        n = len(companies_set)
        print ("n = ", n)
        for i in range(n):
            # print ("i = ", i)

            set_i = companies_set[i]
            flag_need_add = True
            for j in range(n):
                # print ("i = ", i, " j = ", j)
                flag_need_add = True
                if (j != i):
                    set_j = companies_set[j]
                    if (set_i.issubset(set_j)):
                        flag_need_add = False
                        break
                    
                    # if (len(set_j) <= len(set_i)):
                    #     print ("set_i = ", set_i)
                    #     print ("set_j = ", set_j)
                    #     if (set_j.issubset(set_i)):
                    #         flag_need_add = False
                    #         break
                    # else:
                    #     flag_need_add = False
                else:
                    # flag_need_add = False
                    pass
                        
            if (flag_need_add == True):
                res.append(i)
                
        print ("end res = ", res)
        
        return res