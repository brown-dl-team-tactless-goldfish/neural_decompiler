class Solution {
public:
    int getLastMoment(int n, vector<int>& left, vector<int>& right) {
        left.emplace_back(0), right.emplace_back(n);
        return std::max(*max_element(left.begin(), left.end()), n-*min_element(right.begin(), right.end()));
        
    }
};