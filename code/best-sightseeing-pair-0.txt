int max(int num1, int num2) {
  return (num1 > num2 ) ? num1 : num2;
}

int maxScoreSightseeingPair(int* values, int valuesSize){
  int temp = 0, res = 0;
  for (int i = 0; i < valuesSize; ++i) {
    res = max(res, temp+values[i]);
    temp = max(temp, values[i])-1;
  }
  return res;
}