class Solution {
public:
    int maxScore(vector<int>& v) {
        sort(v.begin(),v.end());
        reverse(v.begin(),v.end());
        long long a=0;
        long long c=0;
        for(int i=0;i<v.size();i++){
            a+=v[i];
            if(a>0)c++;
        }
        return c;
    }
};