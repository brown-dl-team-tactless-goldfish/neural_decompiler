bool* prefixesDivBy5(int* A, int ASize, int* returnSize){
    bool *b = malloc(sizeof(bool [ASize]));
    for (int i = -1, n = 0 ; ++i < ASize ; b[i] = !(n = ((n << 1) | A[i]) % 5));
    return *returnSize = ASize, b;
}