# Write your MySQL query statement below

select contest_id, round(100*c/c1,2) as percentage from ( select count(distinct user_id) as c1 from users) as t1,
(select contest_id,user_id, count(user_id) as c from users inner join register using(user_id) group by contest_id ) as t2 order by percentage desc , contest_id asc
