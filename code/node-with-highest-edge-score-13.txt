class Solution {
public:
    int edgeScore(vector<int>& edges) {
        unsigned int n = size(edges), score[100000] = {};
        for (int i = 0; i < n; i++)
            score[edges[i]] += i;
        return max_element(begin(score), begin(score)+n) - begin(score);
    }
};