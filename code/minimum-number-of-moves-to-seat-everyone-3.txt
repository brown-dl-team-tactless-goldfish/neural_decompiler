//先把椅子跟學生由小到大排好
insert_sort(seats, seatsSize);
insert_sort(students, studentsSize);

//學生號碼跟最近的椅子相減，就是要移動的步數
for(int i=0; i< studentsSize; i++)
{
    if(students[i]>=seats[i])
        Move+=students[i]-seats[i];
    else
        Move+=seats[i]-students[i];
}

//回傳步數
return Move;