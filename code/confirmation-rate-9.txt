
select s.user_id, 
       ISNULL(round(sum(case when action = 'confirmed' then 1 else 0 end) * 1.0 / NULLIF(count(*),0) ,2), 0.00) as confirmation_rate
       from Signups s 
       left join Confirmations c on c.user_id  = s.user_id 
group by s.user_id 