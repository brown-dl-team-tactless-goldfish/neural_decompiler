int rand10() 
{
    int random = 0;
    for(int i = 0; i < 7; i++)
        random += rand7();
        
	//random can be max 49 so if we do modulo 10 and add 1 we can get any number between 1 till 10
    return random % 10 + 1;
}