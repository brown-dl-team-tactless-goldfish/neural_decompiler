class Solution(object):
    def guessNumber(self, n):
        lower, upper = 0, n
        num = random.randint(lower, upper)
        while guess(num) is not 0:
            if guess(num) is 1: lower = num
            else: upper = num
            num = random.randint(lower, upper)
        return num