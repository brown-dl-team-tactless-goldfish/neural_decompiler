int minDepth(struct TreeNode* root){
    if(root==NULL)
        return 0; 
    
    int left = minDepth(root->left) + 1; 
    int right = minDepth(root->right) + 1; 
    
    bool flag = (right < left || left < 2) && right>1;
    
    return flag ? right : left; 

}