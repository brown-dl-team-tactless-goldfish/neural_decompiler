int singleNonDuplicate(int* nums, int numsSize){
int i;
for (i=1; i<numsSize; i+=2) // Check each pair, skipping through by twos
    if (nums[i]!=nums[i-1]) return nums[i-1];
return nums[numsSize-1];  // If we finish without finding it, it's the last element
}