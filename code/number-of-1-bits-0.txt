int hammingWeight(uint32_t n) {
    /* Takes an unsigned integer and
    returns the number of '1' bits it has. */
    int c = 0;
    while(n){
        n &= (n - 1);
        c++;
    }
    return c;
}