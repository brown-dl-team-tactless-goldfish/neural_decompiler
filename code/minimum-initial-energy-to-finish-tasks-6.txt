    int minimumEffort(vector<vector<int>>& tasks) {
        sort(tasks.begin(), tasks.end(), [](auto& v1, auto& v2){return v1[0] - v1[1] > v2[0] - v2[1];});
        int init = 0;
        for(auto& v:tasks) init = max(init+v[0], v[1]);
        return init;
    }