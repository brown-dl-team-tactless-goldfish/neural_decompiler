vector<string> simplifiedFractions(int n) {
    vector<string> res;
    for (int div = 1; div < n; ++div)
        for (int den = div + 1; den <= n; ++den)
            if (__gcd(den, div) == 1)
                res.push_back(to_string(div) + "/" + to_string(den));
    return res;
}