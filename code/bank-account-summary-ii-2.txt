# Write your MySQL query statement below
select  name,s as balance from users inner join (select account,s from (select account,sum(amount) as s from transactions group by account) as v where s>10000) as c on c.account=users.account;