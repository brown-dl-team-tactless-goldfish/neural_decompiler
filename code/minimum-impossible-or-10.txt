class Solution {
public:
    int minImpossibleOR(vector<int>& nums) {
        unordered_set<int> s(nums.begin(), nums.end());
        int n = 0;
        for(int i=0; i<32; i++) {
            n = (1<<i);
            if(s.count(n)) continue;
            break;
        }
        return n;
    }
};