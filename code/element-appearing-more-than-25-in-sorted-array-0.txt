int findSpecialInteger(int* arr, int arrSize){
    int t = arrSize/4 + 1 ;
    int cn = 1;
    for(int i = 1 ; i < arrSize ; i++){
        if(arr[i] == arr[i-1])
            cn++ ;
        else{
            if(cn >= t)
                return arr[i-1] ;
            cn  = 1 ;
        }
    }
    return arr[arrSize - 1] ;
}