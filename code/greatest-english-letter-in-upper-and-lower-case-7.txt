    if(s[i]>='a' && s[i]<='z' && appears(s,s[i]-32) && tmp == 0){
        tmp = 1;
        max[0] = s[i]-32;
    }
     if(s[i]>='A' && s[i]<='Z' && appears(s,s[i]+32) && tmp == 1){
        if (max[0] < s[i])
            max[0] = s[i];
    }
        
    if(s[i]>='a' && s[i]<='z' && appears(s,s[i]-32) && tmp == 1){
        if (max[0] < s[i]-32)
            max[0] = s[i]-32;
    }   
    i++;
}
if (!tmp)
    strcpy(max,"");
return max;