string removeDuplicates(string s, int k, string res = "", vector<pair<char, int>> v = {}) {
	for (char c : s)
		if (v.empty() || v.back().first != c) v.push_back({c, 1});
		else if (++v.back().second == k) v.pop_back();
	for (auto &p : v) res.append(p.second, p.first);
	return res;
}