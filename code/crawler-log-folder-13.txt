class Solution {
public:
    int minOperations(vector<string>& logs) {
        int c = 0;
        for(auto &x:logs){
            if(x == "../") c = max(0,c-1);
            else if(x == "./") continue;
            else c++;
        }
        return c;
    }
};