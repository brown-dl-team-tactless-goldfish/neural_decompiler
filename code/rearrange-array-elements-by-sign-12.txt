    vector<int> rearrangeArray(vector<int>& nums) {
        vector<int> pos, nag, res;
        for(auto x:nums) x>0 ? pos.push_back(x) : nag.push_back(x);
        for(int i=0;i<pos.size();i++)
            res.push_back(pos[i]), res.push_back(nag[i]);
        return res;
    }