class Solution {
public:
    int removePalindromeSub(string s) {
        bool ok=true;
        int n=s.size();
        for(int i=0;i<n/2;++i)
        {
            if(s[i]!=s[n-1-i])ok=false;
        }
        return ok?1:2;
    }
};