public class Solution 
{
    public int GetLastMoment(int n, int[] left, int[] right) 
    {
        int result = 0;
        foreach(var item in left)
            result = Math.Max(result, item);
         
        foreach(var item in right)
            result = Math.Max(result, n-item);
        return result;
    }
}