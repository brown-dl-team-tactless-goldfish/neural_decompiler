public string BaseNeg2(int N) {
	var sb = new StringBuilder();
	while (N != 0) {
		sb.Append(N & 1);
		N = -(N >> 1);
	}
	return sb.Length > 0
		? new string(sb.ToString().Reverse().ToArray())
		: "0";
}