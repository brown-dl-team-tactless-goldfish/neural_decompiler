long long numberOfWeeks(vector<int>& m) {
    long long n=m.size();
    long long sum=0;
    if(n==1)
        return 1;
    sort(m.begin(),m.end());
    for(long long i=0;i<n-1;i++)
        sum+=m[i];
    if(sum-m[n-1]>=0)
    {
        return sum+m[n-1];
    }
    return 2*sum+1;
}