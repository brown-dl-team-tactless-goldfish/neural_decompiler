int maximum69Number (int num){
    char number[10];
    sprintf(number,"%d",num);
    for(int ptr = 0; ptr < strlen(number) ;ptr++){
        //printf("%d \n",ptr);
        if(number[ptr] == '6'){
            number[ptr] = '9';
            break;
        }
    }
    return atoi(number);
}