/**
 * @param {number} n
 * @return {Function} counter
 */
var createCounter = function (n) {
  let status = false;
  let c = n;
  return function () {
    if (!status) {
      status = true;
      return c;
    } else {
      c += 1;
      return c;
    }
  };
};