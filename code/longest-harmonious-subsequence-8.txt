int findLHS(vector<int>& nums) {
    unordered_map<int,int>m;
    for(auto x:nums) m[x]++;
    int c = 0;
    for(auto x:nums){
        if(m.find(x+1)!=m.end()){
            c = max(c,m[x]+m[x+1]);
        }
    }
    return c;
}