class Solution
{
public:

    bool isValid( string S, size_t pos=string::npos )
    {
        for(; ! S.empty() && ( pos = S.find( "abc" )) != string::npos; S.erase( pos, 3 ));
        return S.empty();
    }
	
};