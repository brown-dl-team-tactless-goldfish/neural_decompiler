int numJewelsInStones(char* J, char* S) {
    int i, j;
    int jewelCount = 0;
    for(i = 0; i < strlen(S); i++)
        for(j = 0; j < strlen(J); j++)
            if(S[i] == J[j])
                jewelCount++;
    
    
    return jewelCount;
}