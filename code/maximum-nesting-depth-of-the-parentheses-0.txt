int maxDepth(char * s){
    int max=0;
    int cnt=0;
    for(int i=0; s[i]!='\0'; i++)
    {
        if(s[i]=='(')
        {
            cnt++;
        }
        else if(s[i]==')')
        {
            cnt--;
        }
        if(cnt>max)
            max=cnt;
    }
    return max;
}