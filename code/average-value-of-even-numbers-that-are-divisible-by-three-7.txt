int averageValue(int* nums, int numsSize){
    int count=0,sum=0;
    for(int i=0;i<numsSize;i++)
    {
        if(nums[i]%6==0)
        {    count++;
        sum+=nums[i];
        }
        
    }
    if(!count)
        return 0;
return sum/count;
}