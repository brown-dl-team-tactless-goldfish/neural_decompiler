int cmp(const void *a,const void *b){
	return ( *(int*)a-*(int*)b);
}
void check(int *arr,int row){
	int ans=0;
	qsort(arr,row,sizeof(int),cmp);

	for(int i=row-1;i>0;i--){
		if(arr[i-1]!=arr[i]){
			ans+=row-i;
		}
	}
return ans;
}