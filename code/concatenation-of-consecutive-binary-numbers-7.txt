class Solution {
    const int c = 1e9 + 7;
public:
    int concatenatedBinary(int n) {
        long long d = 0, r = 0, l = 1 << d;
        for(int i = 1; i <= n; i++) {
            if(i >= l) l = 1 << ++d;
            r = ((r << d) + i) % c;
        }
        return r;
    }
};