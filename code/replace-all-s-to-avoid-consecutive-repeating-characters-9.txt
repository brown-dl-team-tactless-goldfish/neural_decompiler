class Solution:
    def modifyString(self, s: str) -> str:
        cha = ['#']+list(s)+['#']
        for i,e in enumerate(cha):
            if e=='?':
                cha[i]=list({'a','b','c'}-{cha[i-1],cha[i+1]})[0]
        return ''.join(cha[1:-1])