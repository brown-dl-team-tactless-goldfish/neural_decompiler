select b.Score Score, c.Rank Rank from 
(select a.score Score, @curr:=@curr+1 Rank from
 (select distinct score from Scores order by score desc) a, (select @curr:=0) r) c 
join Scores b on c.score = b.score 
order by c.Rank