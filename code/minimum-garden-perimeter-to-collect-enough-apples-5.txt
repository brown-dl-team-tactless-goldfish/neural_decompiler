class Solution {
public:
    long long minimumPerimeter(long long need) {
        long long a = 12;
        long long i = 1;
        for (need -= a; need > 0; need -= a, i++) {
            a += (i << 3) + (((i << 1) + 1) << 2) + ((i + 1) << 3); 
        }
        return i << 3;
    }
};