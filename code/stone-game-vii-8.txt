return dp[start][end] = max(front,back);

}

int stoneGameVII(vector<int>& stones)
{
    memset(dp,-1,sizeof(dp));
    
    int sum = accumulate(stones.begin(),stones.end(),0);
    
    return help(0,stones.size()-1, sum, stones);
}