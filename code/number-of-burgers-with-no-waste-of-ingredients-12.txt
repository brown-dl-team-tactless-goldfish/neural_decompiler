vector<int> numOfBurgers(int a, int b) {
        int x=(a-2*b)/2;
        int y=(4*b-a)/2;
        vector<int> ans;
        if(4*x+2*y==a && x+y==b && x>=0 && y>=0){
            ans.push_back(x);
            ans.push_back(y);
        }
        return ans;
    }