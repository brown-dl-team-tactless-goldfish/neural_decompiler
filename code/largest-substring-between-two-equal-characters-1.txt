int maxLengthBetweenEqualCharacters(char * s){
	int i, j, ans = 0;
	
	for(i = 0; i < strlen(s) && i - strlen(s) > ans; i++)
	{
		for(j = strlen(s); j >= 0 && j > i; j--)
		{
			if(s[i] == s[j] && j - i > ans)
			{
				ans = j - i;
				break;
			}
				
		}
	}
	return ans - 1;
}