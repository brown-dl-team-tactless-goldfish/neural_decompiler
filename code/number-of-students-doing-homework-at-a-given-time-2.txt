int busyStudent(int* startTime, int startTimeSize, int* endTime, int endTimeSize, int queryTime){
    int count=0;
    for(startTimeSize--;startTimeSize>=0;startTimeSize--){
        if(queryTime>=startTime[startTimeSize]&&queryTime<=endTime[startTimeSize])
            count++;
    }
    return count;
}