bool hasCycle(struct ListNode *head) {
    for(; head!=NULL; head = head->next)
    {
        if(head->val == -100000)
            return true;
        head->val = -100000;   
    }
    return false;
}