int addDigits(int num){

    if(num<10)
        return num;

    else
    {
        int sum=0;
        for(;num>0;)
        {
            sum=sum+num%10;
            num=num/10;
        }
        num=sum;
        return addDigits(num);
    }
}