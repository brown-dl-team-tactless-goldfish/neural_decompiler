int partitionString(char * s){
    int count=1, t[26]={0};
    for(int i=0; s[i]!='\0'; i++){
        t[s[i]-'a']++;
        if(t[s[i]-'a']!=1){
            count++;
            for(int j=0; j<=25; j++)
                t[j]=0;
            i--;
        }
    }
    return count;
}