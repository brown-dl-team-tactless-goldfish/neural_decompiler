int countOdds(int low, int high){
  // First we check whether the two numbers is even:
 if(low%2 == 0 && high%2 == 0)
 {
     return (high-low)/2;
 }
//if it is odd means:
 else{ return (high-low)/2 + 1;
 }
}
