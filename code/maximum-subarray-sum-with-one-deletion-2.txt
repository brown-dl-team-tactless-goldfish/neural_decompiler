int ret = -10000, addAll = -10000, addDropOne = -10000;
for (int index = 0; index < arrSize; index++) {
    addDropOne = max(addAll, addDropOne + arr[index]);
    addAll = max(addAll + arr[index], arr[index]);
    ret = max(ret, addDropOne);
    ret = max(ret, addAll);
}
return ret;