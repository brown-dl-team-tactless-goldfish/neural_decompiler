int* sortArrayByParityII(int* A, int ASize, int* returnSize){
    *returnSize = ASize;
    int* B = malloc(ASize*sizeof(int));
    int odd = 0;
    int even = 0;
    for(int i=0; i<ASize; i++){
        if(A[i]%2) B[2*(odd++)+1] = A[i];
        else B[2*even++] = A[i];
    }
    return B;
}