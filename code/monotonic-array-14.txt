for(int i = 0; i < (ASize-1); i++)
{
    tmp = (A[i] - A[i+1]);
    
    if(tmp != 0)
    {
        tmp = tmp & BIT32;
                
        if( pre != 1 )
        {
            if( (tmp ^ pre) == BIT32 )
            {
                return false;
            }
        }
        
        pre = tmp;
    }
    
}

return true;