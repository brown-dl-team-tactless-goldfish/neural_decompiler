int count=1;
qsort(candies,candiesSize,sizeof(int),cmp);

int t=candies[0];
for(int i=1;i<candiesSize;i++)
{
    if(candies[i]!=t)
    {
        count++;
        t=candies[i];
    }
    
}

if (count<=candiesSize/2) return count;

else return candiesSize/2;