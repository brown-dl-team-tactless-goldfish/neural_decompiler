int dsum(int n)
{
    int s=0;
    while(n)
    {
        s=s+n%10;
        n/=10;
    }
    return s;
}

int countBalls(int l, int h)
{
    int a[46]={0},m=INT_MIN;
    for(int i=l;i<=h;i++)
    {
        int k=dsum(i);
        a[k]++;
        if(a[k]>m)
            m=a[k];
    }
    return m;
}