int minAddToMakeValid(char* s) {
    int slen = strlen(s), top = 0;
    if (slen == 1) return 1;
    char stack[1001];
    for (int i = 0; i < slen; i++) {
        stack[top] = s[i];
        if (top > 0 && (stack[top-1] == '(' && stack[top] == ')')) top -= 2;
        top++;
    }
    return top;
}