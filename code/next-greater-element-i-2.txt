int cntb = 0,cnta = 0;
int tgt ;
*returnSize=findNumsSize;
int *res;
res=(int*)malloc(sizeof(int)*findNumsSize);
while(cntb<=numsSize) 
{
	tgt=findNums[cnta];
	if(nums[cntb]==tgt)
	{
		cntb++;
		while(cntb<numsSize)
		{
			if(nums[cntb]>tgt){
				res[cnta]=nums[cntb];
				cnta++;
				cntb=0;
				break;
			}
			cntb++;
		}
		if(cntb==numsSize){
			res[cnta]=-1;
			cntb=0;
			cnta++;
		}
	}
	else
		cntb++;		
}
return res;