class Solution {
public:
    int addRungs(vector<int>& rungs, int dist) {
        int count = 0, prev = 0;
        
        for(int &x : rungs) {
            count += (x-prev-1)/dist;
            prev = x;
        }
        
        return count;
    }
};