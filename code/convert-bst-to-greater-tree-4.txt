int sum=0;
TreeNode* convertBST(TreeNode* root) {
    return solve(root);
}
TreeNode *solve(TreeNode *root)
{
    if(!root)
        return root;
    solve(root->right);
    root->val+=sum;
    sum=root->val;
    solve(root->left);
    return root;
}