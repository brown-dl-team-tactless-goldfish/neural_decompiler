
int is_max(int* A, int i, int j){
    int x,y;
    if(i>=j) return 0;
    if(i<j && A[i]<=A[j]) return j-i;
    
    x= is_max(A, i+1, j);
    y= is_max(A, i, j-1);
    return x>y ? x:y;
}
int maxWidthRamp(int* A, int ASize){
    return is_max(A,0,ASize-1);
}

