      for(auto c : s2){  // Similarlly do it for string 2!!
        if(c == ' '){
            map[w]++;
            w = "";
        }
          else
        w += c;
    }
    map[w]++;      /*Now increase the frequency of word if present already in map it will be 2 otherwise the unique words will have frequency equal to 1.!! */
    for(auto i : map){
        if(i.second == 1){
            res.push_back(i.first); // Push unique words i.e words having frequency as 1 in result vector
        }
    }
    return res;
    
}