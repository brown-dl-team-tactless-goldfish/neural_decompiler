class Solution {
public:
    int minimumOneBitOperations(int n) {
        int ret = 0;
        for (int j = 1; n; j *= -1, n &= (n - 1)) 
            ret += j * ((n & -n) * 2 - 1);
        return abs(ret);
    }
};