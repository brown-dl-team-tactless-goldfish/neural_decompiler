int min(int x, int y)
{
    return x < y ? x : y;
}
int minFlipsMonoIncr(char * s)
{
    int ans = 0;
    int ones = 0;
    for(int i = 0; s[i] != '\0'; i++)
    {
        if(s[i] == '0')
            ans = min(ans + 1, ones);
        else
            ones++;
    }
    return ans;
}