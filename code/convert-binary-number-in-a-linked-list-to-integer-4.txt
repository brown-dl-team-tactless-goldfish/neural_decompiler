int getDecimalValue(struct ListNode* head){
    
    register int n = 0; 
    
    while(head != NULL){
        n = n << 1; 
        
        // OR that stuff into the value
        n |= head->val;
        
        head = head->next; 
    }
    
    return n; 
}
