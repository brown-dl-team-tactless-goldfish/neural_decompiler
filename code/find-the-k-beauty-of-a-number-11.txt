int divisorSubstrings(int num, int k) {
    string res=to_string(num);
    int count=0;
    for(int i=0;i<res.size()-k+1;i++)
    {
        int x=stoi(res.substr(i,k));
         if(x!=0&&num%x==0)
             count++;
    }
    return count;
}