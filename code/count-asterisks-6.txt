int countAsterisks(string s) {
        bool flag=false;
        int count=0;
        for(int i=0;i<s.size();i++){
            if(s[i]=='|'){
                flag=!flag;
            }
            if(!flag && s[i]=='*'){
                count++;
            }
        }
        return count;
    }