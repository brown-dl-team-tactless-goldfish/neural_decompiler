class Solution:
    def countPoints(self, rings: str) -> int:
        rgb = {"R": 4, "G": 2, "B": 1}
        counter = [0] * 10
        for x in range(0, len(rings), 2):
            counter[int(rings[x + 1])] |= rgb[rings[x]]
        return counter.count(7)

