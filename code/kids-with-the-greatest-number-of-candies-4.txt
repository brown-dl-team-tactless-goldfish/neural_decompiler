    int max=0, i;
    *returnSize=candiesSize;
    bool* greatest;
    greatest = (bool*)malloc(sizeof(bool)*candiesSize);
    for(i=0;i<candiesSize;i++)
        if(candies[i]>max) max= candies[i];
    for(i=0;i<candiesSize;i++)
            greatest[i] = candies[i]+extraCandies>=max;
    return greatest;
	``